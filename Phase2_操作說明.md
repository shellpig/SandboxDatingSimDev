# Sandbox Dating Sim - Phase 2 藍圖測試操作說明

本手冊提供 Phase 2 階段產出的「提示詞生成器」與「藍圖推演引擎」的使用教學。

---

## 1. 快速啟動 (批次檔說明)

我們在專案根目錄準備了兩個快捷批次檔 (`.bat`)，它們會自動處理繁瑣的路徑與環境變數，您只需要 **直接點擊兩下 (Double-Click)** 即可開啟功能：

### 🛠️ `build_prompt.bat` (產生 AI 提示詞)
- **用途**：讀取您在 Phase 1 設定好的 Setup Package，並自動生成一份 Markdown 提示詞。
- **後續動作**：您可以直接把產出的提示詞複製給 AI (如 ChatGPT 或 Claude)，AI 就會依照極度嚴格的規則，幫您生成符合規格的 `Event Blueprint` 劇情與腳本。
- **產出位置**：`outputs/<world_id>/prompts/<world_id>_event_blueprint_prompt.md`

### 🎮 `play_walkthrough.bat` (藍圖推演遊玩)
- **用途**：這是一個文字冒險遊戲介面！當您把 AI 寫好的腳本放進 `event_blueprints/` 資料夾後，就能用這個工具親自遊玩、測試邏輯與劇情分歧。
- **驗證機制**：啟動時，系統會先對藍圖進行嚴格的除錯 (Linter)，如果有邏輯錯誤會阻擋您遊玩並列出錯誤列表。

---

## 2. 藍圖推演 (Walkthrough) 模式與操作

當您執行 `play_walkthrough.bat` 進入遊戲後，系統會詢問您想要如何遊玩：

### 🏁 啟動方式 (Start Mode)
* **`[1] beginning`**：從頭開始，主角將回到 Day 1 的第一個初始事件。
* **`[2] checkpoint`**：從存檔點繼續。如果您之前遊玩時有存檔，可以選擇此模式接續進度。

### 👁️ 遊玩視角 (Test Mode)
* **`[1] omniscient` (全知模式 / 測試用)**：無論您身在何處，畫面會直接列出世界上「目前條件吻合」的所有可觸發事件與動作。適合開發者快速跳轉測試不同路線。
* **`[2] normal` (正常模式 / 玩家視角)**：完全模擬玩家體驗。您在自由行動 (Free Roam) 階段只能看到開放的「地點清單」。您必須先進入某個地點，才能看到該地點裡面目前有哪些事件或動作可以執行。

---

## 3. 遊戲中的指令列表 (Commands)

遊戲進行中，畫面會不斷提示您當前的狀態：
> `=== 2026-05-01 morning @ protagonist_home ===`

當系統跳出 `Command (<number>, s/save, state, history, graph, restart, q/quit)` 提示時，您可以輸入以下指令：

| 指令 | 說明 |
| :--- | :--- |
| **輸入數字** (如 `1`) | 選擇畫面上的對應選項（例如進入地點、觸發事件、回答對話選項、或者進行 Rest/Wait 動作）。 |
| **`state`** | 偷看「幕後數值」。列出主角目前身上生效的旗標 (Flags)、好感度 (Favor)、屬性 (Stats)、以及倒數中的負面/正面狀態 (Active Statuses)。 |
| **`history`** | 查看玩家從開始到現在的操作軌跡（包含了您在哪一天、哪個時段做了什麼選擇）。 |
| **`s`** 或 **`save`** | **存檔！** 會要求您輸入存檔標籤（Label），然後存入 `outputs` 資料夾中。*(注意：只有在自由行動階段可以存檔，事件劇情中無法存檔)* |
| **`graph`** | **匯出路線圖！** 系統會自動解析這個世界的藍圖，並在 `outputs/.../route_graphs/` 資料夾下產生一個可用 Mermaid 渲染的分歧路線圖 (`.md` 檔)。 |
| **`restart`** | **重新開始**。如果您是從存檔開始的，它會回到該存檔的起始狀態；如果是從頭開始的，則直接回歸原點，並清空歷史紀錄。 |
| **`q`** 或 **`quit`** | 離開遊戲。 |
