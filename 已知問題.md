# 已知問題2

本文件只保留目前尚未完成或尚未通過驗收的項目。`舊文件/` 仍可忽略；規格與實作以根目錄最新文件為準。

## Phase 2：Event Blueprint MVP 規格已確定，待實作

狀態：規格已記入文件，尚未實作。

規格確定日期：2026-05-04

規格記錄於：

- `開發設計方針.md` v1.26 — Phase 2 完整開發規格（2-A-0 / 2-A / 2-B / 2-C / 2-D）與 2-D 模糊點補強。
- `測試指南.md` v1.16 — Phase 2 完整測試規格、必測項目、手動驗收與通過標準。

Phase 2 之前既有已知問題後續另行處理；本節只追蹤 Phase 2 新主線。

### 2-A-0 Phase 2 Setup Prerequisites

狀態：已實作。

決議摘要：

- 新 Setup Package 必須包含 `protagonist_home`。
- `protagonist_home` 是普通 `Location`，靠固定 ID + `system/home/rest/private` tags 表示系統必備地點，不新增 schema 欄位。
- UIW 初始化自動建立 `protagonist_home`，且不可刪除。
- `protagonist_home.available_time_slots` 必須等於 `world.time_slots`。
- 所有 location `available_time_slots` 必須是 `world.time_slots` 子集。
- `export-setup` 預設輸出到 `outputs/<world_id>/setup_package/<world_id>_setup_package.md`。
- 同名檔案存在時直接報錯，不覆寫、不自動 rename、不 version bump。

影響範圍：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- `src/sandbox_dating_sim/uiw/defaults.py`
- `src/sandbox_dating_sim/uiw/linter.py`
- `src/sandbox_dating_sim/cli.py`
- setup fixtures / UIW tests / CLI tests

### 2-A Event Blueprint Schema

狀態：已實作。

決議摘要：

- 新增 `src/sandbox_dating_sim/schema/blueprint.py`。
- Event Blueprint MD 採 Markdown shell + exactly one YAML code block；canonical data 只有 YAML block。
- 檔名固定為 `outputs/<world_id>/event_blueprints/<world_id>_event_blueprint.md`。
- `blueprint_id` 固定為 `<world_id>_event_blueprint`。
- `initial_event_id` 必填，由 AI 在 Blueprint 中生成並引用 opening event。
- Event 必須有 `scene_summary`、`expected_assets`、`time_cost`、`repeat_policy`。
- `repeat_policy` 只允許 `once` / `daily`。
- Event priority 使用 `critical/main/route/normal/ambient`。
- 每個 event 必須有 1 到 4 個 choices。
- Choice 必須有 `choice_label` / `choice_intent` / `result`。

影響範圍：

- `src/sandbox_dating_sim/schema/blueprint.py`
- `tests/test_blueprint_schema.py`
- `tests/fixtures/blueprint_*.md`

### Phase 2 strict DSL

狀態：待實作。

決議摘要：

- `conditions` / `choices[].result` 維持 `list[str]`，但必須符合嚴格可解析 DSL。
- Blueprint DSL 只支援 boolean flags；`new_flags_proposed` 只允許 boolean。
- stats 只支援 `INT/CHA/STR/MORAL/Cash/Debt`。
- character DSL 只支援 `character.<id>.favor`。
- status DSL 支援 active/inactive；target 必須符合 `StatusFlag.targets`。
- 不支援 `item.*` / `inventory.*`；劇情物品以 boolean flag 表示。
- status 變更必須透過 event result 明確設定，不依賴 `clear_rule`。
- location `unlock_conditions` 為 AND，`closed_conditions` 為 OR。
- ending 全部由 event result `ending:<ending_id>` 顯式觸發。

影響範圍：

- `src/sandbox_dating_sim/validation/blueprint_linter.py`
- `src/sandbox_dating_sim/walkthrough/blueprint_walkthrough.py`
- DSL parser helper（實作時可內聚於 linter/walkthrough 或獨立模組）

### 2-B Blueprint Prompt Builder

狀態：已實作。

決議摘要：

- 新增 `src/sandbox_dating_sim/prompts/blueprint_prompt.py`。
- Prompt Builder 產出章節化 Markdown prompt document。
- CLI 只做互動式：`python -m sandbox_dating_sim.cli build-blueprint-prompt`。
- Scope 三選一：`minimal_complete`（預設）、`ai_decides`、`custom`。
- 產出路徑：`outputs/<world_id>/prompts/<world_id>_event_blueprint_prompt.md`。
- 同名存在直接報錯。
- Prompt 必須要求 AI 生成 opening event、cover all endings、建立 fallback ending event、不生成完整對話、不使用 item/inventory DSL。

影響範圍：

- `src/sandbox_dating_sim/prompts/blueprint_prompt.py`
- `src/sandbox_dating_sim/cli.py`
- `tests/test_blueprint_prompt.py`

### 2-C Blueprint Parser + Linter

狀態：已實作。

決議摘要：

- 新增 `src/sandbox_dating_sim/pipeline/blueprint_parser.py`。
- Parser 只接受 exactly one YAML code block。
- 新增 `src/sandbox_dating_sim/validation/blueprint_linter.py`。
- Linter 沿用 `Issue` / `ValidationReport`。
- 提供兩層介面：
  - `validate_blueprint_only(blueprint)`
  - `validate(setup_package, blueprint)`
- 每個 SetupPackage ending 必須至少被一個 `ending:` result 引用，否則 error。
- 必須有至少一個 `fallback_ending` event 且該 event 有 ending result，否則 error。
- Direct `goto:<event_id>` target time_slot 必須符合 source + effective time_cost 的時間推進，否則 error。
- `validate-blueprint` CLI 只做互動式，只印 terminal report，不寫檔、不自動修 Blueprint。

影響範圍：

- `src/sandbox_dating_sim/pipeline/blueprint_parser.py`
- `src/sandbox_dating_sim/validation/blueprint_linter.py`
- `src/sandbox_dating_sim/cli.py`
- `tests/test_blueprint_parser.py`
- `tests/test_blueprint_linter.py`
- `tests/fixtures/blueprint_*.md`

### 2-D Blueprint Logic Walkthrough

狀態：待實作。

決議摘要：

- 2-D 是人工 Blueprint Logic Walkthrough，不是完整 runtime simulator，也不是 Phase 3 Route Validator。
- 先做純函式 engine，再做 CLI 薄殼。
- CLI 只做互動式：`python -m sandbox_dating_sim.cli walkthrough-blueprint`。
- 由 `outputs/` 選 world / setup，並自動搭配 `<world_id>_event_blueprint.md`。
- 初始 state 固定從 `world.start_date`、第一個 `world.time_slots`、`protagonist_home`、`initial_event_id` 開始。
- `event_visit_log` 結構為 `dict[str, list[date]]`；`once` 檢查 key 存在、`daily` 檢查當日是否在 list 中。
- 啟動序列：`create_initial_walkthrough_state()` → 立即 `enter_event(initial_event_id)`；`free_roam` 只是 state struct 初始值。
- status activation：`status = true` 從 `StatusFlag.duration` 初始化 remaining 計數；同 target 同 status 重複觸發覆蓋（重設倒數）。
- choice 可選性：`effective_time_cost(choice) <= remaining_slots_today`；不可選 choice 隱藏，事件須至少一個 choice 可選才顯示。
- `dead_end` 限定 `current_date > world.end_date`；`blocked` 限定 goto 失敗或 free_roam 無任何可選項。
- 2-D 會處理：
  - time_cost 時間推進。
  - choice-level `time_cost:` override。
  - location unlock/closed 粗篩。
  - status `time_slots` / `days` duration 倒數。
  - repeat `once` / `daily`。
  - critical event 遮蔽低 priority events。
  - direct goto runtime checks。
  - ending reached / dead_end / blocked mode。
- 2-D 不處理：
  - NPC schedule 強制。
  - pathfinding。
  - travel cost。
  - `StatusFlag.effect`。
  - `clear_rule` 自動執行。
- 啟動後（選完 beginning 或 checkpoint 後）選擇測試模式，session 中不切換：
  - **全知方式**：free_roam 依 location 分組列出所有可觸發事件與 action，以全域數字選擇。
  - **正常模式**：free_roam 只列有可選事件的地點名稱，以全域數字選地點，進入地點後才顯示該地點事件與 action。
- 支援指令：`<number>`、`s/save`、`state`、`history`、`graph`、`restart`、`help`、`q/quit`。
- checkpoint 可在 `free_roam` / `dead_end` / `blocked` / `ending_reached` 保存；event 中不可保存。
- checkpoint 保存完整 state + history，檔名 `cp_YYYYMMDD_HHMMSS.yaml`。
- `graph` 輸出 Mermaid + adjacency summary；不畫 `free_roam -> events` 動態邊。

影響範圍：

- `src/sandbox_dating_sim/walkthrough/blueprint_walkthrough.py`
- `src/sandbox_dating_sim/walkthrough/checkpoint.py`
- `src/sandbox_dating_sim/walkthrough/route_graph.py`
- `src/sandbox_dating_sim/cli.py`
- `tests/test_blueprint_walkthrough.py`
- `tests/test_blueprint_checkpoint.py`
- `tests/test_blueprint_route_graph.py`

## Phase 1-G-8 驗證後待處理問題

狀態：部分已解決；剩餘 1-G-8 UI 行為測試覆蓋不足。

驗證日期：2026-05-03

驗證結果摘要：

- 1-G-8 目標測試：提升權限重跑後 `62 passed in 1.02s`。
- Phase 1 非 integration 全回歸：提升權限重跑後 `109 passed in 1.19s`。
- 2026-05-03 針對本節三項重驗：`tests/test_uiw_1g8.py tests/test_streamlit_uiw.py` 通過，結果 `18 passed in 0.84s`。
- 新增 Status 表單 UI 規格缺口已處理。
- 新增 Flag / Status 表單驗證失敗後可能清空輸入的功能面已處理。
- 1-G-8 UI 行為測試已有補強，但仍待補齊完整手動驗收覆蓋。

### 1. 新增 Status 表單未完全符合 1-G-8 UI 規格

狀態：已處理。

原始問題：

- 新增 Status Flag 表單目前永遠顯示 `duration.value` 的「持續值」欄位。
- 規格要求：
  - `duration.type in {"time_slots", "days"}` 時才顯示 value，且必填正整數。
  - `duration.type in {"until_event", "until_cleared"}` 時不顯示 value，canonical 存 `None`。
  - `duration.type == "permanent"` 時不顯示 value，canonical 存 `None`，且必填 `permanent_reason`。
- 新增 Status Flag 時，目前未在 UI submit 階段擋下：
  - 非永久狀態 `clear_rule` 空。
  - `permanent` 狀態缺 `permanent_reason`。

影響範圍：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- Streamlit UIW 的「旗標與狀態(Flags & Status)」分頁。
- 使用者可能新增出不符合 1-G-8 UX 規格的狀態；後續才由 linter 或 schema 暴露問題。

處理結果：

- 新增 Status Flag 表單已改為與既有 Status inline 編輯區對齊。
- `duration.type in {"time_slots", "days"}` 時才顯示「持續值」欄位。
- `duration.type in {"until_event", "until_cleared", "permanent"}` 時不顯示 value widget，寫入 `None`。
- submit 前已補驗證：
  - `targets` 不可空。
  - `effect` 必須 parse 成非空 `list[dict]`。
  - 非永久狀態 `clear_rule` 不可空。
  - permanent 狀態 `permanent_reason` 必填。
- 待補測試覆蓋新增 Status 表單的 duration/value/clear_rule/permanent 行為；此項歸入下一節「UI 行為測試覆蓋不足」。

### 2. 1-G-8 UI 行為測試覆蓋不足

狀態：已補強；仍待補齊完整手動驗收覆蓋。

問題描述：

- 目前 1-G-8 測試已覆蓋 schema / parser / linter / 純函式 helper。
- 目前也新增了部分 Streamlit UI 測試：
  - 1-G-5 角色刪除防呆會被 `status_flags[].targets` 阻擋。
  - status inline 編輯時，permanent 狀態缺 `permanent_reason` 會報錯且不儲存。
  - flag inline 編輯儲存 / 取消 / 刪除防呆。
  - 新增 Flag 驗證失敗後 draft state 保留，成功後清除 draft state。
  - 多筆 flag/status 同時展開不產生 duplicate widget key。
  - 新增 Status Flag 的 `duration.type=until_event` 時，`duration.value` 寫入 `None`。
- 但 Streamlit UI 行為測試仍不足，尚不能證明 1-G-8 手動驗收清單已被自動測試保護。

影響範圍：

- `tests/test_uiw_1g8.py`
- `tests/test_streamlit_uiw.py` 或新增專用 UI 測試檔。
- 後續修改 flag/status UI 時，可能回歸：
  - inline save/cancel 不正確。
  - 刪除引用防呆漏判。
  - duration UI 顯示錯誤。
  - effect YAML 驗證不足。
  - duplicate widget key。
  - keyed session state 清理不完整。

建議補測：

- status inline 編輯儲存 / 取消 / 刪除防呆。
- status `targets` multiselect：主角、NPC、dangling target、dedupe。
- duration.type 每個選項的 value 顯示與 canonical 寫入，目前只覆蓋 `until_event`。
- 新增 / 編輯 permanent 缺 `permanent_reason` 不儲存，目前只覆蓋既有 status inline 編輯。
- effect YAML UI corner cases：空字串、非 list、list 元素非 dict、dict key 非 str。
- 成功刪除 status 後清空對應 keyed session state。
- 新增 Status 表單驗證失敗後 draft state 仍保留，目前僅以臨時 harness 驗證，尚未納入正式測試。
- 新增 Status 表單的 `clear_rule` / `permanent` 行為。

### 3. 新增 Flag / Status 表單驗證失敗後可能清空輸入

狀態：功能面已處理；測試覆蓋仍併入前一節追蹤。

原始問題：

- 新增 Flag / Status Flag 表單目前仍使用 `clear_on_submit=True`。
- Streamlit 的 `clear_on_submit=True` 不區分 submit 成功或驗證失敗；驗證失敗也可能清空使用者已輸入內容。
- 這與前面 1-G-6 / 1-G-7 已建立的「驗證失敗保留 draft state」UX 原則不一致。

影響範圍：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- 旗標與狀態新增流程。
- 使用者在 ID 格式錯誤、ID 重複、targets 空、effect YAML 錯誤等情況下，可能需要重新輸入整份表單。

處理結果：

- 新增 Flag / Status Flag 已不再使用 `st.form(clear_on_submit=True)`。
- 新增 Flag / Status Flag 已改用 keyed widgets + button。
- 驗證失敗時不會進入成功清除 draft keys 與 `st.rerun()` 的路徑。
- 新增成功後才手動清除該表單 draft keys，append canonical data，然後 `st.rerun()`。
- 新增 Flag 驗證失敗後 draft key 保留已納入自動測試。
- 新增 Status 驗證失敗後 draft key 保留已用臨時 harness 驗證通過，仍待納入正式自動測試；此項歸入前一節「UI 行為測試覆蓋不足」。

## Phase 1-G-9：ScheduleEntry `specific_date` schema 升級與 `schedule_id` 自動生成

狀態：規格已確定，待實作。

規格確定日期：2026-05-03

規格記錄於：

- `開發設計方針.md` v1.24 — 完整規格與漏洞補強（schema、linter、UI、exporter/parser、fixture、schedule ID 全域檢查、specific_date tie key）。
- `測試指南.md` v1.15 — 測試要求、必測項目、手動驗收清單 #1-#12、通過標準。

### 1. `ScheduleEntry` 沒有 `specific_date` 欄位

狀態：已實作。

決議摘要：

- 新增 `ScheduleEntry.specific_date: date | None = None`。
- 跨欄位邏輯不加 `model_validator`，只靠 UIW Linter（三條 error）。
- UIW `day_type` selectbox 加回 `特定日期(specific_date)`，選中時動態顯示 `st.date_input`（min/max 鎖定世界日期範圍）。
- 非 `specific_date` submit 必須寫入 `None`，不得保存 stale 日期。
- exporter / parser 不改碼，`exclude_none=True` 與 Pydantic default 已處理。
- `setup_minimal.yaml` 新增一筆 `specific_date` fixture。
- `schedule_tie` 必須區分不同 `specific_date` 實際日期：不同日期不報 tie，同日期才照既有規則報 tie。

### 2. `schedule_id` 無自動生成

狀態：已實作。

決議摘要：

- 比照 1-G-7 自動 ID：保留手動輸入欄位，提示「行程 ID (英文，留空則自動產生)」。
- 留空時 `_make_unique_id` 自動生成：prefix `sch_`，slug `{character_id}_{day_type}_{time_slot}`，suffix 避重。
- `schedule_id` 必須納入 UIW Linter `_check_ids` 的全域 `invalid_id_format` / `duplicate_id` 檢查。
- `_collect_existing_ids()` 必須納入所有既有 `characters[].schedule[].schedule_id`，供自動避重與手動 ID 擋重複使用。
- 手動輸入非法或重複 `schedule_id` 時，不新增、不清 draft、不走成功 rerun。
- 自動 ID 成功訊息需在 rerun 後仍可見；單純 mock `st.caption` 呼叫不足以代表實機通過。
