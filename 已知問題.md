# 已知問題2

本文件只保留目前尚未完成或尚未通過驗收的項目。`舊文件/` 仍可忽略；規格與實作以根目錄最新文件為準。

## Phase 1-G-8 驗證後待處理問題

狀態：部分已解決；剩餘 1-G-8 UI 行為測試覆蓋不足。

驗證日期：2026-05-03

驗證結果摘要：

- 1-G-8 目標測試：提升權限重跑後 `62 passed in 1.02s`。
- Phase 1 非 integration 全回歸：提升權限重跑後 `109 passed in 1.19s`。
- 2026-05-03 針對本節三項重驗：`tests/test_uiw_1g8.py tests/test_streamlit_uiw.py` 通過，結果 `18 passed in 0.84s`。
- 新增 Status 表單 UI 規格缺口已處理。
- 新增 Flag / Status 表單驗證失敗後可能清空輸入的功能面已處理。
- 1-G-8 UI 行為測試已有補強，但仍待補齊完整手動驗收覆蓋。

### 1. 新增 Status 表單未完全符合 1-G-8 UI 規格

狀態：已處理。

原始問題：

- 新增 Status Flag 表單目前永遠顯示 `duration.value` 的「持續值」欄位。
- 規格要求：
  - `duration.type in {"time_slots", "days"}` 時才顯示 value，且必填正整數。
  - `duration.type in {"until_event", "until_cleared"}` 時不顯示 value，canonical 存 `None`。
  - `duration.type == "permanent"` 時不顯示 value，canonical 存 `None`，且必填 `permanent_reason`。
- 新增 Status Flag 時，目前未在 UI submit 階段擋下：
  - 非永久狀態 `clear_rule` 空。
  - `permanent` 狀態缺 `permanent_reason`。

影響範圍：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- Streamlit UIW 的「旗標與狀態(Flags & Status)」分頁。
- 使用者可能新增出不符合 1-G-8 UX 規格的狀態；後續才由 linter 或 schema 暴露問題。

處理結果：

- 新增 Status Flag 表單已改為與既有 Status inline 編輯區對齊。
- `duration.type in {"time_slots", "days"}` 時才顯示「持續值」欄位。
- `duration.type in {"until_event", "until_cleared", "permanent"}` 時不顯示 value widget，寫入 `None`。
- submit 前已補驗證：
  - `targets` 不可空。
  - `effect` 必須 parse 成非空 `list[dict]`。
  - 非永久狀態 `clear_rule` 不可空。
  - permanent 狀態 `permanent_reason` 必填。
- 待補測試覆蓋新增 Status 表單的 duration/value/clear_rule/permanent 行為；此項歸入下一節「UI 行為測試覆蓋不足」。

### 2. 1-G-8 UI 行為測試覆蓋不足

狀態：已補強；仍待補齊完整手動驗收覆蓋。

問題描述：

- 目前 1-G-8 測試已覆蓋 schema / parser / linter / 純函式 helper。
- 目前也新增了部分 Streamlit UI 測試：
  - 1-G-5 角色刪除防呆會被 `status_flags[].targets` 阻擋。
  - status inline 編輯時，permanent 狀態缺 `permanent_reason` 會報錯且不儲存。
  - flag inline 編輯儲存 / 取消 / 刪除防呆。
  - 新增 Flag 驗證失敗後 draft state 保留，成功後清除 draft state。
  - 多筆 flag/status 同時展開不產生 duplicate widget key。
  - 新增 Status Flag 的 `duration.type=until_event` 時，`duration.value` 寫入 `None`。
- 但 Streamlit UI 行為測試仍不足，尚不能證明 1-G-8 手動驗收清單已被自動測試保護。

影響範圍：

- `tests/test_uiw_1g8.py`
- `tests/test_streamlit_uiw.py` 或新增專用 UI 測試檔。
- 後續修改 flag/status UI 時，可能回歸：
  - inline save/cancel 不正確。
  - 刪除引用防呆漏判。
  - duration UI 顯示錯誤。
  - effect YAML 驗證不足。
  - duplicate widget key。
  - keyed session state 清理不完整。

建議補測：

- status inline 編輯儲存 / 取消 / 刪除防呆。
- status `targets` multiselect：主角、NPC、dangling target、dedupe。
- duration.type 每個選項的 value 顯示與 canonical 寫入，目前只覆蓋 `until_event`。
- 新增 / 編輯 permanent 缺 `permanent_reason` 不儲存，目前只覆蓋既有 status inline 編輯。
- effect YAML UI corner cases：空字串、非 list、list 元素非 dict、dict key 非 str。
- 成功刪除 status 後清空對應 keyed session state。
- 新增 Status 表單驗證失敗後 draft state 仍保留，目前僅以臨時 harness 驗證，尚未納入正式測試。
- 新增 Status 表單的 `clear_rule` / `permanent` 行為。

### 3. 新增 Flag / Status 表單驗證失敗後可能清空輸入

狀態：功能面已處理；測試覆蓋仍併入前一節追蹤。

原始問題：

- 新增 Flag / Status Flag 表單目前仍使用 `clear_on_submit=True`。
- Streamlit 的 `clear_on_submit=True` 不區分 submit 成功或驗證失敗；驗證失敗也可能清空使用者已輸入內容。
- 這與前面 1-G-6 / 1-G-7 已建立的「驗證失敗保留 draft state」UX 原則不一致。

影響範圍：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- 旗標與狀態新增流程。
- 使用者在 ID 格式錯誤、ID 重複、targets 空、effect YAML 錯誤等情況下，可能需要重新輸入整份表單。

處理結果：

- 新增 Flag / Status Flag 已不再使用 `st.form(clear_on_submit=True)`。
- 新增 Flag / Status Flag 已改用 keyed widgets + button。
- 驗證失敗時不會進入成功清除 draft keys 與 `st.rerun()` 的路徑。
- 新增成功後才手動清除該表單 draft keys，append canonical data，然後 `st.rerun()`。
- 新增 Flag 驗證失敗後 draft key 保留已納入自動測試。
- 新增 Status 驗證失敗後 draft key 保留已用臨時 harness 驗證通過，仍待納入正式自動測試；此項歸入前一節「UI 行為測試覆蓋不足」。

## Phase 1-G-9：ScheduleEntry `specific_date` schema 升級

狀態：待處理。

提出日期：2026-05-01

### 1. `ScheduleEntry` 沒有 `specific_date` 欄位

問題描述：

- `DayType = Literal["weekday", "weekend", "holiday", "specific_date", "any"]` 已宣告 `specific_date` 為合法選項。
- 但 `ScheduleEntry` 目前沒有對應的 `specific_date: date | None` 欄位。
- 使用者即使選擇 `day_type = specific_date`，也沒有地方填入實際日期。
- canonical data、exporter、parser 也無法保存或還原指定日期。

目前暫行處理：

- Interactive UIW 行程表單暫時不顯示 `specific_date` 選項。
- canonical schema 仍保留 `specific_date` 字面值，避免破壞既有資料。

影響範圍：

- `src/sandbox_dating_sim/schema/setup.py`
- `src/sandbox_dating_sim/uiw/linter.py`
- `src/sandbox_dating_sim/pipeline/setup_exporter.py`
- `src/sandbox_dating_sim/pipeline/setup_parser.py`
- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- `tests/`

建議修法：

- 新增 `ScheduleEntry.specific_date: date | None = None`。
- UIW Linter 新增規則：
  - `day_type == "specific_date"` 時 `specific_date` 必填。
  - `day_type != "specific_date"` 時 `specific_date` 必須為 `None`。
  - `specific_date` 必須落在 `World.start_date` 到 `World.end_date` 範圍內。
- Interactive UIW 行程表單依 `day_type` 動態顯示日期欄位。
- exporter / parser roundtrip 保留 `specific_date`。
- 補 schema、linter、exporter、parser 與 Streamlit UI 測試。
