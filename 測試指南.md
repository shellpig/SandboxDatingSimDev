# SandboxDatingSimDev 測試指南

## 版本紀錄

| 版本 | 日期 | 內容 |
| :--- | :--- | :--- |
| v1.16 | 2026-05-04 | 完整新增 Phase 2 測試規格：2-A-0 `protagonist_home` 必備與固定 output layout；2-A Event Blueprint schema（`initial_event_id`、1-4 choices、`choice_label`、`scene_summary`、`repeat_policy`、`expected_assets`）；2-B 互動式章節化 prompt `.md`；2-C 單一 YAML block parser、blueprint-only / full linter、嚴格 DSL、ending coverage、fallback ending、direct goto time；2-D Blueprint Logic Walkthrough engine、時間推進、location unlock/closed、status duration、repeat once/daily、critical 遮蔽、checkpoint、Mermaid graph 與互動式 CLI 驗收。 |
| v1.15 | 2026-05-03 | 補強 Phase 1-G-9 測試漏洞：新增 `schedule_id` 納入 UIW Linter 全域 ID 檢查（非法格式、跨類型重複、schedule 間重複）；`_collect_existing_ids()` 必須納入所有 schedule ID；schedule 自動 ID 避重需覆蓋跨角色與既有手動 schedule ID；`specific_date` 的 `schedule_tie` 必須區分不同實際日期；非 `specific_date` submit 不得保存 stale 日期；caption / toast / pending message 需在 rerun 後可見；UI mock 必須提供 `start_date` / `end_date` 與 `date_input` 合法回傳。 |
| v1.14 | 2026-05-03 | 新增 Phase 1-G-9 測試要求：`ScheduleEntry.specific_date` schema 合法/非法案例；UIW Linter 新增 `missing_specific_date` / `unexpected_specific_date` / `specific_date_out_of_range` 三條 error 必測（含邊界值）；exporter/parser roundtrip 含 `specific_date` 保留與 `None` 不輸出；`schedule_id` 自動生成（prefix `sch_`、suffix 避重、手動輸入保留）；`setup_minimal.yaml` 新增 `specific_date` fixture；UI 行為含 `day_type` 五選項、動態 `date_input`、清單日期顯示；手動驗收 #1-#8。 |
| v1.13 | 2026-05-03 | 修正 1-G-7 / 1-G-8 測試 hang 風險：`_parse_effect_yaml` 補 `isinstance(yaml_str, str)` 非字串拒絕案例為必測；`tests/test_uiw_1g7.py::apply_common_mocks` 必須補 `mock_st.text_area.side_effect`，否則 `_tab_flags` 內的 status form `text_area` 會回 MagicMock 並讓 `yaml.safe_load` 在 PyYAML Reader 死迴圈；通用測試準則新增「凡 `mock_st.form_submit_button.return_value = True` 的測試，所有與被測 tab 同層的 widget（含未必驗證的後段 form）都必須 mock 出合法 default」一條。 |
| v1.12 | 2026-05-03 | 補強 Phase 1-G-8 測試要求：拆出純函式單測層（`_normalize_legacy_status` / `_parse_effect_yaml` / `_status_targets_options` / `_has_flag_reference` / `_has_status_reference`）為必跑；明列空 `targets` 由 schema `Field(min_length=1)` 拒絕、`model_validator(mode="before")` normalize 落點、舊 `target` 直 kwarg 構造仍合法；新增 `setup_legacy_status_target.yaml` 相容 fixture 與既有 fixture / 測試遷移為 `targets=[...]`；UIW Linter 補測 `unknown_status_target` / `empty_status_targets` / `unexpected_duration_value`；引用比對 word boundary 必測；`duration.type ∈ {until_event, until_cleared}` 不顯示 value 並存 None；補 effect YAML corner cases、targets UI dedupe、silent migration 端對端驗收 #13、widget key prefix 驗收 #14、1-G-5 角色刪除引用 targets 回歸 #15、實機 end-to-end #16；通過標準明列 1-G-7 `_make_unique_id` / `_generate_system_id` 簽章不變。 |
| v1.11 | 2026-05-03 | 新增 Phase 1-G-8 測試要求：旗標 / 狀態頁 inline 編輯、刪除引用防呆、keyed state 清理；`StatusFlag.targets` 複選 schema/parser/exporter/linter 對齊；duration.type 中文(英文) UI 與說明 caption；effect YAML 驗證與 roundtrip；`status_id` linter 補洞列為已先行修復回歸。 |
| v1.10 | 2026-05-03 | 補強 Phase 1-G-7 測試要求：補 `_slugify_label` / `_make_unique_id` 純函式單測為必跑項目；suffix 連鎖測試擴充至 `_3`、`_4` 並挑邊「順序遞增取最小未占用」；驗收 #6/#7/#13 改寫為可實際操作的步驟；新增驗收 #14（自動 ID 失敗保留 draft）、#15（submit 後 caption 預覽）、#16（1-G-3 `_generate_system_id` 行為不變回歸）、#17（end-to-end UIW → export → parse roundtrip）；模板衝突挑邊為「自動 suffix 避讓」；補進階手動 ID 擋 `protagonist`。 |
| v1.9 | 2026-05-03 | 修正 Phase 1-G-7 測試要求：旗標來源欄位限定為 `description`，補 description slug 截斷、中文標點、`unnamed` fallback、結局純拼音範例與不得修改 `_generate_system_id` 既有行為。 |
| v1.8 | 2026-05-03 | 新增 Phase 1-G-7 測試要求：地點 / NPC / 旗標 / 狀態 / 結局新增流程自動產生唯一 canonical ID，覆蓋中文來源、合法格式、跨類型唯一、同名遞增 suffix、匯出 canonical ID 不變。 |
| v1.7 | 2026-05-01 | 新增 Phase 1-G-6 測試要求：分頁拆分、結局清單顯示、inline expander 編輯、priority 與 target 中文化、F-γ multiselect、跨類型 ID 唯一即時擋（UI 層含 `status_id`）、最小可用 Setup Package 匯出 roundtrip；附帶補測 1-G-5 character_id 跨類型唯一即時擋。 |
| v1.6 | 2026-05-01 | 補強 Phase 1-G-5 Streamlit 實作限制相關測試：禁止 nested `st.expander`、多角色同時編輯不可 duplicate widget key、新增角色擋空/重複/非法 `character_id`、編輯期不可改 `character_id`、刪除角色清空相關 session state。 |
| v1.5 | 2026-05-01 | 新增 Phase 1-G-5 測試要求：角色清單顯示、刪除引用防呆、inline expander 編輯、行程清單與新增搬入編輯區、行程 enum 中文化、白名單顯示與進階設定收納；明確 1-G-5 不顯示 `specific_date` 行程選項。 |
| v1.4 | 2026-05-01 | 修正 Phase 1-G-4 測試規格：區分 container 與可進入模板的 time slot 要求、補手動驗收前置條件，並加入 Phase 1-A 到 1-G-3 回歸門檻。 |
| v1.3 | 2026-05-01 | 新增 Phase 1-G-4 測試要求：地點與角色頁順序、地點模板擴充、模板 ID 防重、地點刪除引用檢查、中文化地點類型與時段選項、地點 tags 進階設定。 |
| v1.2 | 2026-05-01 | 補充 Phase 1-G-3 驗收結果與「沒有秘密」canonical 行為：UI 清空 secrets list，輸出 `secrets: []`。 |
| v1.1 | 2026-05-01 | 新增 Phase 1-G-3 測試要求：語意型欄位 `id + label`、主角與 NPC 秘密複選、職業/秘密預設擴充、中文自訂輸入與系統 ID 驗證。 |
| v1.0 | 2026-04-27 | 初版，定義 Phase 0 與 Phase 1-A 到 1-F 的測試流程、執行指令、通過標準與手動驗收清單。 |

> **目的：** 本文件定義 SandboxDatingSimDev 每個 Phase / 子階段的測試流程。
> 包含：環境準備、執行指令、預期結果、通過/失敗判定標準、故障排除。
> **每個子階段測試全數通過，才能進入下一個子階段。**

---

## 測試環境準備

### Step 1：安裝依賴

```powershell
cd "C:\Users\User\OneDrive\桌面\AI_Work\ProjectGpt\SandboxDatingSimDev"
uv sync --extra dev
```

若尚未安裝 `uv`：

```powershell
irm https://astral.sh/uv/install.ps1 | iex
```

驗證：

```powershell
uv --version
uv run python -c "import pydantic, yaml, typer, rich; print('ALL OK')"
```

預期輸出：

```text
ALL OK
```

### Step 2：確認 pytest 設定

`pyproject.toml` 應包含：

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "integration: 需要外部 API、瀏覽器或網路連線的測試",
]
```

Phase 1 不需要外部 AI API。

---

## 測試指令速查表

| 用途 | 指令 |
| :--- | :--- |
| 跑全部單元測試 | `uv run pytest tests/ -v -m "not integration"` |
| 跑全部測試 | `uv run pytest tests/ -v` |
| 跑指定測試檔 | `uv run pytest tests/test_setup_exporter.py -v` |
| 跑單一測試 | `uv run pytest tests/test_setup_parser.py::test_parse_exported_markdown_roundtrip -v` |
| 顯示簡短錯誤 | `uv run pytest tests/ -v --tb=short` |
| 顯示完整錯誤 | `uv run pytest tests/ -v --tb=long` |
| 只收集測試 | `uv run pytest tests/ --collect-only` |
| 覆蓋率 | `uv run pytest tests/ --cov=src --cov-report=term-missing -m "not integration"` |

---

## 測試分層原則

### Schema Validation

Pydantic schema validation 負責資料形狀與基本型別。

應由 schema 測試覆蓋：

- 缺 required field。
- 欄位型別錯誤。
- 巢狀資料結構錯誤。
- enum 值不合法。
- 必填欄位完全不存在。

範例：

```python
def test_schema_rejects_missing_schedule_order():
    """schedule_order 缺欄位時，Pydantic validation fail。"""
```

### UIW Linter

UIW Linter 負責語意、引用與跨欄位邏輯。

應由 linter 測試覆蓋：

- ID 重複。
- ID 格式不合法。
- 欄位存在但值為空陣列或空字串。
- schedule 引用不存在地點。
- container 沒有任何可進入 sub_location。
- ending 引用不存在角色。
- ending 條件引用未宣告 flag。
- status flag 的 `clear_rule` 存在但為空陣列。
- `duration.type: permanent` 但沒有 `permanent_reason`。
- schedule 同角色、同時間段、同 priority 且條件可能同時成立。

範例：

```python
def test_linter_rejects_empty_clear_rule():
    """clear_rule 欄位存在但為空陣列時，由 UIW Linter 回報 issue。"""
```

不要在 UIW Linter 測試中期待「缺 required field」能進入 linter。這類錯誤應由 schema 測試覆蓋。

### Streamlit UI 測試 mock 完整性

當測試使用 `@patch("sandbox_dating_sim.ui.streamlit_uiw.st")` 並設 `mock_st.form_submit_button.return_value = True` 時，**整個被測 `_tab_*` 函式內所有 form 都會被當成已 submit**，不只目標 form。因此：

- 凡是被測 tab 內可能出現的 widget（含未必驗證的後段 form），都必須在共用 mock helper（如 `apply_common_mocks`）中提供合法 default 回值。
- **特別注意需要傳給 yaml/json/regex 等解析器的 widget**：`text_area`（如 `_tab_flags` 的 effect YAML）必須回 `str` default，否則 `MagicMock` 會被 PyYAML `Reader` 當 file-like，呼叫 `stream.read(N)` 永遠拿到 truthy MagicMock 而陷入死迴圈卡死整個 process。
- 範例：

  ```python
  mock_st.text_input.side_effect = lambda label, value="", **kwargs: mock_st.session_state.get(kwargs.get("key"), value)
  mock_st.text_area.side_effect = lambda label, value="", **kwargs: mock_st.session_state.get(kwargs.get("key"), value)
  ```

- 較精細的選擇是改用 `mock_st.form_submit_button.side_effect = lambda label, **kwargs: label == "目標 form 的 submit label"`，讓只有目標 form 真的 submit；若採用此法仍應一併補完 widget mock，避免後段 form 的初始化邏輯（read default）被 MagicMock 污染。
- 對等地，production 端任何接收 widget 結果的 helper（如 `_parse_effect_yaml`）開頭都應補 `isinstance(...)` 等 type guard，作為 runtime 防線而非依賴 type hint。

---

## Phase 0：專案骨架與基礎工具

### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/core/ids.py` | `tests/test_ids.py` |

### 前置條件

- [ ] `pyproject.toml` 已建立。
- [ ] `src/sandbox_dating_sim/core/constants.py` 已建立。
- [ ] `src/sandbox_dating_sim/core/exceptions.py` 已建立。
- [ ] `src/sandbox_dating_sim/core/ids.py` 已建立。

### 執行指令

```powershell
uv run pytest tests/test_ids.py -v
```

預期：

```text
tests/test_ids.py::test_valid_id PASSED
tests/test_ids.py::test_invalid_id_starts_with_number PASSED
tests/test_ids.py::test_invalid_id_contains_chinese PASSED
tests/test_ids.py::test_normalize_id PASSED
```

### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| `tests/test_ids.py` 全部 PASSED | 必須 |
| 中文 ID 不得被自動音譯 | 必須 |

---

## Phase 1：Setup Package MVP

Phase 1 的核心目標：

```text
fixture YAML
-> SetupPackage schema
-> UIW Linter
-> Setup Package MD export
-> Setup Package MD parse
-> roundtrip
-> 1-F 表單原型輸出同樣格式
```

1-F 前置門檻：

> **Phase 1-A 到 1-E 的單元測試必須先全數通過，才能開始 1-F。**

理由：1-F 會輸出複雜巢狀 YAML。如果 parser、exporter 與 roundtrip 尚未穩定，UI 問題會和資料問題混在一起，debug 成本會很高。

---

### 1-A Canonical Setup Schema

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/schema/setup.py` | `tests/test_setup_schema.py` |
| `src/sandbox_dating_sim/schema/validation.py` | `tests/test_setup_schema.py` |

#### 固定資料

| Fixture | 用途 |
| :--- | :--- |
| `tests/fixtures/setup_minimal.yaml` | 合法最小設定 |
| `tests/fixtures/setup_invalid_missing_required.yaml` | 缺 required field |

#### 執行指令

```powershell
uv run pytest tests/test_setup_schema.py -v
```

#### 必測項目

```python
def test_minimal_setup_package_loads():
    """setup_minimal.yaml 可載入為 SetupPackage。"""

def test_schema_rejects_missing_character_id():
    """角色缺 character_id 時 Pydantic validation fail。"""

def test_schema_rejects_missing_schedule_order():
    """schedule 缺 schedule_order 時 Pydantic validation fail。"""

def test_schema_rejects_missing_status_duration():
    """status flag 缺 duration 時 Pydantic validation fail。"""

def test_schema_rejects_missing_clear_rule():
    """status flag 缺 clear_rule 時 Pydantic validation fail。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| 合法 fixture 可載入 | 必須 |
| 缺 required field 由 Pydantic 擋下 | 必須 |
| `uiw_version == "1.2"` | 必須 |

---

### 1-B UIW Linter

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/uiw/linter.py` | `tests/test_uiw_linter.py` |

#### 執行指令

```powershell
uv run pytest tests/test_uiw_linter.py -v
```

#### 必測項目

```python
def test_linter_passes_minimal_fixture():
    """setup_minimal.yaml 應通過。"""

def test_linter_rejects_duplicate_character_id():
    """重複 character_id 應產生 error。"""

def test_linter_rejects_unknown_schedule_location():
    """schedule location_id 不存在應產生 error。"""

def test_linter_rejects_container_without_sub_location():
    """container 沒有可進入 sub_location 應產生 error。"""

def test_linter_rejects_empty_clear_rule():
    """clear_rule 欄位存在但為空陣列時應產生 error。"""

def test_linter_rejects_permanent_without_reason:
    """duration.type=permanent 但缺 permanent_reason 應產生 error。"""

def test_linter_detects_schedule_conflict():
    """同角色同時間段同 priority 且條件可能同時成立時應產生 issue。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| 合法 fixture `status: passed` | 必須 |
| 語意錯誤產生 `ValidationReport.issues` | 必須 |
| issue 至少包含 `severity`、`type`、`path`、`message` | 必須 |

---

### 1-C Setup Package MD Exporter

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/pipeline/markdown.py` | `tests/test_setup_exporter.py` |
| `src/sandbox_dating_sim/pipeline/setup_exporter.py` | `tests/test_setup_exporter.py` |

#### 執行指令

```powershell
uv run pytest tests/test_setup_exporter.py -v
```

#### 必測項目

```python
def test_exporter_outputs_markdown_with_single_yaml_block():
    """輸出包含 # Setup Package，且只有一個主要 yaml code block。"""

def test_exporter_includes_validation_report():
    """輸出 YAML 包含 validation_report。"""

def test_exporter_writes_uiw_version_1_2:
    """輸出 YAML 的 uiw_version 必須是 1.2。"""

def test_write_file_uses_world_id_filename(tmp_path):
    """輸出檔名為 <world_id>_setup_package.md。"""

def test_exporter_preserves_chinese_labels():
    """中文 label 在 YAML 中保留，canonical ID 不被中文取代。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Markdown 可讀 | 必須 |
| YAML 可 parse | 必須 |
| `validation_report` 必定存在 | 必須 |
| `uiw_version` 必定為 `"1.2"` | 必須 |

---

### 1-D Setup Package Parser 與 Roundtrip

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/pipeline/setup_parser.py` | `tests/test_setup_parser.py` |
| `src/sandbox_dating_sim/pipeline/markdown.py` | `tests/test_setup_parser.py` |

#### 執行指令

```powershell
uv run pytest tests/test_setup_parser.py -v
```

#### 必測項目

```python
def test_parse_exported_markdown_roundtrip():
    """SetupPackage -> Markdown -> SetupPackage 後核心欄位一致。"""

def test_parse_rejects_missing_yaml_block():
    """沒有 yaml code block 時拋出 MarkdownParseError。"""

def test_parse_rejects_invalid_schema():
    """YAML 欄位不完整時 validation fail。"""

def test_roundtrip_preserves_nested_locations_and_schedule():
    """巢狀 locations 與 character schedule 在 roundtrip 後一致。"""

def test_roundtrip_preserves_status_flag_lifecycle():
    """duration、clear_rule、permanent_reason 在 roundtrip 後一致。"""

def test_roundtrip_preserves_asset_vocabularies_and_aliases():
    """asset vocabularies 與 alias tables 在 roundtrip 後一致。"""
```

#### Parser 穩健性要求

Parser 必須：

- 只讀取指定的主要 YAML code block。
- 遇到沒有 YAML block 時明確失敗。
- 遇到多個 YAML block 時採用第一個主要 block，或明確要求 exporter 只輸出一個 block。
- 保留中文 label。
- 保留 list 順序。
- 保留 `schedule_order`。
- 保留巢狀資料，例如 `characters[].schedule`。

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| roundtrip 核心欄位一致 | 必須 |
| 巢狀 Location / Schedule roundtrip 一致 | 必須 |
| Status lifecycle roundtrip 一致 | 必須 |
| 中文 label 不遺失 | 必須 |

---

### 1-E CLI：產出 Setup Package MD

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/cli.py` | `tests/test_cli.py` |

#### 執行指令

```powershell
uv run pytest tests/test_cli.py -v
```

手動 smoke test：

```powershell
uv run python -m sandbox_dating_sim.cli export-setup tests/fixtures/setup_minimal.yaml --out examples/
```

預期產物：

```text
examples/<world_id>_setup_package.md
```

#### 必測項目

```python
def test_cli_export_setup_creates_file(tmp_path):
    """CLI 可從 fixture YAML 產出 Setup Package MD。"""

def test_cli_export_setup_output_can_parse(tmp_path):
    """CLI 產出的 MD 可被 SetupPackageParser 讀回。"""

def test_cli_invalid_input_returns_nonzero(tmp_path):
    """不合法 input 應回傳非 0 或丟出清楚錯誤。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| CLI 能產生檔案 | 必須 |
| 產物可 parse 回 `SetupPackage` | 必須 |
| invalid input 不產生半壞輸出 | 必須 |

---

### Phase 1-A 到 1-E 回歸測試

1-F 開始前必跑：

```powershell
uv run pytest `
  tests/test_ids.py `
  tests/test_setup_schema.py `
  tests/test_uiw_linter.py `
  tests/test_setup_exporter.py `
  tests/test_setup_parser.py `
  tests/test_cli.py `
  -v -m "not integration" --tb=short
```

通過標準：

| 條件 | 必要性 |
| :--- | :--- |
| 全部 PASSED | 必須 |
| 無 parser roundtrip 失敗 | 必須 |
| CLI 產物可 parse | 必須 |

> 只有這一關通過，才進入 1-F。

---

### 1-F Interactive UIW Prototype

#### 測試方式

1-F 包含 Streamlit UI，因此採用：

- `tests/test_uiw_defaults.py` 自動測試 defaults。
- Phase 1-A 到 1-E 自動測試保護資料契約。
- 手動驗收清單驗證 UI 行為。

#### 自動測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/uiw/defaults.py` | `tests/test_uiw_defaults.py` |

#### 執行指令

```powershell
uv run pytest tests/test_uiw_defaults.py -v
```

#### 必測項目

```python
def test_style_presets_have_id_and_label():
    """每個 style preset 都有 id 與中文 label。"""

def test_location_templates_have_required_fields():
    """每個 location template 都有 template_id、label、location_type。"""

def test_container_templates_suggest_sub_locations():
    """container template 必須提供 suggested_sub_locations。"""

def test_template_ids_are_unique():
    """template_id 不可重複。"""

def test_template_ids_are_valid_canonical_ids():
    """template_id 與建議 location_id 必須符合 ID 規則。"""
```

#### 啟動指令

```powershell
uv run streamlit run src/sandbox_dating_sim/ui/streamlit_uiw.py
```

預期：

```text
Local URL: http://localhost:8501
```

#### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 無錯啟動 | 執行 Streamlit 啟動指令 | 頁面正常顯示，終端無 traceback | [ ] |
| 2 | World | 填 world_id、title、日期 | 可產生 world YAML | [ ] |
| 3 | 風格參考詞 | 點選都市戀愛、黑色幽默等 preset | `world.global_style` 加入對應英文 ID | [ ] |
| 4 | Protagonist | 填主角資料與初始數值 | 數值進入 `protagonist.initial_stats` | [ ] |
| 5 | 地點模板 | 套用「學校」模板 | 建立 container 草稿，並提示建立子地點 | [ ] |
| 6 | Container 規則 | 建立 container 但不建子地點後按 Validate | validation report 顯示錯誤 | [ ] |
| 7 | 子地點 | 在學校下新增圖書室 | `parent_location_id` 自動帶入 `school` | [ ] |
| 8 | 獨立地點 | 新增主角家 | `location_type=standalone` 且沒有 parent | [ ] |
| 9 | Character | 建立角色並選表情、服裝、位置 | 白名單輸出 canonical ID | [ ] |
| 10 | Schedule | 新增角色 schedule | location 下拉只顯示可進入地點 | [ ] |
| 11 | Status Flag | 新增 overworked | 必須填 duration 與 clear_rule | [ ] |
| 12 | Ending | 新增 ending | 可填 required_flags、required_stats、route_tags | [ ] |
| 13 | Validate | 點驗證 | 顯示 UIW Linter report | [ ] |
| 14 | YAML Preview | 進入 Review | 顯示 SetupPackage YAML，`uiw_version: 1.2` | [ ] |
| 15 | Export | 點匯出 | 產出 `<world_id>_setup_package.md` | [ ] |
| 16 | Parser Roundtrip | 用 parser 讀回匯出的 MD | 成功 parse 為 SetupPackage | [ ] |

#### 1-F 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-E 回歸全過 | 必須 |
| `tests/test_uiw_defaults.py` 全過 | 必須 |
| 手動驗收 #1-#16 全過 | 必須 |
| 匯出的 MD 可由 parser 讀回 | 必須 |

#### 1-F 範圍提醒

1-F 是 Functional Prototype。

允許：

- 樸素表單。
- Tabs / expanders。
- 表格 + 新增按鈕。
- parent 下拉選單。

不要求：

- 正式產品級 UI。
- 拖拉地圖樹。
- Flowchart。
- AI API 串接。
- 完整 Dashboard。

---

### 1-G 使用者實測回饋迭代

1-G 是 1-F Functional Prototype 後的使用者操作回饋階段。每個子階段依需求範圍補自動測試與手動驗收。

#### 1-G-3 語意型欄位 id + label

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/schema/setup.py` | `tests/test_setup_schema.py` |
| `src/sandbox_dating_sim/uiw/defaults.py` | `tests/test_uiw_defaults.py` |
| `src/sandbox_dating_sim/uiw/linter.py` | `tests/test_uiw_linter.py` |
| `src/sandbox_dating_sim/pipeline/setup_exporter.py` | `tests/test_setup_exporter.py` |
| `src/sandbox_dating_sim/pipeline/setup_parser.py` | `tests/test_setup_parser.py` |
| `src/sandbox_dating_sim/cli.py` | `tests/test_cli.py` |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py` | 手動驗收 |

##### 必測項目

- `world.global_style` 可保存 `id + label` list。
- `protagonist.occupation` 可保存 `id + label`。
- `protagonist.personality` 可保存 `id + label`。
- `protagonist.secrets` 可保存最多 3 個 `id + label`。
- `characters[].personality_tags` 可保存 `id + label` list。
- `characters[].secrets` 可保存最多 3 個 `id + label`。
- 語意型欄位 `id` 必須符合 canonical ID 規則。
- 語意型欄位 `label` 不可為空。
- `protagonist.secrets` 超過 3 個時 UIW Linter 應回報 error。
- `characters[].secrets` 超過 3 個時 UIW Linter 應回報 error。
- `none = 沒有秘密` 為 UI 清空操作；輸出應為 `secrets: []`，不得和其他秘密並存。
- `world.global_style[].id` 不可重複。
- `characters[].personality_tags[].id` 不可重複。
- exporter 輸出 `id + label`。
- parser roundtrip 不遺失中文 label。
- CLI 產物仍可 parse 回 `SetupPackage`。

##### defaults 測試

`tests/test_uiw_defaults.py` 必須驗證：

- 主角職業預設選項總數為 16。
- 新增職業包含 `rich_heir`、`startup_founder`、`tech_worker`、`finance_professional`。
- 主角與 NPC 共用的秘密預設選項新增 8 個，並保留 `none`。
- 所有預設選項都有 `id` 與 `label`。
- 所有預設選項 ID 不重複且符合 canonical ID 規則。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 職業預設 | 點選「咖啡廳店員」 | 顯示「目前選擇：咖啡廳店員」，系統 ID 為 `cafe_staff` | [ ] |
| 2 | 職業新增預設 | 點選「富二代」「新創公司老闆」「科技業」「金融業」之一 | 寫入對應 `id + label` | [ ] |
| 3 | 自訂中文職業 | 在自訂職業名稱輸入中文並按「選擇」 | 自動產生系統 ID，下方欄位可進階修改 | [ ] |
| 4 | 主角性格 | 點選或自訂中文性格 | 寫入 `protagonist.personality.id` 與 `label` | [ ] |
| 5 | 主角秘密複選 | 選 1-3 個秘密 | 寫入 `protagonist.secrets` list | [ ] |
| 6 | 主角秘密上限 | 嘗試選第 4 個秘密 | UI 阻止或 linter 回報錯誤 | [ ] |
| 7 | 沒有秘密互斥 | 選「沒有秘密」後再選其他秘密 | UI 清空 secrets list；輸出 `secrets: []`，不保存 `none` | [x] |
| 8 | 角色性格標籤 | 選預設與自訂中文 tag | 寫入 `characters[].personality_tags[].id/label` | [ ] |
| 9 | NPC 秘密複選 | 替角色選 1-3 個秘密 | 寫入 `characters[].secrets` list | [x] |
| 10 | YAML Preview | 進入 Review | 語意型欄位皆顯示 `id + label` | [ ] |
| 11 | Export / Roundtrip | 匯出 MD 後用 parser 讀回 | 中文 label 不遺失 | [ ] |

##### 1-G-3 驗證紀錄

2026-05-01 驗證結果：

- 自動測試：`67 passed`。
- Streamlit `AppTest` 抽查：角色設定頁無 exception。
- 主角職業預設「咖啡廳店員」寫入 `id + label`。
- 自訂中文職業可產生合法 canonical ID。
- 主角秘密最多保留 3 個。
- 「沒有秘密」會清空 secrets list，canonical output 為 `secrets: []`。
- 角色性格標籤與 NPC 秘密可寫入角色資料，新增角色後暫存選項會清空。

##### 1-G-3 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| 1-G-3 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#11 全過 | 必須 |
| 匯出的 MD 可由 parser 讀回且中文 label 不遺失 | 必須 |

#### 1-G-4 地點與地圖項目調整

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/uiw/defaults.py` | `tests/test_uiw_defaults.py` |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py` | 手動驗收 / Streamlit AppTest |
| `src/sandbox_dating_sim/uiw/linter.py` | 必要時補 `tests/test_uiw_linter.py` |

##### 必測項目

- 分頁順序為 World -> Protagonist -> Locations -> Characters -> Flags/Status/Endings -> Review & Export。
- 每頁「下一頁」按鈕順序與上方導覽一致。
- 原本地點模板保留，且補齊 `車站周邊`。
- 新增地點模板皆存在，template ID 與 location ID suggestion 不重複且符合 canonical ID 規則。
- container 模板會建立指定子地點。
- 每個 sub_location 與 standalone 模板有合理 `available_time_slots`；所有模板都有 AI 劇情參考用 `tags`。
- 子地點實際 `location_id` 由 `<parent_location_id>_<location_id_suffix>` 組成，避免與 standalone 模板 ID 衝突。
- 套用模板時若主地點、獨立地點或預計子地點 ID 已存在，不建立重複地點並顯示提示。
- 名稱相同或接近的模板在 UI 顯示時有脈絡或副標，例如 `便利商店(獨立)` 與 `便利商店(商店街內)`。
- 地點清單每個項目旁可直接刪除，不需展開 JSON。
- 刪除仍有子地點引用的 container 會被阻止，並提示引用的子地點。
- 刪除被角色 schedule 引用的地點會被阻止，並提示引用來源。
- `location_type` UI 顯示中文(英文)，canonical data 仍保存英文 ID。
- `available_time_slots` UI 顯示中文(英文)，canonical data 仍保存英文 ID。
- `tags` 位於進階設定，允許中文或英文描述，並清楚說明用途是提供 AI 劇情生成參考。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 導覽順序 | 啟動 UIW 查看上方導覽 | 地點與地圖在主角設定後、角色設定前 | [ ] |
| 2 | 下一頁順序 | 先完成主角必填欄位後，從主角設定按下一頁 | 進入地點與地圖 | [ ] |
| 3 | 車站周邊模板 | 套用車站周邊 | 建立 container 與月台、站前廣場、地下街 | [ ] |
| 4 | 新增 standalone 模板 | 套用主角公司、公園、醫院、夜市等任一模板 | 建立 standalone 地點 | [ ] |
| 5 | 新增 container 模板 | 套用遊樂園、百貨公司、海邊或溫泉旅館 | 建立指定子地點 | [ ] |
| 6 | 模板防重 | 連續套用同一模板兩次 | 第二次提示 ID 已存在且不新增地點 | [ ] |
| 7 | 地點類型中文顯示 | 開啟手動新增地點類型選單 | 顯示主地點/區域(container) 等文字 | [ ] |
| 8 | 時段中文顯示 | 開啟可使用時段選單 | 顯示早上(morning)、下午(afternoon)、晚上(evening) | [ ] |
| 9 | 刪除子地點防呆 | 嘗試刪除仍有子地點的父地點 | 阻止刪除並列出子地點 | [ ] |
| 10 | 刪除 schedule 引用防呆 | 建立角色 schedule 後刪除該地點 | 阻止刪除並提示引用來源 | [ ] |
| 11 | 進階 tags | 在進階設定輸入中文/英文標籤 | YAML 保存為 `locations[].tags`，且 UI 說明其為 AI 劇情參考 | [ ] |

##### 1-G-4 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| Phase 1-G-1 到 1-G-3 回歸不破壞 | 必須 |
| 1-G-4 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#11 全過 | 必須 |
| 匯出的 Setup Package 中地點資料 canonical ID 維持英文 | 必須 |

---

#### 1-G-5 角色設定頁面 UX 完整化

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py` | 手動驗收 / 必要時補 `tests/test_streamlit_uiw.py`（Streamlit `AppTest`） |

1-G-5 不應修改 schema、linter、exporter、parser；如測試需新增 fixture 僅限 UI 行為驗證。

##### 必測項目

清單顯示與刪除：

- 角色清單 expander label 顯示「{display_name} · {性別中文} · {定位中文}」，不再顯示英文 `(character_id)`。
- 角色清單每列右側可直接刪除，不需展開 expander。
- 刪除被 `endings[].target_character_id` 引用的角色被阻擋，toast 列出所有引用 `ending_id`。
- 刪除被 `status_flags[].targets` 引用的角色被阻擋，toast 列出所有引用 `status_id`；`protagonist`、`global` 與空字串等特殊 target 不被誤判為角色引用。舊格式 `status_flags[].target` 需先正規化為 `targets` 後再檢查。
- 多筆引用同時存在時 toast 訊息合併列出。

既有角色 inline 編輯：

- 編輯既有角色基本資訊：修改 `display_name` / `gender` / `orientation` / `role` / `identity` / `initial_favor` 並按「儲存修改」後，`st.session_state["characters"][i]` 對應欄位更新。
- 編輯既有角色性格與秘密：修改 `personality_tags` / `secrets` 後儲存，對應 keyed temp state 寫回該角色，不影響其他角色。
- 編輯既有角色受控素材白名單：修改 `allowed_emotions` / `allowed_costumes` / `allowed_positions` 後儲存，對應欄位更新；底層仍保存 `{id, label}` 結構。
- 「取消」按鈕清空該角色 keyed temp state；下次開啟 expander 重新依當前值初始化。
- 多角色同時開啟 expander 編輯，keyed temp state 不互相污染。
- `character_id` 於編輯 expander 進階設定區以唯讀文字顯示，1-G-5 不可修改。

行程管理：

- 行程 `day_type` / `time_slot` / `priority` UI 顯示「中文(英文)」，canonical 仍英文 enum。
- 行程 `day_type` 選項在 1-G-5 不含 `specific_date`。
- 行程地點 `selectbox` 顯示 `location.name`，value 仍 `location_id`。
- 每個角色 expander 內「角色行程」區塊列出該角色既有 `schedule`，每筆顯示「{day_type 中文} {time_slot 中文} @ {地點 name} [{priority 中文}] order=N」。
- 每筆行程右側「刪除」按鈕，按下後從該角色 `schedule` 移除該筆。
- 行程清單顯示遇到已被刪除的 `location_id` 時，顯示原 ID 並加註「(地點已刪除)」。
- 「新增行程」表單於每個角色編輯 expander 內運作；頁面下方既有共用「新增角色行程」表單已移除。

白名單顯示與進階設定收納：

- 表情、服裝、位置白名單收進編輯 expander 內「進階設定 (Advanced Settings)」展開區。
- `multiselect` 顯示文字為純中文 label，移除 `(id)` 後綴。
- 進階設定區頂部顯示「💡 受控素材清單」常駐說明文字。

不破壞既有功能：

- Phase 1-G-1 / 1-G-2 / 1-G-3 / 1-G-4 行為不被破壞（主角頁、地點頁、語意型欄位 `id + label`、地點清單 / 模板 / 刪除防呆 / 中文化）。
- 匯出 Setup Package 後 `characters[].schedule` canonical 仍保存英文 enum；可 parse 回 `SetupPackage`。
- exporter / parser / linter 行為與 1-G-4 一致。

Streamlit 實作限制：

- 多角色同時開啟編輯 expander 時，Streamlit 不可 raise duplicate widget key 例外。
- 進階設定區未使用 nested `st.expander`；採 `st.checkbox` / `st.toggle` 切換顯示，state key 含 `character_id` 後綴。
- 不同角色「進階設定展開狀態」彼此獨立，不互相影響。
- 新增角色 `character_id` 為空字串時被擋下並顯示 `st.error`，不寫入 `st.session_state["characters"]`。
- 新增角色 `character_id` 與既有重複時被擋下並顯示 `st.error`。
- 新增角色 `character_id` 含中文、空格或大寫等不符 canonical ID 格式時被擋下並顯示 `st.error`。
- 編輯 expander 不提供修改 `character_id` 的 widget；該欄位於進階設定區為唯讀文字。
- 刪除角色（通過引用防呆後）會清掉該角色所有 `*_<character_id>` keyed session state；後續操作或新增同名角色時不殘留舊 state。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 角色清單 label | 進入角色頁，至少先建立 1 名角色 | expander label 顯示「{name} · {性別中文} · {定位中文}」，無英文 ID | [ ] |
| 2 | 列旁刪除按鈕 | 角色清單右側 | 每列右側可見「刪除」按鈕，不需展開 expander | [ ] |
| 3 | endings 引用防呆 | 建立角色 → 建立 ending 並指向該角色 → 刪除該角色 | 阻止刪除，toast 列出 `ending_id` | [ ] |
| 4 | status_flags 引用防呆 | 建立角色 → 建立 status_flag `target` 為該角色 ID → 刪除該角色 | 阻止刪除，toast 列出 `status_id` | [ ] |
| 5 | 特殊 target 不誤判 | 建立 status_flag `target = protagonist` → 刪除任意 NPC | 不被視為引用，可正常刪除 | [ ] |
| 6 | inline 編輯基本資訊 | expander 展開 → 修改 display_name 等 → 儲存修改 | JSON 對應欄位更新 | [ ] |
| 7 | inline 編輯性格 / 秘密 | 修改性格標籤、增減秘密（最多 3）→ 儲存修改 | `personality_tags` / `secrets` 對應更新 | [ ] |
| 8 | inline 編輯白名單 | 進階設定區修改表情/服裝/位置白名單 → 儲存修改 | `allowed_emotions/costumes/positions` 對應更新，底層保留 `{id, label}` | [ ] |
| 9 | 取消編輯 | expander 修改任意欄位 → 取消 | 角色資料不變；再次展開 expander 顯示當前值 | [ ] |
| 10 | 多角色不互相污染 | 同時展開 2 名角色 → 各自修改性格 → 各自儲存 | 兩名角色性格獨立更新，未互相覆蓋 | [ ] |
| 11 | 行程 enum 中文化 | expander 內新增行程，查看 day_type / time_slot / priority 選項 | 顯示「中文(英文)」 | [ ] |
| 12 | specific_date 不顯示 | 查看行程 day_type 選項 | 不出現 `specific_date` 選項 | [ ] |
| 13 | 行程地點選單中文 | 查看行程地點 selectbox | 顯示地點 `name`（中文），底層仍是 `location_id` | [ ] |
| 14 | 行程清單顯示 | 為角色建立 1-2 筆行程 → 查看 expander 行程清單 | 顯示「平日 早上 @ XXX [一般] order=N」格式 | [ ] |
| 15 | 行程刪除 | 行程清單右側按刪除 | 該筆從角色 `schedule` 移除 | [ ] |
| 16 | 行程地點 dangling | 建立行程後刪除其地點 | 行程清單顯示原 `location_id` 並加註「(地點已刪除)」 | [ ] |
| 17 | 新增行程位置 | 比對頁面下方是否仍有共用「新增角色行程」表單 | 已移除；新增行程僅能在 expander 內進行 | [ ] |
| 18 | 白名單純中文 | 進階設定區查看表情/服裝/位置 multiselect | 顯示僅中文 label，無 `(id)` 後綴 | [ ] |
| 19 | 受控素材說明 | 進階設定區頂部 | 顯示「💡 受控素材清單」常駐說明文字 | [ ] |
| 20 | character_id 唯讀 | 編輯 expander 進階設定區 | 顯示 `character_id` 為唯讀文字 | [ ] |
| 21 | 匯出 canonical | 完成設定後匯出 Setup Package | `schedule[*]` 仍保存英文 enum；可被 parser 還原 | [ ] |
| 22 | 多角色 expander 並行 | 同時展開 3 名角色編輯 expander | 不噴 duplicate widget key 例外；各 widget 互不干擾 | [ ] |
| 23 | 進階設定切換 | 點擊「顯示進階設定」checkbox / toggle | 白名單區顯示/隱藏；不同角色切換 state 彼此獨立 | [ ] |
| 24 | 新增空 ID 被擋 | 新增角色表單 `character_id` 留空 → 提交 | `st.error` 顯示，無新角色寫入 | [ ] |
| 25 | 新增重複 ID 被擋 | 新增與既有角色相同 `character_id` | `st.error` 顯示，無新角色寫入 | [ ] |
| 26 | 新增非法 ID 被擋 | `character_id` 輸入中文、含空格或大寫 → 提交 | `st.error` 顯示，無新角色寫入 | [ ] |
| 27 | 編輯期不可改 ID | 編輯 expander 內找不到可編輯的 `character_id` widget | 進階設定區僅顯示為唯讀文字 | [ ] |
| 28 | 刪除清空 state | 為角色 A 修改性格但不儲存 → 刪除 A → 再新增同 ID 角色 | 新角色 expander 不殘留 A 的編輯草稿，舊行程刪除按鈕 state 也不殘留 | [ ] |

##### 1-G-5 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| Phase 1-G-1 到 1-G-4 回歸不破壞 | 必須 |
| 1-G-5 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#28 全過 | 必須 |
| 匯出的 Setup Package 中 `characters[].schedule` canonical enum 維持英文 | 必須 |
| `characters[].allowed_emotions/costumes/positions` 仍保存 `{id, label}` 結構 | 必須 |
| 多角色同時開啟編輯 expander 不噴 Streamlit duplicate widget key 例外 | 必須 |
| 進階設定區未使用 nested `st.expander` | 必須 |

---

#### 1-G-6 拆分旗標/結局頁與結局頁 UX 完整化

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py`（拆 `_tab_flags()` 與 `_tab_endings()`、結局 inline 編輯、`_collect_existing_ids` / `_purge_ending_state` helpers、修補角色頁 character_id 跨類型唯一，且 `_collect_existing_ids` 納入 `status_id`） | 手動驗收 / 必要時補 `tests/test_streamlit_uiw.py` |
| `src/sandbox_dating_sim/exporter/` 與 `src/sandbox_dating_sim/parser/` | 補 `tests/test_exporter.py` 對最小可用 fixture 的 roundtrip 測試 |
| `src/sandbox_dating_sim/uiw/linter.py` | 補 `tests/test_uiw_linter.py` 對最小可用 fixture 不報 error 的測試 |

1-G-6 不應修改 schema、linter 規則、exporter / parser 邏輯；僅新增測試 fixture 與測試 case。

##### 必測項目

分頁拆分：

- `PAGE_LABELS` 第 5、6 項分別為「旗標與狀態(Flags & Status)」與「結局(Endings)」，順序在「角色設定」之後、「預覽與匯出」之前。
- 「下一頁」按鈕從旗標與狀態頁進入結局頁，從結局頁進入預覽與匯出頁。
- 旗標 / 狀態頁內容與 1-G-5 之前的合併頁旗標 / 狀態區塊功能完全一致。
- 結局頁內容為 1-G-6 改造後的版本；原合併頁中的結局區塊已移出。

結局清單顯示：

- 結局清單 expander label 顯示「{title or ending_id} · {priority 中文} · {target 顯示}」。
- title 為空時 expander label fallback 顯示 `ending_id`。
- target 為 None 顯示「（無）」、`"global"` 顯示「全局結局」、character_id 顯示對應 `display_name`；歷史 / 外部匯入資料若有 dangling reference，需保留原 ID 並加註「(角色已刪除)」。
- 每列右側可直接刪除，不需展開 expander。

inline 編輯：

- 編輯既有結局：修改 title / ending_type / target_character_id / description / priority / route_tags 並按「儲存修改」後，`st.session_state["endings"][i]` 對應欄位更新。
- 編輯既有結局 required_flags / forbidden_flags：multiselect 選擇 boolean flag → 點「加入到必須條件 / 禁止條件」→ free-form text_area 對應字串被 append；儲存後 ending 對應 list 更新。
- 編輯既有結局 required_stats：直接修改 free-form text_input → 儲存後 list 更新。
- 「取消」清空該結局 keyed temp state；下次開啟 expander 重新依當前值初始化。
- 多結局同時開啟 expander 編輯，keyed temp state 不互相污染。
- `ending_id` 在編輯 expander 為唯讀。

中文化：

- priority selectbox 顯示「中文(英文)」：必定觸發(critical) / 主線(main) / 路線(route) / 一般(normal) / 背景(ambient)。
- priority selectbox 下方顯示五級語意 caption。
- target_character_id selectbox 顯示中文 / 「（無 / 不指定）」/「全局結局 (global)」。
- 匯出後 canonical 仍保存英文 priority enum 與英文 character_id / "global" / None。

F-γ multiselect 行為：

- multiselect 顯示現有旗標清單。
- 點擊「加入到必須條件」按鈕後，所選 flag 展開為 placeholder 字串 append 到 free-form text_area，multiselect 清空。
- boolean flag 展開為 `flag.<id> == true` / `flag.<id> == false`。
- integer flag 展開為 `flag.<id> == <initial_value>`（不加引號）。
- string / enum flag 展開為 `flag.<id> == "<initial_value>"`（雙引號包住字串；必要時 escape `"` 與 `\`）。
- multiselect 取消勾選不會自動移除已加入的字串。
- flags=[] 時 multiselect 空，下方顯示「尚未設定旗標」caption。

跨類型 ID 唯一即時擋（結局頁）：

- 新增 ending_id 為空字串時被擋下並顯示 `st.error`，不寫入 `endings`。
- 新增 ending_id 與既有 world_id / character_id / location_id / flag_id / status_id / ending_id 任一重複時被擋下。
- 新增 ending_id 含中文、空格或大寫等不符 canonical ID 格式時被擋下。

附帶修正（1-G-5 character_id 跨類型唯一）：

- 新增 character_id 與既有 world_id / location_id / flag_id / status_id / ending_id / character_id 任一重複時被擋下並顯示 `st.error`。
- 既有 1-G-5 character_id 空 / 重複 / 非法格式擋下行為不被破壞。

最小可用 Setup Package：

- fixture：`flags=[]` / `status_flags=[]`，其他 World / Protagonist / locations / characters / endings 全有，且該 ending `required_flags=[]` / `forbidden_flags=[]` / `required_stats=[]`。
- UIW Linter 對此 fixture 不報 error。
- `SetupPackageExporter.to_markdown()` 成功產出 MD。
- MD 透過 parser 還原為 `SetupPackage`，roundtrip 一致。

實作限制（Streamlit 層）必測：

- 多結局同時開啟編輯 expander 不可 raise duplicate widget key 例外。
- 結局編輯 expander 不使用 nested `st.expander`。
- 編輯 expander 不提供修改 `ending_id` 的 widget。
- 刪除結局後清空所有 `*_<ending_id>` keyed session state；新增同名 ending_id 不殘留舊 state。

不破壞既有功能：

- Phase 1-G-1 / 1-G-2 / 1-G-3 / 1-G-4 / 1-G-5 行為不被破壞。
- 1-G-5 角色頁 inline 編輯、刪除引用防呆、行程清單功能皆正常。
- 既有 fixture（含 flags / status_flags / endings 都有設定的 fixture）匯出 roundtrip 不變。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 分頁拆分 | 啟動 UIW 查看上方導覽 | 第 5 項「旗標與狀態」、第 6 項「結局」分開顯示 | [ ] |
| 2 | 下一頁順序 | 旗標與狀態頁按下一頁 | 進入結局頁；結局頁按下一頁進入預覽與匯出 | [ ] |
| 3 | 旗標頁內容保留 | 旗標與狀態頁建立 1 個 boolean flag、1 個 status_flag | 與 1-G-5 之前合併頁行為一致 | [ ] |
| 4 | 結局清單 label | 結局頁建立 1 個結局後查看清單 | label 顯示「{title} · {priority 中文} · {target 顯示}」，無原始 JSON dump | [ ] |
| 5 | 列旁刪除按鈕 | 結局清單右側 | 每列右側可見「刪除」按鈕，不需展開 | [ ] |
| 6 | title 為空 fallback | 建立結局時 title 留空 → 查看清單 | label 第一段 fallback 顯示 ending_id | [ ] |
| 7 | target 為 None | 結局 target_character_id 留 None | label 顯示「（無）」 | [ ] |
| 8 | target 為 global | 結局 target_character_id 設為 global | label 顯示「全局結局」 | [ ] |
| 9 | target 為角色 | 結局關聯任一角色 | label 顯示該角色 display_name（中文） | [ ] |
| 10 | target dangling 防禦顯示 | 以 fixture / session_state 注入 `target_character_id` 指向不存在角色的 ending → 查看結局清單 | label 保留原 ID 並加註「(角色已刪除)」；正常 UI 刪除被 ending 引用的角色時應先被防呆阻擋 | [ ] |
| 11 | inline 編輯基本資訊 | expander 展開 → 修改 title / description / ending_type / route_tags → 儲存 | 對應欄位更新 | [ ] |
| 12 | inline 編輯 target | expander 內切換 target_character_id 並儲存 | 對應欄位更新；canonical 仍存英文 ID / "global" / None | [ ] |
| 13 | inline 編輯 priority | 切換 priority 中文選項並儲存 | canonical 仍存英文 enum；UI 顯示中文(英文) | [ ] |
| 14 | priority caption | 查看 priority selectbox 下方 | 顯示五級語意 caption（critical 到 ambient） | [ ] |
| 15 | F-γ 加入必須 | 旗標頁建立 1 個 boolean flag → 結局 expander multiselect 選該 flag → 點「加入到必須條件」 | text_area append `flag.<id> == true`；multiselect 清空；儲存後 ending.required_flags 含該字串 | [ ] |
| 16 | F-γ 加入禁止 | 同上但點「加入到禁止條件」 | text_area append 到 forbidden 欄位；儲存後 ending.forbidden_flags 含該字串 | [ ] |
| 17 | F-γ integer | 建立 integer flag（initial_value 例 100）→ 結局 expander 加入該 flag | text_area append `flag.<id> == 100` | [ ] |
| 17a | F-γ string / enum | 建立 string 或 enum flag（initial_value 例 open）→ 結局 expander 加入該 flag | text_area append `flag.<id> == "open"` | [ ] |
| 18 | F-γ 取消勾選不移除 | 加入後 multiselect 取消勾選 | text_area 字串保留，未自動移除 | [ ] |
| 19 | F-γ 修改條件值 | text_area 直接編輯字串（true → false）→ 儲存 | ending 對應 list 反映修改 | [ ] |
| 20 | F-γ flags=[] 提示 | 不建立任何 flag 直接打開結局 expander | multiselect 空，顯示「尚未設定旗標」caption | [ ] |
| 21 | required_stats free-form | 在 required_stats text_input 打 `stat.Cash >= 100` → 儲存 | ending.required_stats 反映該字串 | [ ] |
| 22 | 取消編輯 | expander 修改任意欄位 → 取消 | ending 資料不變；再展開顯示當前值；temp_req_flags / temp_forb_flags 清空 | [ ] |
| 23 | 多結局並行編輯 | 同時展開 2 個結局 expander → 各自修改條件 → 各自儲存 | 兩結局欄位獨立更新；無 duplicate widget key 例外 | [ ] |
| 24 | ending_id 唯讀 | 編輯 expander 內 | 找不到可編輯 ending_id widget；以唯讀文字顯示 | [ ] |
| 25 | 新增空 ending_id | 新增結局 ending_id 留空 → 提交 | st.error 顯示，無新結局寫入 | [ ] |
| 26 | 新增重複 ending_id（同類） | 新增與既有結局相同 ending_id | st.error 顯示，無新結局寫入 | [ ] |
| 27 | 新增重複 ending_id（跨類） | 將 ending_id 設為某既有 character_id / status_id | st.error 顯示，無新結局寫入 | [ ] |
| 28 | 新增非法 ending_id | ending_id 輸入中文、含空格或大寫 | st.error 顯示，無新結局寫入 | [ ] |
| 29 | 結局刪除清 state | 為某結局 multiselect 加入旗標但不儲存 → 刪除該結局 → 重新建立同 ending_id | 新結局 expander 不殘留前一個結局的 temp 編輯草稿 | [ ] |
| 30 | character_id 跨類重複擋 | 角色頁新增 character_id = 某既有 location_id / flag_id / status_id / ending_id | st.error 顯示，無新角色寫入 | [ ] |
| 31 | 最小可用匯出 | 完成 World / Protagonist / 1 地點 / 1 角色 / 1 結局，跳過 flags 與 status_flags → 匯出 | UI 顯示 passed 與輸出檔案位置；該檔案存在 | [ ] |
| 32 | 最小可用 parse | 用 CLI parser 載入 #31 產生的 MD | 成功還原為 `SetupPackage`，無例外 | [ ] |

##### 1-G-6 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| Phase 1-G-1 到 1-G-5 回歸不破壞 | 必須 |
| 1-G-6 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#32 全過 | 必須 |
| `PAGE_LABELS` 為 7 項，且第 5、6 項為「旗標與狀態(Flags & Status)」「結局(Endings)」 | 必須 |
| 匯出的 Setup Package 中 `endings[].priority` canonical enum 維持英文 | 必須 |
| 匯出的 Setup Package 中 `endings[].target_character_id` canonical 仍為英文 ID / "global" / None | 必須 |
| 最小可用 fixture（flags=[] / status_flags=[]）UIW Linter 通過、exporter 成功、parser roundtrip 一致 | 必須 |
| 多結局同時開啟編輯 expander 不噴 Streamlit duplicate widget key 例外 | 必須 |
| 結局編輯 expander 未使用 nested `st.expander` | 必須 |
| 角色頁 character_id 跨類型唯一即時擋已生效 | 必須 |

---

#### 1-G-7 新增流程自動產生唯一 canonical ID

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py`（共用 ID 產生 helper、地點 / 角色 / 旗標 / 狀態 / 結局新增流程） | `tests/test_streamlit_uiw.py` |
| `src/sandbox_dating_sim/core/ids.py` 或 UI helper（若抽出純函式） | `tests/test_ids.py` 或 `tests/test_streamlit_uiw.py` |

1-G-7 不應修改 schema、exporter、parser。UIW Linter 的 `status_id` 補洞原排 1-G-8，但已在 1-G-7 收尾先行修復並補測；1-G-8 只需納入回歸驗收。

##### 必測項目

純函式單測（必跑、不依賴 Streamlit AppTest）：

- `_slugify_label("咖啡廳") == "ka_fei_ting"`、`_slugify_label("Cafe Shop") == "cafe_shop"`、`_slugify_label("") == "unnamed"`。
- `_slugify_label` 處理中文標點 `、`、`，`、`。`、`：` 與空白同樣轉 `_` 並合併重複。
- `_slugify_label` 對長 description 截斷至前 8 個 token，截斷後再合併重複 `_` 並去頭尾 `_`。
- `_make_unique_id("咖啡廳", "loc_", "unnamed", set()) == "loc_ka_fei_ting"`。
- suffix 連鎖：`existing = {"loc_x"} → loc_x_2`、`{"loc_x", "loc_x_2"} → loc_x_3`、`{"loc_x", "loc_x_2", "loc_x_3"} → loc_x_4`。
- suffix 順序遞增取最小未占用：`existing = {"loc_x", "loc_x_2", "loc_x_4"} → loc_x_3`。
- 跨類前綴隔離：`_make_unique_id("林小雨", "ch_", "unnamed", {"loc_lin_xiao_yu"}) == "ch_lin_xiao_yu"`。
- 空白或全無效來源使用 `unnamed`：`_make_unique_id("", "flag_", "unnamed", set()) == "flag_unnamed"`，不得產生 `flag_flag`、`flag_unnamed_unnamed` 類疊字。
- `inspect.signature(_generate_system_id)` 與 1-G-3 完成時一致；該函式不得新增 `prefix` 等參數。

自動產生 ID（UI 行為）：

- 手動新增地點時，使用中文 `name` 可自動產生合法 `location_id`，不要求使用者填英文 ID。
- 新增 NPC 角色時，使用中文 `display_name` 可自動產生合法 `character_id`，不要求使用者填英文 ID。
- 新增旗標時，使用中文 `description` 可自動產生合法 `flag_id`，不要求使用者填英文 ID。
- 新增狀態旗標時，使用中文 `label` 可自動產生合法 `status_id`，不要求使用者填英文 ID。
- 新增結局時，使用中文 `title` 可自動產生合法 `ending_id`，不要求使用者填英文 ID。

唯一性：

- 同名來源連續新增三筆以上時，suffix 順序遞增 `_2`、`_3`、`_4`，不覆蓋既有資料、不回填空缺。
- 自動 ID 路徑下，prefix 隔離使各類型永不跨類衝突。**跨類唯一只在進階手動 ID 入口、地點模板套用、既有匯入資料三種場景觸發**；其中：
  - 進階手動 ID 跨類撞同名 → 顯示錯誤，不新增。
  - 地點模板套用撞既有 ID → **自動 suffix 避讓（不阻止建立）**。
  - 匯入既有資料 → 維持原狀，由 UIW Linter 後續處理（不在 1-G-7 範圍）。
- 自動產生 ID 必須納入 `world_id / 固定 protagonist / character_id / location_id / flag_id / status_id / ending_id` 全域集合；`_collect_existing_ids` 須補入 `protagonist`。
- 旗標 ID 來源使用 `description`，不得要求 schema 不存在的 `label` 欄位。
- 長 description 產生 slug 時最多保留前 8 個 token，避免 ID 過長。
- 中文標點如 `、`、`，`、`。`、`：` 會和空白一樣轉為 `_` 並被後續清理。
- 空白或全無效來源使用中性 fallback `unnamed`，再加 prefix，例如 `flag_unnamed`，不得產生 `flag_flag`、`loc_location` 這類重複語意 ID。
- `_make_unique_id` 只能包裝既有 `_generate_system_id`；不得修改 `_generate_system_id` 的簽章或讓 1-G-3 semantic choice 被加上類型 prefix。

UI 行為：

- 英文 ID 欄位不再是一般使用者必填主欄位。
- 自動產生的系統 ID 在 **submit 後**以 caption 顯示於新增結果旁，不在 form 內 widget callback 即時預覽。
- 若提供手動調整 ID，非法格式 / 重複 ID（含與固定 `protagonist` 衝突）會被擋下，且不清空使用者已輸入內容。
- 新增成功後才清空該表單 draft state。
- **自動 ID 路徑驗證失敗（必填欄位空、衝突無解等）也必須保留使用者輸入**，不清空 draft。

不破壞既有功能：

- 地點模板套用仍正常；模板建議 ID 衝突時**自動 suffix 避讓**（不阻止建立），允許重複套用同一模板。
- 角色、地點、旗標、狀態、結局新增後仍可進入 Review & Export。
- 匯出的 Setup Package 中 canonical ID 仍是英文，中文 name / display_name / label / title / description 不遺失。
- parser roundtrip 不因自動產生 ID 受影響。
- 1-G-3 自訂語意選項（personality / occupation / secret）行為不變，產出 ID 不帶類型 prefix。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 地點自動 ID | 手動新增地點，名稱輸入「咖啡廳」 | 新增成功，系統 ID 自動為合法英文，例如 `loc_ka_fei_ting`，submit 後 caption 顯示 | [ ] |
| 2 | NPC 自動 ID | 新增角色，姓名輸入「林小雨」 | 新增成功，系統 ID 自動為合法英文，例如 `ch_lin_xiao_yu` | [ ] |
| 3 | 旗標自動 ID | 新增旗標，description 輸入「是否失業」 | 新增成功，系統 ID 自動為合法英文，例如 `flag_shi_fou_shi_ye` | [ ] |
| 4 | 狀態自動 ID | 新增狀態，label 輸入「過勞」 | 新增成功，系統 ID 自動為合法英文，例如 `status_guo_lao` | [ ] |
| 5 | 結局自動 ID | 新增結局，標題輸入「蘇菲好結局」 | 新增成功，系統 ID 自動為合法英文，例如 `end_su_fei_hao_jie_ju` | [ ] |
| 6 | 同名 suffix 連鎖 | 連續新增**四個**同名地點（皆輸入「咖啡廳」） | suffix 順序遞增為 `loc_ka_fei_ting`、`_2`、`_3`、`_4`，四筆皆保留 | [ ] |
| 7 | 跨類唯一（進階手動 ID） | 透過進階手動 ID 在 NPC 建立 `xxx`，再以進階手動 ID 在地點輸入 `xxx` | 第二次輸入被擋下，顯示錯誤訊息，原輸入保留 | [ ] |
| 8 | ID 非必填 | 查看各新增表單 | 英文 ID 不再是主流程必填欄位 | [ ] |
| 9 | 進階手動 ID 防呆 | 進階手動 ID 入口輸入中文 / 大寫 / 既有重複 ID / 字面 `protagonist` | 全部被擋下，顯示錯誤，原輸入保留 | [ ] |
| 10 | 匯出 canonical | 完成最小設定並匯出 | Setup Package 保存英文 ID，中文顯示文字不遺失，可 parse 回 `SetupPackage` | [ ] |
| 11 | 長描述截斷 | 新增旗標，description 輸入長中文句子（>8 token） | 自動 ID 只取前 8 個 token，仍合法且可讀 | [ ] |
| 12 | 空值 fallback | 來源文字空白或只含無效符號（純標點） | 自動 ID 使用中性 fallback，例如 `flag_unnamed`；連續新增則 `flag_unnamed_2`、`_3` | [ ] |
| 13 | 1-G-3 語意型回歸 | 走完 1-G-3 自訂語意選項流程（personality / occupation / secret），產出 ID 比對 | 產出 ID 與 1-G-3 完成時一致，不帶類型 prefix；`_generate_system_id` 簽章未變 | [ ] |
| 14 | 自動 ID 失敗保留 draft | 新增地點時故意留空 `name` 欄位送出，或讓自動 ID 撞無解衝突 | 提交失敗，顯示錯誤；其他已填欄位（type / description / tags）保留，不清空 | [ ] |
| 15 | 預覽 ID 時機 | 在 form 內持續修改來源欄位但**不送出** | form 內**不**即時顯示自動 ID；按 submit 後 caption 才出現最終 ID | [ ] |
| 16 | 模板衝突避讓 | 連續套用同一個地點模板（例如「家」）兩次 | 第二次自動產生 `_2` suffix（如 `loc_home_2`），不阻止建立 | [ ] |
| 17 | 實機 end-to-end | 啟動 UIW、跑完最小可用設定、export → parser roundtrip | UIW Linter 通過、export 成功、parser 載回的 `SetupPackage` 與匯出前一致 | [ ] |

##### 1-G-7 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| Phase 1-G-1 到 1-G-6 回歸不破壞 | 必須 |
| 1-G-7 純函式單測（`_slugify_label` / `_make_unique_id`）全過 | 必須 |
| 1-G-7 UI 行為測試（Streamlit AppTest）全過 | 必須 |
| 手動驗收 #1-#17 全過 | 必須 |
| 地點 / NPC / 旗標 / 狀態 / 結局新增流程皆不要求一般使用者輸入英文 ID | 必須 |
| 自動產生 ID 符合 canonical ID 格式且全域唯一 | 必須 |
| `_generate_system_id` 簽章與行為與 1-G-3 完成時一致 | 必須 |
| 既有 ID rename / reference migration 未被混入 1-G-7 | 必須 |

---

#### 1-G-8 旗標 / 狀態頁 inline 編輯與 status targets 複選

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/schema/setup.py` (`StatusFlag.targets`) | `tests/test_setup_schema.py` |
| `src/sandbox_dating_sim/pipeline/setup_parser.py` / `setup_exporter.py` | `tests/test_setup_parser.py` / `tests/test_setup_exporter.py` |
| `src/sandbox_dating_sim/uiw/linter.py` | `tests/test_uiw_linter.py` |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py`（flag/status inline UI） | `tests/test_streamlit_uiw.py` 或新增 `tests/test_uiw_1g8.py` |

##### 必測項目

純函式單測（必跑、不依賴 Streamlit AppTest、ROI 最高）：

- `_normalize_legacy_status({"target": "protagonist", "label": "x", ...})` 結果含 `targets=["protagonist"]` 且不殘留 `target` key。
- `_normalize_legacy_status({"target": "", ...})` 結果為 `targets=[]`（保留給 schema `Field(min_length=1)` 拒絕）。
- `_normalize_legacy_status({"targets": ["a"], "target": "b", ...})` 不覆寫已有 `targets`，新格式優先。
- `_normalize_legacy_status({"targets": ["a"]})` 直接回原樣。
- `_parse_effect_yaml("")` → 拒絕（None 不是 list）。
- `_parse_effect_yaml("foo: bar")` → 拒絕（dict 不是 list）。
- `_parse_effect_yaml("- foo")` → 拒絕（list 元素非 dict）。
- `_parse_effect_yaml("- 123: x")` → 拒絕（dict key 非 str）。
- `_parse_effect_yaml(MagicMock())` → 立刻 raise `ValueError`（**避免 PyYAML Reader 把 MagicMock 當 file-like，呼叫 `stream.read(N)` 永遠拿到 truthy MagicMock 而陷入死迴圈卡死整個 process**）。
- `_parse_effect_yaml(None)` / `_parse_effect_yaml(123)` / `_parse_effect_yaml([])` / `_parse_effect_yaml({"foo": "bar"})` → 全部拒絕（type guard：`isinstance(yaml_str, str)` 必須在函式開頭）。
- `_parse_effect_yaml("- block_time_slot: evening\n- stat_multiplier:\n    stat: CHA\n    value: 0.8")` → 兩筆合法 dict。
- `yaml.safe_load(yaml.safe_dump(parsed)) == parsed`（合法 effect round-trip 等價）。
- `_status_targets_options([{"character_id": "sophie", "display_name": "蘇菲"}], current_targets=[])` 第一項 `("主角(protagonist)", "protagonist")`，第二項 `("蘇菲(sophie)", "sophie")`。
- `_status_targets_options([{"character_id": "protagonist", "display_name": "怪資料"}], current_targets=[])` 只回一項 `protagonist`（dedupe）。
- `_status_targets_options([], current_targets=["dangling_x"])` 含 `("dangling_x(角色已刪除)", "dangling_x")`。
- `_has_flag_reference("not flag.over and flag.over_x", "over") == True`。
- `_has_flag_reference("flag.over_x only", "over") == False`（word boundary）。
- `_has_status_reference("status.protagonist.overworked", "overworked") == True`（qualified）。
- `_has_status_reference("status.overworked == true", "overworked") == True`（bare）。
- `_has_status_reference("status.overworked_x", "overworked") == False`（word boundary）。
- `inspect.signature(_make_unique_id)` 與 1-G-7 完成時一致；`inspect.signature(_generate_system_id)` 與 1-G-3 完成時一致。

Schema / parser / exporter：

- `StatusFlag(targets=["protagonist", "sophie"])` 合法。
- `StatusFlag(targets=[])` 由 schema `Field(min_length=1)` 拋 `ValidationError`；UIW Linter 同時回報 `empty_status_targets` 提供友善訊息（雙層保險）。
- `StatusFlag(target="protagonist", label=..., effect=..., duration=..., clear_rule=..., description=...)`（舊格式直接 kwarg）由 `model_validator(mode="before")` 正規化為 `targets=["protagonist"]`，不拋例外、不殘留 `target` 欄位。
- `targets` 只能引用 `protagonist` 或既有 `characters[].character_id`；未知 target 由 UIW Linter 報 `unknown_status_target`。
- 既有 `tests/fixtures/setup_minimal.yaml`（已遷移為 `targets:`）通過 schema validation 與 UIW Linter。
- 新增 `tests/fixtures/setup_legacy_status_target.yaml`（保留舊 `target:`）能被 parser 載入並 normalize 為 `targets: ["protagonist"]`。
- 既有 `tests/test_setup_schema.py` / `tests/test_uiw_linter.py` 內所有 `StatusFlag(target=...)` 構造遷移為 `targets=[...]`，舊行為由新增的相容測試覆蓋。
- exporter 一律輸出 `targets`，不得再輸出舊欄位 `target`。
- export → parse roundtrip 後多 targets 不遺失，順序保留。
- `status_id` invalid format / duplicate ID 仍由 UIW Linter 擋下（此補洞已先行修復，1-G-8 只回歸）。

UIW Linter 新增 issue type 必測：

- `unknown_status_target`：`targets=["unknown_id"]` 報 error，path=`status_flags[i].targets[j]`。
- `empty_status_targets`：構造空 targets（透過 model_construct 跳過 schema 限制）時 linter 報 error，path=`status_flags[i].targets`。
- `unexpected_duration_value`：`duration.type ∈ {until_event, until_cleared}` 但 `value=3` 報 warning，不擋 export。

Flag UI：

- 目前旗標以 `{description} · {type} · 初始值 {initial_value}` 顯示；description 空時 fallback `flag_id`。
- 每筆 flag 有 inline expander，`flag_id` 唯讀。
- 編輯 `description` / `type` / `initial_value` 後，按「儲存修改」才寫回 canonical。
- 按「取消」不寫回 canonical，並清空該 flag keyed temp state。
- `boolean` initial_value 使用 true/false 選項並保存 bool。
- `integer` initial_value 保存 int。
- `string` / `enum` initial_value 保存 str。
- 刪除被 `endings[].required_flags` / `forbidden_flags` 引用的 flag 時被阻止（word boundary 命中）。
- 刪除被 `characters[].schedule[].condition` 引用的 flag 時被阻止（word boundary）。
- `flag.over_x` 不會誤判為 `flag.over` 引用（word boundary 反例必測）。
- 成功刪除 flag 後清空 `*_<flag_id>` keyed session state。

Status UI：

- 目前狀態以 `{label} · {targets 顯示} · {duration.type 中文} · {clear_rule 摘要}` 顯示；label 空時 fallback `status_id`。
- 每筆 status 有 inline expander，`status_id` 唯讀。
- `targets` multiselect 選項包含主角與所有 NPC 角色；顯示為 `主角(protagonist)` / `{display_name}(<character_id>)`。
- 多選 targets 儲存後 canonical 保存 ID list，不保存中文 label。
- 既有 dangling target 在 UI 中以 `<raw_id>(角色已刪除)` 顯示並可保留；取消勾選後可移除。
- `_status_targets_options` dedupe 在 UI 層也驗證：若資料中 NPC `character_id == "protagonist"` 不會出現重複選項。
- `targets` 空時不儲存，保留使用者輸入並顯示 error。
- `duration.type` selectbox 顯示中文(英文)：時段數(time_slots)、天數(days)、直到事件(until_event)、直到解除(until_cleared)、永久(permanent)。
- 每個 `duration.type` 選項有對應說明 caption。
- `duration.type ∈ {time_slots, days}` 時 UI **顯示** value number_input，必填正整數。
- `duration.type ∈ {until_event, until_cleared}` 時 UI **不顯示** value widget，儲存為 `None`；舊資料帶數字也 normalize 為 None。
- `duration.type == permanent` 時，`duration.value` 存 `None`，缺 `permanent_reason` 不儲存。
- 非 permanent 且 `clear_rule` 空時不儲存。
- 非 permanent 且 `clear_rule == ["manual_only"]` 可儲存，但顯示 warning，並與 UIW Linter `hard_to_clear` warning 對齊。
- `effect` YAML parse 成非空 `list[dict]` 後才可儲存。
- `effect` YAML 語法錯誤、空字串、空 list、非 list、list 元素非 dict、dict key 非 str 時不儲存。
- 多筆 `effect` roundtrip 不遺失，順序保留。
- 刪除被 `endings[].required_stats` 或 `characters[].schedule[].condition` 引用的 status 時被阻止（兩種寫法 `status.<status_id>` / `status.<target>.<status_id>` 皆命中）。
- `status.overworked_x` 不會誤判為 `status.overworked` 引用（word boundary 反例必測）。
- 1-G-5 角色刪除防呆對 `status_flags[].targets` 引用比對改用 `targets contains` 後仍能阻擋被引用的角色刪除（normalize 後比對）。
- 成功刪除 status 後清空 `*_<status_id>` keyed session state。

Streamlit key 與回歸：

- 多筆 flag/status 同時開啟 inline expander 不產生 duplicate widget key。
- 至少同時展開 2 flag + 2 status，斷言 widget key 帶 `flag_<field>_<flag_id>` / `status_<field>_<status_id>` prefix，所有 key 不重複。
- flag/status 編輯區未使用 nested `st.expander`。
- 新增 flag/status 仍沿用 1-G-7 自動 ID，英文 ID 不成為一般使用者必填欄位；submit 後 caption 顯示自動產生的 ID。
- 1-G-7 helper 簽章不變：`inspect.signature(_make_unique_id)` 與 `inspect.signature(_generate_system_id)` 與 1-G-7 完成時一致。
- 1-G-6 結局頁 F-γ flag multiselect 不退化。
- 最小可用 Setup Package（flags=[] / status_flags=[]）仍可通過 UIW Linter、export、parse roundtrip。

Silent migration 端對端鏈路：

- 載入 `setup_legacy_status_target.yaml`（含 `target: protagonist`）→ UI 開啟該 status 編輯任一欄位（`description`）→ 儲存 → 匯出 → 重新 parse → 結果為 `targets: ["protagonist"]`，不殘留 `target` key。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | flag inline 編輯 | 新增旗標後展開該 flag，修改說明與初始值，儲存 | 清單與匯出資料更新；`flag_id` 未改變 | [ ] |
| 2 | flag 取消 | 展開 flag，修改欄位後按取消 | canonical 不變；下次展開回到原值 | [ ] |
| 3 | flag 刪除防呆 + word boundary | 讓 ending `required_flags` 引用 `flag.over` 後嘗試刪除 `over`；另建 `flag.over_x` 不影響 `over` 的刪除判斷 | 引用 `flag.over` 阻擋；`flag.over_x` 不誤判 | [ ] |
| 4 | status targets 複選 | 新增 / 編輯 status，勾選主角與至少一名角色 | 匯出 YAML 保存 `targets: [protagonist, <character_id>]` | [ ] |
| 5 | status duration 中文化 + value 顯示 | 檢查 duration.type 選項，並依序切到每種類型 | 顯示中文(英文)、切換時 caption 對應；`time_slots`/`days` 顯示 value、`until_*`/`permanent` 不顯示 value 並存 None | [ ] |
| 6 | permanent 驗證 | 選永久(permanent) 但不填永久原因後儲存 | 不寫回，顯示錯誤並保留輸入 | [ ] |
| 7 | effect YAML 驗證（含 corner cases） | 依序輸入：空字串、`foo: bar`、`- foo`、`- 123: x` | 全部不寫回，顯示錯誤並保留輸入 | [ ] |
| 8 | effect 多筆保留 | 輸入兩筆 effect，儲存、匯出、parse | 兩筆 effect 完整保留，順序不變 | [ ] |
| 9 | status 刪除防呆 + word boundary | 讓 `required_stats` 引用 `status.protagonist.overworked` 與 `status.overworked` 各一份後嘗試刪除 `overworked`；另建 `status.overworked_x` 不影響判斷 | 兩種引用形式皆阻擋；`overworked_x` 不誤判 | [ ] |
| 10 | 舊 target 相容（端對端） | 匯入含 `target: protagonist` 的舊 YAML | parser 由 `model_validator(mode="before")` 轉成 `targets: ["protagonist"]`，UI 開啟正常顯示 | [ ] |
| 11 | duplicate key 回歸 | 同時展開 2 flag + 2 status 編輯，每個 expander 內動到所有 widget | 不出現 Streamlit duplicate widget key 例外；widget key 帶 `flag_<field>_<flag_id>` / `status_<field>_<status_id>` prefix | [ ] |
| 12 | 1-G-7 自動 ID 回歸 | 新增中文 flag/status，不填英文 ID | 自動產生合法 ID，submit 後 caption 顯示；`_make_unique_id` / `_generate_system_id` 行為不變 | [ ] |
| 13 | silent migration 端對端 | 載入舊格式 fixture → 編輯該 status 任一欄位 → 儲存 → 匯出 → parse | 匯出 YAML 為新格式 `targets: [protagonist]`，無殘留 `target` key | [ ] |
| 14 | targets dedupe / dangling | (a) 偽造資料含 NPC `character_id == "protagonist"` 後檢查 multiselect 選項；(b) 刪除被 status targets 引用的 NPC | (a) 不出現重複選項；(b) UI 中該 target 顯示為 `<raw_id>(角色已刪除)` 並可保留或取消勾選 | [ ] |
| 15 | 1-G-5 角色刪除引用 targets 回歸 | 在某 status 的 `targets` 加入某 NPC 後嘗試刪除該 NPC | 角色刪除被阻擋，toast 列出 status 引用 | [ ] |
| 16 | 實機 end-to-end | 啟動 UIW、跑完最小可用設定 + 至少 1 個 flag + 1 個 status（含複數 targets）、export → parser roundtrip | UIW Linter 通過、export 成功、parser 載回的 `SetupPackage` 與匯出前一致 | [ ] |

##### 1-G-8 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| 1-G-8 純函式單測（`_normalize_legacy_status` / `_parse_effect_yaml` / `_status_targets_options` / `_has_flag_reference` / `_has_status_reference`）全過 | 必須 |
| `StatusFlag.targets` schema 拒絕空 list；`model_validator(mode="before")` normalize 舊 `target` 全過 | 必須 |
| Schema / parser / exporter / linter 對齊（含新 issue types `unknown_status_target` / `empty_status_targets` / `unexpected_duration_value`） | 必須 |
| 既有 fixture / 測試遷移完成；新增 `setup_legacy_status_target.yaml` 相容測試通過 | 必須 |
| 1-G-8 UI 行為測試全過 | 必須 |
| 1-G-8 手動驗收 #1-#16 全過 | 必須 |
| Phase 1-A 到 1-G-7 回歸不破壞 | 必須 |
| 1-G-7 `_make_unique_id` / `_generate_system_id` 簽章與行為不變 | 必須 |
| 1-G-5 角色刪除防呆對 `status_flags[].targets` 引用比對改用 `targets contains` 後仍正確 | 必須 |
| 最小可用 Setup Package 仍可 export → parse roundtrip | 必須 |
| 不混入 Status Manager runtime 或 Event Blueprint migration | 必須 |

---

#### 1-G-9 ScheduleEntry `specific_date` schema 升級與 `schedule_id` 自動生成

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/schema/setup.py`（`ScheduleEntry.specific_date`） | `tests/test_setup_schema.py` |
| `src/sandbox_dating_sim/pipeline/setup_parser.py` / `setup_exporter.py` | `tests/test_setup_parser.py` / `tests/test_setup_exporter.py` |
| `src/sandbox_dating_sim/uiw/linter.py` | `tests/test_uiw_linter.py` |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py`（schedule UI） | `tests/test_streamlit_uiw.py` 或新增 `tests/test_uiw_1g9.py` |

##### 必測項目

Schema：

- `ScheduleEntry(specific_date=date(2026, 5, 1), day_type="specific_date", ...)` 合法。
- `ScheduleEntry(specific_date=None, day_type="weekday", ...)` 合法（default）。
- `ScheduleEntry(...)` 不帶 `specific_date` 時 default 為 `None`。
- 既有不帶 `specific_date` 的 `ScheduleEntry` 構造仍合法（向後相容）。

UIW Linter 新增 issue type 必測：

- `missing_specific_date`：`day_type="specific_date"` 且 `specific_date=None` 報 error，path=`characters[i].schedule[j].specific_date`。
- `unexpected_specific_date`：`day_type="weekday"` 且 `specific_date=date(2026, 5, 1)` 報 error，path=`characters[i].schedule[j].specific_date`。
- `specific_date_out_of_range`：`specific_date` 在 `world.start_date` 之前報 error。
- `specific_date_out_of_range`：`specific_date` 在 `world.end_date` 之後報 error。
- 合法邊界：`specific_date` 恰好等於 `world.start_date` 不報 error。
- 合法邊界：`specific_date` 恰好等於 `world.end_date` 不報 error。
- 合法案例：`day_type="weekday"` 且 `specific_date=None` 不報 error。
- 既有 schedule linter 規則不退化（`unknown_schedule_location`、`invalid_schedule_time_slot`、`schedule_tie`）。
- `schedule_id` 非法格式（例如中文、空字串、`123_bad`）報 `invalid_id_format`，path=`characters[i].schedule[j].schedule_id`。
- `schedule_id` 與 world / character / location / flag / status / ending 任一既有 ID 重複時報 `duplicate_id`，path=`characters[i].schedule[j].schedule_id`。
- 兩筆 schedule 使用同一個 `schedule_id` 時報 `duplicate_id`。
- `specific_date` 不同日期、同 time_slot、同 priority、同 schedule_order 不報 `schedule_tie`。
- `specific_date` 同日期、同 time_slot、同 priority、同 schedule_order 報 `schedule_tie`。

Exporter / Parser roundtrip：

- `setup_minimal.yaml`（含 `specific_date` 行程）export → parse roundtrip 一致。
- `specific_date` 為 `None` 時 export YAML 不含 `specific_date` key。
- `specific_date` 為 `date(2026, 5, 1)` 時 export YAML 含 `specific_date: '2026-05-01'`，parse 回 `date(2026, 5, 1)`。
- 既有不含 `specific_date` 的 fixture roundtrip 不破壞。

`schedule_id` 自動生成：

- 留空 `schedule_id` 時自動生成 `sch_{character_id}_{day_type}_{time_slot}`。
- 同角色同 `day_type` 同 `time_slot` 第二筆自動生成 `sch_{character_id}_{day_type}_{time_slot}_2`。
- 既有任一角色 schedule 已有同 base ID 時，新 schedule 留空 ID 必須 suffix 避讓。
- 既有 world / character / location / flag / status / ending ID 與自動產生候選 ID 相同時，也必須 suffix 避讓。
- 手動輸入合法 `schedule_id` 保留原值。
- 手動輸入不合法 ID（含中文、非法字元）被擋。
- 手動輸入已存在 ID 被擋。
- 手動輸入已存在 ID 的範圍包含其他 schedule ID 與跨類型 ID。
- 手動輸入不合法或重複時，不 append schedule、不清 draft、不呼叫成功 rerun。
- `_collect_existing_ids()` 單測必須確認包含所有 `characters[].schedule[].schedule_id`。
- `_make_unique_id` 簽章與 1-G-7 一致（`inspect.signature` 斷言）。

UI 行為：

- `day_type` selectbox 含 5 個選項（含 `特定日期(specific_date)`）。
- 選 `specific_date` 時顯示 `date_input` widget，min/max 鎖定世界日期範圍。
- 選其他 `day_type` 時不顯示 `date_input`。
- 選其他 `day_type` submit 後，新增 entry 的 `specific_date` 必須為 `None` 或不出現在 dict 中；即使 session_state 有 stale 日期也不可保存。
- 清單顯示 `specific_date` 行程時含實際日期（如 `特定日期 2026-05-01 下午(afternoon) @ 圖書室 [路線(route)] order=10`）。
- `schedule_id` 輸入欄位提示「行程 ID (英文，留空則自動產生)」。
- submit 後自動產生 ID 的訊息必須在 rerun 後仍可見；可用 pending caption / toast / session message 驗證，單純 mock `st.caption` 呼叫不算完整實機驗收。
- 1-G-9 的 `_tab_characters` mock 測試必須提供 `world.start_date` / `world.end_date` 對應的 session state，以及 `mock_st.date_input` 合法 `date` 回傳，避免 MagicMock 混入 canonical data。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | specific_date 新增 | 新增行程選 `特定日期`，選日期，submit | 清單顯示含日期；匯出 YAML 含 `specific_date` | [ ] |
| 2 | specific_date 日期範圍 | 觀察 date_input widget 的可選範圍 | min/max 鎖定 world.start_date ~ world.end_date | [ ] |
| 3 | 非 specific_date 無日期欄位 | 選 `平日` 等其他 day_type | 不顯示 date_input widget | [ ] |
| 4 | schedule_id 自動生成 | 新增行程不填 ID | 自動生成 `sch_{character_id}_{day_type}_{time_slot}`，submit 後 caption 顯示 | [ ] |
| 5 | schedule_id 手動輸入 | 新增行程填入合法英文 ID | 使用填入的 ID | [ ] |
| 6 | schedule_id 留空重複避讓 | 同角色同 day_type 同 time_slot 連續新增兩筆不填 ID | 第二筆 ID 帶 `_2` suffix | [ ] |
| 7 | schedule_id 全域避讓 | 先建立任一角色 schedule ID 為 `sch_sophie_weekday_morning`，再於 Sophie 新增同 base 行程不填 ID | 新增 ID 自動避讓為 `_2`，不撞既有 schedule ID | [ ] |
| 8 | schedule_id 非法/重複保留 draft | 手動輸入中文 ID 或既有 ID 後 submit | 不新增行程，顯示錯誤，已填 day_type/time_slot/location/condition 等 draft 保留 | [ ] |
| 9 | stale 日期清除 | 先選 `特定日期` 產生 date_input，再切回 `平日` submit | 新增 weekday 行程不含 `specific_date` 或為 `None` | [ ] |
| 10 | roundtrip 驗收 | 含 specific_date 行程的 Setup Package export → parse | 資料一致，`specific_date` 日期保留 | [ ] |
| 11 | rerun 後 ID 訊息 | 新增行程不填 ID | rerun 後仍看得到自動產生的 ID 訊息 | [ ] |
| 12 | 回歸 | Phase 1-A 到 1-G-8 全測試通過 | 不破壞既有功能 | [ ] |

##### 1-G-9 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Schema 合法/非法案例全過 | 必須 |
| UIW Linter 新增三條規則（`missing_specific_date` / `unexpected_specific_date` / `specific_date_out_of_range`）全過 | 必須 |
| UIW Linter 對 `schedule_id` 的 `invalid_id_format` / `duplicate_id` 全過 | 必須 |
| `specific_date` 的 `schedule_tie` 同日報、不同日不報 | 必須 |
| UIW Linter 既有 schedule 規則回歸不破壞 | 必須 |
| Exporter / Parser roundtrip 含 `specific_date` 全過 | 必須 |
| `schedule_id` 自動生成、全域避讓、手動輸入、失敗保留 draft 全過 | 必須 |
| `_collect_existing_ids()` 納入所有 schedule ID | 必須 |
| UI 非 `specific_date` submit 不保存 stale 日期 | 必須 |
| `setup_minimal.yaml` 更新後既有測試不破壞 | 必須 |
| 1-G-9 手動驗收 #1-#12 全過 | 必須 |
| Phase 1-A 到 1-G-8 回歸不破壞 | 必須 |
| 1-G-7 `_make_unique_id` / `_generate_system_id` 簽章與行為不變 | 必須 |

---

## Phase 1 全階段回歸測試

Phase 1-F 完成後，執行：

```powershell
uv run pytest tests/ -v -m "not integration" --tb=short
```

再手動執行：

```powershell
uv run python -m sandbox_dating_sim.cli export-setup tests/fixtures/setup_minimal.yaml --out examples/
uv run streamlit run src/sandbox_dating_sim/ui/streamlit_uiw.py
```

通過標準：

| 條件 | 必要性 |
| :--- | :--- |
| 自動測試全過 | 必須 |
| CLI 可輸出 Setup Package MD | 必須 |
| Streamlit UI 可輸出 Setup Package MD | 必須 |
| 兩種產物都可 parse 回 SetupPackage | 必須 |

> Phase 1 完成後，才進入 Phase 2：Event Blueprint MVP。

---

## Phase 2：Event Blueprint MVP 測試規格

Phase 2 測試使用 `.venv\Scripts\python.exe`，不呼叫外部 AI API。Blueprint fixtures 以 `.md` 為主，內容必須是 Markdown shell + exactly one YAML code block。

建議指令：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_blueprint_schema.py tests/test_blueprint_prompt.py tests/test_blueprint_parser.py tests/test_blueprint_linter.py tests/test_blueprint_walkthrough.py tests/test_blueprint_checkpoint.py tests/test_blueprint_route_graph.py -v -m "not integration"
```

### Phase 2-A-0：Setup prerequisites

#### 目標

在 Phase 2 schema / prompt / walkthrough 前，確保 Setup Package 有穩定初始地點與固定輸出布局。

#### 建議測試檔

```text
tests/test_uiw_phase2_prerequisites.py
tests/test_cli_phase2_outputs.py
```

#### 必測項目

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | UIW 初始 state | 呼叫初始化 helper / 渲染前 state | `locations` 含 `protagonist_home` |
| 2 | protagonist_home 欄位 | 檢查預設 location dict | `location_id=protagonist_home`、`is_visitable=True`、tags 含 `system/home/rest/private`、`empty_behavior=allow_rest` |
| 3 | 不可刪除 | 渲染 locations tab 或測純函式 | `protagonist_home` 刪除按鈕隱藏/disabled，不會被 pop |
| 4 | linter 必備地點 | 移除 `protagonist_home` 後 validate | error `missing_protagonist_home` |
| 5 | protagonist_home 時段 | 修改 `available_time_slots` 少於 world.time_slots | error `invalid_protagonist_home_time_slots` |
| 6 | location 時段子集 | 任一 location 使用 world.time_slots 外時段 | error `invalid_location_time_slot` |
| 7 | export 預設路徑 | CLI export setup 不給 output | 寫入 `outputs/<world_id>/setup_package/<world_id>_setup_package.md` |
| 8 | 同名不覆寫 | 預先建立目標檔再 export | 報錯，不覆寫原檔 |

#### 手動驗收

1. 啟動 UIW。
2. 進入地點頁。
3. 確認已存在「主角家」。
4. 確認主角家不可刪。
5. 匯出 Setup Package。
6. 確認輸出路徑為 `outputs/<world_id>/setup_package/<world_id>_setup_package.md`。
7. 再匯出一次，確認同名報錯。

---

### Phase 2-A：Event Blueprint Schema

#### 建議測試檔

```text
tests/test_blueprint_schema.py
tests/fixtures/blueprint_minimal.md
tests/fixtures/blueprint_invalid_schema.md
```

#### 必測項目

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | minimal blueprint valid | 載入最小合法 YAML | 建成 `EventBlueprint` |
| 2 | top-level required | 移除 `blueprint_id` / `source_world_id` / `initial_event_id` | Pydantic validation error |
| 3 | fixed blueprint id | `blueprint_id` 欄位存在 | schema 接受；full linter 再檢查 `<world_id>_event_blueprint` |
| 4 | choice label required | 移除 `choice_label` | validation error |
| 5 | scene_summary required | 移除 `scene_summary` | validation error |
| 6 | choices min/max | 0 個 choices / 5 個 choices | validation error |
| 7 | priority literal | priority=`important` | validation error |
| 8 | repeat_policy literal | repeat_policy=`always` | validation error |
| 9 | expected_assets required | YAML 顯式有 `expected_assets` | valid；若缺欄位由 linter 或 raw parser 檢查 |
| 10 | time_cost positive | `time_cost: 0` | validation error |
| 11 | new_flags_proposed schema | boolean FlagDef valid | valid |

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Schema model 與開發設計方針一致 | 必須 |
| 1-4 choices 由 schema 擋 | 必須 |
| `choice_label` / `scene_summary` 必填 | 必須 |
| priority / repeat_policy literal 擋非法值 | 必須 |

---

### Phase 2-B：Blueprint Prompt Builder

#### 建議測試檔

```text
tests/test_blueprint_prompt.py
```

#### 必測項目

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | 回傳 Markdown prompt | 呼叫 `build_event_blueprint_prompt(...)` | 回傳 str，含章節標題 |
| 2 | 含 output format | 檢查 prompt | 明確要求 exactly one YAML code block |
| 3 | 含 schema 欄位 | 檢查 prompt | `blueprint_id`、`source_world_id`、`initial_event_id`、`events`、`new_flags_proposed` 都出現 |
| 4 | 含 DSL 白名單 | 檢查 prompt | condition/result DSL 列出，且禁止 `item.*` / `inventory.*` |
| 5 | 含 initial event 規則 | 檢查 prompt | `protagonist_home`、第一個 time slot、不可要求 flag/stat/favor/status |
| 6 | 含 ending 規則 | 檢查 prompt | 每個 ending 必須被 `ending:` 引用，且需要 `fallback_ending` event |
| 7 | 含 status 規則 | 檢查 prompt | 不依賴 `clear_rule`；status 變更需 explicit result |
| 8 | minimal_complete scope | scope=`minimal_complete` | prompt 要求最小完整事件網並 cover all endings |
| 9 | ai_decides scope | scope=`ai_decides` | prompt 要求 AI 自決事件數但 cover all endings |
| 10 | custom scope | scope=`custom` + notes/count | prompt 含 target event count / custom notes，仍要求 cover all endings |
| 11 | CLI 互動流程 | mock input 選 world/setup/scope | 寫出 `outputs/<world_id>/prompts/<world_id>_event_blueprint_prompt.md` |
| 12 | 同名不覆寫 | 預先建立 prompt 檔 | CLI 報錯，不覆寫 |
| 13 | canonical character 邊界 | 檢查 prompt | 明確禁止 AI 新增 canonical characters；背景 NPC 只能純文字出現在 `scene_summary`，不可進入 cast/assets/DSL/ending target |

#### 手動驗收

1. 先產生 Setup Package。
2. 執行：
   ```powershell
   .\.venv\Scripts\python.exe -m sandbox_dating_sim.cli build-blueprint-prompt
   ```
3. 依互動提示選 world / setup / scope。
4. 確認產生 `outputs/<world_id>/prompts/<world_id>_event_blueprint_prompt.md`。
5. 打開 prompt，確認可整份複製給外部 AI。
6. 再跑一次，確認同名報錯。

---

### Phase 2-C：Blueprint Parser + Linter

#### 建議測試檔

```text
tests/test_blueprint_parser.py
tests/test_blueprint_linter.py
tests/fixtures/blueprint_minimal.md
tests/fixtures/blueprint_invalid_no_yaml.md
tests/fixtures/blueprint_invalid_multi_yaml.md
tests/fixtures/blueprint_invalid_unknown_location.md
tests/fixtures/blueprint_invalid_missing_terminal.md
tests/fixtures/blueprint_invalid_uncovered_ending.md
```

#### Parser 必測

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | 單一 YAML block | Markdown shell + one `yaml` block | parse 成 EventBlueprint |
| 2 | yml alias | 使用 ```yml | parse 成 EventBlueprint |
| 3 | 無 YAML block | fixture 無 fenced yaml | parser error |
| 4 | 多 YAML block | fixture 兩個 yaml block | parser error |
| 5 | YAML 語法錯 | malformed YAML | parser error |
| 6 | YAML root 非 mapping | YAML block 是 list/string | parser error |
| 7 | 外部 Markdown ignored | YAML 前後有 AI 廢話 | parse 成功且只取 YAML |

#### Blueprint-only linter 必測

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | duplicate event_id | 兩個 event 同 ID | error `duplicate_event_id` |
| 2 | invalid event_id | event_id 含中文/空格/大寫 | error `invalid_id_format` |
| 3 | missing initial reference | `initial_event_id` 不存在 | error |
| 4 | duplicate choice_id | 同 event 兩個 choice 同 ID | error |
| 5 | missing terminal | choice result 無 `goto`/`ending` | error |
| 6 | multiple terminal | 同 result 同時 `goto` + `ending` | error |
| 7 | unknown goto | `goto: missing_event` | error |
| 8 | invalid DSL | 自然語言 condition/result | error |
| 9 | item/inventory syntax | `item.key == true` | error |
| 10 | duplicate time_cost override | 同 choice 兩個 `time_cost:` | error |
| 11 | empty route tag | `route_tags: [""]` | error |

#### Full linter 必測

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | world mismatch | blueprint.source_world_id != setup.world.world_id | error |
| 2 | blueprint id mismatch | blueprint_id 非 `<world_id>_event_blueprint` | error |
| 3 | source setup filename mismatch | source_setup_package 非 `<world_id>_setup_package.md` | error |
| 4 | missing protagonist_home | setup 無 protagonist_home | error |
| 5 | protagonist_home time slots mismatch | protagonist_home 時段不等於 world.time_slots | error |
| 6 | location time slot outside world | location.available_time_slots 超出 world.time_slots | error |
| 7 | initial event wrong location | initial event 不在 protagonist_home | error |
| 8 | initial event wrong slot | initial event 不在第一個 time slot | error |
| 9 | initial event gated | initial event 要求 flag/stat/favor/status | error |
| 10 | unknown location | event.location_id 不存在 | error |
| 11 | event time outside world | event.time_slot 不在 world.time_slots | error |
| 12 | insufficient time | event/choice effective time_cost 超出當日剩餘時段 | error |
| 13 | direct goto time mismatch | source + effective_time_cost 後不等於 target time_slot | error |
| 14 | unknown cast | cast character 不存在 | error |
| 15 | asset not in cast | expected_assets.characters 角色不在 cast | error |
| 16 | asset whitelist | costume/emotion/position 超出角色白名單 | error/warning 依 vocab 存在性 |
| 17 | non-boolean flag | Blueprint 引用非 boolean flag | error |
| 18 | unknown flag | condition/result 引用未知 flag | error |
| 19 | proposed flag not boolean | new_flags_proposed type != boolean | error |
| 20 | unknown stat | `stat.Luck` | error |
| 21 | unknown favor character | `character.unknown.favor` | error |
| 22 | unknown status | status_id 不存在 | error |
| 23 | invalid status target | target 不在 StatusFlag.targets | error |
| 24 | location unlock/closed parse | location conditions 引用未知 flag/status | error |
| 25 | unknown ending | `ending: missing_end` | error |
| 26 | uncovered ending | setup.endings 有 ending 未被任何 result 引用 | error |
| 27 | missing fallback ending | 無 `fallback_ending` event | error |
| 28 | fallback no ending result | `fallback_ending` event 無 ending result | error |
| 29 | daily high impact | daily event 增減 favor/stat 或 route tag | warning |
| 30 | multiple critical options | 多個可疑 critical 同時存在 | warning |

#### CLI 手動驗收

1. 準備 setup package 與 `<world_id>_event_blueprint.md`。
2. 執行：
   ```powershell
   .\.venv\Scripts\python.exe -m sandbox_dating_sim.cli validate-blueprint
   ```
3. 依互動提示選 world / setup。
4. 確認 CLI 自動找到 matching blueprint。
5. 合法 Blueprint 顯示 passed。
6. 修改 Blueprint 製造 unknown location。
7. 重跑，確認 terminal report 顯示 issue type / path / message / suggested_action。
8. 確認不寫 validation report 檔、不自動修檔。

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Parser 單一 YAML block 規則全過 | 必須 |
| Blueprint-only linter 全過 | 必須 |
| Full linter 與 SetupPackage 交叉檢查全過 | 必須 |
| Ending coverage / fallback ending error 全過 | 必須 |
| Direct goto time mismatch error 全過 | 必須 |
| CLI 只印 terminal report | 必須 |

---

### Phase 2-D：Blueprint Logic Walkthrough

#### 建議測試檔

```text
tests/test_blueprint_walkthrough.py
tests/test_blueprint_checkpoint.py
tests/test_blueprint_route_graph.py
```

#### Engine 必測

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | initial state | create_initial_walkthrough_state | date=start_date、time=first slot、location=protagonist_home、stats/favor/flags initialized |
| 2 | enters initial event | walkthrough start | 進入 `initial_event_id` event |
| 3 | time cost 1 | morning cost 1 | next time afternoon |
| 4 | time cost 2 | morning cost 2 | next time evening |
| 5 | time cost full day | morning cost len(time_slots) | next date morning |
| 6 | insufficient time | afternoon cost 3 in 3-slot world | event/choice unavailable |
| 7 | choice override time_cost | result `time_cost: 2` | 使用 override 推進 |
| 8 | boolean flag set | result `flag.x = true` | state flags 更新 |
| 9 | stat delta | result `stat.Cash -= 100` | stats 更新 |
| 10 | favor delta | result `character.sophie.favor += 5` | favor 更新 |
| 11 | status set true | result status = true | active_statuses 建立 |
| 12 | status set false | result status = false | active_statuses 移除 |
| 13 | status time_slots duration | active status remaining_time_slots | time advance 後倒數，0 移除 |
| 14 | status days duration | active status remaining_days | 跨日後倒數，0 移除 |
| 15 | clear_rule ignored | status 有 on_rest/on_location_visit | Rest/移動不自動清除 |
| 16 | location unlock AND | unlock_conditions 多條 | 全部成立才可用 |
| 17 | location closed OR | closed_conditions 多條 | 任一成立即關閉 |
| 18 | repeat once | event visited | 不再列出 |
| 19 | repeat daily | same date visited | 不列；隔天再列 |
| 20 | critical blocks lower | critical + normal 同時可用 | 只列 critical |
| 21 | multiple critical | 多個 critical 可用 | 全部列出 |
| 22 | direct goto valid | target time/location/conditions pass | 直接進 target event |
| 23 | direct goto blocked | target conditions fail | mode=blocked，含 reason |
| 24 | ending result | choice contains ending | mode=ending_reached，不再 free_roam |
| 25 | end_date exceeded | 未 ending 且 date > end_date | mode=dead_end |
| 26 | Rest/Wait action | allow_rest/allow_wait location | 推進 1 time slot，不改 status |
| 27 | free_roam grouping | 多地點事件可用 | 依 location grouping，選項全域編號 |

#### Checkpoint 必測

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | save in free_roam | make checkpoint | 寫 state + history |
| 2 | save in dead_end | make checkpoint | allowed |
| 3 | save in ending_reached | make checkpoint | allowed |
| 4 | save inside event | 嘗試保存 | rejected |
| 5 | label optional | label 空白 | `label: null` |
| 6 | checkpoint id | 保存 | `cp_YYYYMMDD_HHMMSS` |
| 7 | full state | checkpoint yaml | flags/stats/favor/status/event_visit_log 完整保存 |
| 8 | history grain | checkpoint history | 只記 event_id/choice_id/action/time context，不記 result 明細 |
| 9 | load filter | 列 checkpoints | 只列 same world_id + blueprint_id |

#### Route graph 必測

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | graph generated | build_route_graph | Markdown 含 Mermaid + adjacency summary |
| 2 | goto event edge | result `goto: target` | Mermaid event -> target |
| 3 | goto free_roam edge | result `goto: free_roam` | Mermaid event -> free_roam |
| 4 | ending edge | result `ending: x` | Mermaid event -> ending node |
| 5 | no free_roam outgoing | 有多個 runtime candidate | 不畫 free_roam -> events |
| 6 | edge label | choice_label 過長 | 截斷 |
| 7 | summary labels | adjacency summary | 同時列 choice_id + choice_label |
| 8 | graph command output | CLI `graph` | 寫 `outputs/<world_id>/route_graphs/<world_id>_event_blueprint_graph.md` |
| 9 | graph no overwrite | graph 檔已存在 | 報錯，不覆寫 |

#### CLI 手動驗收

1. 執行：
   ```powershell
   .\.venv\Scripts\python.exe -m sandbox_dating_sim.cli walkthrough-blueprint
   ```
2. 依互動提示選 world / setup。
3. 確認 CLI 自動找 `<world_id>_event_blueprint.md`。
4. 選 from beginning。
5. 選測試模式：**全知方式**。
6. 確認進入 initial event。
7. 用數字選 choice。
8. 確認 free_roam 畫面依 location 分組列出所有可觸發事件/action，使用全域數字編號。
9. 輸入 `state`，確認顯示摘要且不列 false flags。
10. 輸入 `history`，確認顯示 event title + choice_label。
11. 輸入 `s`，填 label 或空白，確認 checkpoint 寫檔。
12. 輸入 `graph`，確認 route graph 寫檔。
13. 輸入 `restart`，確認回 session 起點。
14. 走到 `ending:`，確認進入 ending_reached，只能 save / quit。
15. 重新啟動 walkthrough，選 from beginning，選測試模式：**正常模式**。
16. 確認 free_roam 畫面只列地點名稱（不顯示事件），使用全域數字選地點。
17. 選一個地點，確認畫面切換為只列該地點可觸發事件與 action。
18. 選事件並完成 choice，確認流程與全知方式 engine 行為一致。
19. 重新啟動 walkthrough，選 checkpoint（步驟 11 儲存的），確認從 checkpoint state 開始，並可選測試模式。

#### 2-D 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Engine 核心規則全過 | 必須 |
| Time progression 與 time_cost 語意全過 | 必須 |
| Location unlock AND / closed OR 全過 | 必須 |
| Status duration 自動倒數、clear_rule 不自動執行全過 | 必須 |
| Repeat once/daily 全過 | 必須 |
| Critical 遮蔽低 priority 全過 | 必須 |
| Checkpoint save/load 全過 | 必須 |
| Mermaid graph 全過 | 必須 |
| 全知方式：free_roam 全域數字列出所有事件 | 必須 |
| 正常模式：free_roam 只列地點，選後才見事件 | 必須 |
| CLI 手動驗收 #1-#19 全過 | 必須 |

---

### Phase 2-E：Setup Package Phase 2 Readiness

#### 目標

確保 UIW 匯出的 Setup Package MD 是可直接交給 Phase 2 Prompt Builder 的正式 artifact。匯出成功且 validation `passed` 不得代表草稿；若缺少劇情生成所需的最小角色與結局，必須在 UIW Linter 階段報 error。

#### 建議測試檔

```text
tests/test_uiw_linter.py
tests/test_blueprint_prompt.py
tests/test_blueprint_linter.py
tests/fixtures/setup_minimal.yaml
tests/fixtures/blueprint_minimal.md
```

#### UIW Linter 必測

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | 無 characters | `pkg.characters = []` 後 validate | failed，含 error `missing_required_character` |
| 2 | 無 endings | `pkg.endings = []` 後 validate | failed，含 error `insufficient_endings` |
| 3 | 單一 ending | `pkg.endings = pkg.endings[:1]` 後 validate | failed，含 error `insufficient_endings` |
| 4 | flags/status_flags 可空 | 清空 `flags`、`status_flags`，並移除 ending 中對 flags 的引用 | passed，無 error |
| 5 | baseline fixture | 載入 `setup_minimal.yaml` | passed，至少 1 character、至少 2 endings |

#### Prompt Builder 必測

| # | 測試 | 操作 | 預期 |
| :--- | :--- | :--- | :--- |
| 1 | 禁止新增 canonical character | 呼叫 `build_event_blueprint_prompt(...)` | prompt 含「不可新增 canonical characters」或等價文字 |
| 2 | 背景 NPC 限制 | 檢查 prompt | 背景 NPC 僅可純文字出現在 `scene_summary` |
| 3 | 背景 NPC 不可進入系統欄位 | 檢查 prompt | 明列不可進入 `cast`、`expected_assets`、DSL、ending target |

#### Blueprint Linter 回歸

Phase 2-E 不新增 `new_characters_proposed`。Blueprint full linter 仍應維持既有拒絕行為：

- `events[].cast[]` 引用不存在角色 → `unknown_cast_character`。
- `events[].expected_assets.characters[]` 不在 cast → `asset_character_not_in_cast`。
- `character.<id>.favor` condition/result 引用不存在角色 → `unknown_favor_character`。
- ending result 引用不存在 ending → `unknown_ending`。
- Setup Package ending 未被任何 result 引用 → `uncovered_ending`。
- 缺 `fallback_ending` route_tag event → `missing_fallback_ending`。
- `fallback_ending` event 無 ending result → `fallback_no_ending_result`。

#### 手動驗收

1. 啟動 UIW。
2. 建立只有 world/protagonist/location 的草稿，不新增角色。
3. 到匯出頁按「匯出」。
4. 預期：匯出失敗，顯示至少 1 個角色的 error。
5. 新增 1 個角色，只新增 1 個結局。
6. 再匯出。
7. 預期：匯出失敗，顯示至少 2 個結局的 error。
8. 補第 2 個 fallback/normal/global 類型結局。
9. 再匯出。
10. 預期：若無其他 linter error，匯出成功並可用於 `build-blueprint-prompt`。

---

## Phase 2 後續回歸

Phase 2 每個子階段完成後，至少執行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/ -v -m "not integration" --tb=short
```

Phase 2 全部完成後，執行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/ -v -m "not integration"
```

再手動跑：

```powershell
.\.venv\Scripts\python.exe -m sandbox_dating_sim.cli build-blueprint-prompt
.\.venv\Scripts\python.exe -m sandbox_dating_sim.cli validate-blueprint
.\.venv\Scripts\python.exe -m sandbox_dating_sim.cli walkthrough-blueprint
```

---

## Phase 3 之後的測試方向

### Phase 3：Map Manager / Status Manager / Route Validator

必測：

- Status Flag lifecycle。
- Schedule Conflict Resolution。
- FreeRoamState。
- container / sub_location 進入權限。
- Critical Path Mode。

### Phase 4：Scene Draft / Final Script

必測：

- Scene Draft 不可修改 Event Blueprint 邏輯。
- choice_id 一致。
- result 一致。
- time_cost / goto 不變。
- 素材白名單檢查。

---

## 故障排除

| 問題 | 常見原因 | 解法 |
| :--- | :--- | :--- |
| `ModuleNotFoundError: No module named 'sandbox_dating_sim'` | `pythonpath` 未設或不在專案根目錄執行 | 確認 `pyproject.toml` 與執行目錄 |
| `ValidationError` 發生在 linter 前 | 缺 required field，被 Pydantic schema 擋下 | 改寫 schema 測試，不要放進 linter 測試 |
| Roundtrip 後 list 順序不同 | YAML dump/load 或 model dump 未保序 | 使用 Python dict/list 原順序，測試固定順序 |
| 中文 label 變亂碼 | 編碼不是 UTF-8 | 讀寫檔案指定 UTF-8 |
| `uiw_version` 不是 `1.2` | 常數或 schema 預設值未更新 | 檢查 `core/constants.py` 與 `SetupPackage` 預設值 |
| Streamlit schedule 下拉沒有地點 | 地點尚未存入 session state 或只顯示 container | 下拉應讀取 `is_visitable=true` 的 sub_location / standalone |
| container 驗證沒報錯 | linter 未檢查子地點 | 加上 container without sub_location 測試 |
