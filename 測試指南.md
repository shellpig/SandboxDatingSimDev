# SandboxDatingSimDev 測試指南

## 版本紀錄

| 版本 | 日期 | 內容 |
| :--- | :--- | :--- |
| v1.1 | 2026-05-01 | 新增 Phase 1-G-3 測試要求：語意型欄位 `id + label`、主角與 NPC 秘密複選、職業/秘密預設擴充、中文自訂輸入與系統 ID 驗證。 |
| v1.0 | 2026-04-27 | 初版，定義 Phase 0 與 Phase 1-A 到 1-F 的測試流程、執行指令、通過標準與手動驗收清單。 |

> **目的：** 本文件定義 SandboxDatingSimDev 每個 Phase / 子階段的測試流程。
> 包含：環境準備、執行指令、預期結果、通過/失敗判定標準、故障排除。
> **每個子階段測試全數通過，才能進入下一個子階段。**

---

## 測試環境準備

### Step 1：安裝依賴

```powershell
cd "C:\Users\User\OneDrive\桌面\AI_Work\ProjectGpt\SandboxDatingSimDev"
uv sync --extra dev
```

若尚未安裝 `uv`：

```powershell
irm https://astral.sh/uv/install.ps1 | iex
```

驗證：

```powershell
uv --version
uv run python -c "import pydantic, yaml, typer, rich; print('ALL OK')"
```

預期輸出：

```text
ALL OK
```

### Step 2：確認 pytest 設定

`pyproject.toml` 應包含：

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "integration: 需要外部 API、瀏覽器或網路連線的測試",
]
```

Phase 1 不需要外部 AI API。

---

## 測試指令速查表

| 用途 | 指令 |
| :--- | :--- |
| 跑全部單元測試 | `uv run pytest tests/ -v -m "not integration"` |
| 跑全部測試 | `uv run pytest tests/ -v` |
| 跑指定測試檔 | `uv run pytest tests/test_setup_exporter.py -v` |
| 跑單一測試 | `uv run pytest tests/test_setup_parser.py::test_parse_exported_markdown_roundtrip -v` |
| 顯示簡短錯誤 | `uv run pytest tests/ -v --tb=short` |
| 顯示完整錯誤 | `uv run pytest tests/ -v --tb=long` |
| 只收集測試 | `uv run pytest tests/ --collect-only` |
| 覆蓋率 | `uv run pytest tests/ --cov=src --cov-report=term-missing -m "not integration"` |

---

## 測試分層原則

### Schema Validation

Pydantic schema validation 負責資料形狀與基本型別。

應由 schema 測試覆蓋：

- 缺 required field。
- 欄位型別錯誤。
- 巢狀資料結構錯誤。
- enum 值不合法。
- 必填欄位完全不存在。

範例：

```python
def test_schema_rejects_missing_schedule_order():
    """schedule_order 缺欄位時，Pydantic validation fail。"""
```

### UIW Linter

UIW Linter 負責語意、引用與跨欄位邏輯。

應由 linter 測試覆蓋：

- ID 重複。
- ID 格式不合法。
- 欄位存在但值為空陣列或空字串。
- schedule 引用不存在地點。
- container 沒有任何可進入 sub_location。
- ending 引用不存在角色。
- ending 條件引用未宣告 flag。
- status flag 的 `clear_rule` 存在但為空陣列。
- `duration.type: permanent` 但沒有 `permanent_reason`。
- schedule 同角色、同時間段、同 priority 且條件可能同時成立。

範例：

```python
def test_linter_rejects_empty_clear_rule():
    """clear_rule 欄位存在但為空陣列時，由 UIW Linter 回報 issue。"""
```

不要在 UIW Linter 測試中期待「缺 required field」能進入 linter。這類錯誤應由 schema 測試覆蓋。

---

## Phase 0：專案骨架與基礎工具

### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/core/ids.py` | `tests/test_ids.py` |

### 前置條件

- [ ] `pyproject.toml` 已建立。
- [ ] `src/sandbox_dating_sim/core/constants.py` 已建立。
- [ ] `src/sandbox_dating_sim/core/exceptions.py` 已建立。
- [ ] `src/sandbox_dating_sim/core/ids.py` 已建立。

### 執行指令

```powershell
uv run pytest tests/test_ids.py -v
```

預期：

```text
tests/test_ids.py::test_valid_id PASSED
tests/test_ids.py::test_invalid_id_starts_with_number PASSED
tests/test_ids.py::test_invalid_id_contains_chinese PASSED
tests/test_ids.py::test_normalize_id PASSED
```

### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| `tests/test_ids.py` 全部 PASSED | 必須 |
| 中文 ID 不得被自動音譯 | 必須 |

---

## Phase 1：Setup Package MVP

Phase 1 的核心目標：

```text
fixture YAML
-> SetupPackage schema
-> UIW Linter
-> Setup Package MD export
-> Setup Package MD parse
-> roundtrip
-> 1-F 表單原型輸出同樣格式
```

1-F 前置門檻：

> **Phase 1-A 到 1-E 的單元測試必須先全數通過，才能開始 1-F。**

理由：1-F 會輸出複雜巢狀 YAML。如果 parser、exporter 與 roundtrip 尚未穩定，UI 問題會和資料問題混在一起，debug 成本會很高。

---

### 1-A Canonical Setup Schema

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/schema/setup.py` | `tests/test_setup_schema.py` |
| `src/sandbox_dating_sim/schema/validation.py` | `tests/test_setup_schema.py` |

#### 固定資料

| Fixture | 用途 |
| :--- | :--- |
| `tests/fixtures/setup_minimal.yaml` | 合法最小設定 |
| `tests/fixtures/setup_invalid_missing_required.yaml` | 缺 required field |

#### 執行指令

```powershell
uv run pytest tests/test_setup_schema.py -v
```

#### 必測項目

```python
def test_minimal_setup_package_loads():
    """setup_minimal.yaml 可載入為 SetupPackage。"""

def test_schema_rejects_missing_character_id():
    """角色缺 character_id 時 Pydantic validation fail。"""

def test_schema_rejects_missing_schedule_order():
    """schedule 缺 schedule_order 時 Pydantic validation fail。"""

def test_schema_rejects_missing_status_duration():
    """status flag 缺 duration 時 Pydantic validation fail。"""

def test_schema_rejects_missing_clear_rule():
    """status flag 缺 clear_rule 時 Pydantic validation fail。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| 合法 fixture 可載入 | 必須 |
| 缺 required field 由 Pydantic 擋下 | 必須 |
| `uiw_version == "1.2"` | 必須 |

---

### 1-B UIW Linter

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/uiw/linter.py` | `tests/test_uiw_linter.py` |

#### 執行指令

```powershell
uv run pytest tests/test_uiw_linter.py -v
```

#### 必測項目

```python
def test_linter_passes_minimal_fixture():
    """setup_minimal.yaml 應通過。"""

def test_linter_rejects_duplicate_character_id():
    """重複 character_id 應產生 error。"""

def test_linter_rejects_unknown_schedule_location():
    """schedule location_id 不存在應產生 error。"""

def test_linter_rejects_container_without_sub_location():
    """container 沒有可進入 sub_location 應產生 error。"""

def test_linter_rejects_empty_clear_rule():
    """clear_rule 欄位存在但為空陣列時應產生 error。"""

def test_linter_rejects_permanent_without_reason:
    """duration.type=permanent 但缺 permanent_reason 應產生 error。"""

def test_linter_detects_schedule_conflict():
    """同角色同時間段同 priority 且條件可能同時成立時應產生 issue。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| 合法 fixture `status: passed` | 必須 |
| 語意錯誤產生 `ValidationReport.issues` | 必須 |
| issue 至少包含 `severity`、`type`、`path`、`message` | 必須 |

---

### 1-C Setup Package MD Exporter

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/pipeline/markdown.py` | `tests/test_setup_exporter.py` |
| `src/sandbox_dating_sim/pipeline/setup_exporter.py` | `tests/test_setup_exporter.py` |

#### 執行指令

```powershell
uv run pytest tests/test_setup_exporter.py -v
```

#### 必測項目

```python
def test_exporter_outputs_markdown_with_single_yaml_block():
    """輸出包含 # Setup Package，且只有一個主要 yaml code block。"""

def test_exporter_includes_validation_report():
    """輸出 YAML 包含 validation_report。"""

def test_exporter_writes_uiw_version_1_2:
    """輸出 YAML 的 uiw_version 必須是 1.2。"""

def test_write_file_uses_world_id_filename(tmp_path):
    """輸出檔名為 <world_id>_setup_package.md。"""

def test_exporter_preserves_chinese_labels():
    """中文 label 在 YAML 中保留，canonical ID 不被中文取代。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Markdown 可讀 | 必須 |
| YAML 可 parse | 必須 |
| `validation_report` 必定存在 | 必須 |
| `uiw_version` 必定為 `"1.2"` | 必須 |

---

### 1-D Setup Package Parser 與 Roundtrip

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/pipeline/setup_parser.py` | `tests/test_setup_parser.py` |
| `src/sandbox_dating_sim/pipeline/markdown.py` | `tests/test_setup_parser.py` |

#### 執行指令

```powershell
uv run pytest tests/test_setup_parser.py -v
```

#### 必測項目

```python
def test_parse_exported_markdown_roundtrip():
    """SetupPackage -> Markdown -> SetupPackage 後核心欄位一致。"""

def test_parse_rejects_missing_yaml_block():
    """沒有 yaml code block 時拋出 MarkdownParseError。"""

def test_parse_rejects_invalid_schema():
    """YAML 欄位不完整時 validation fail。"""

def test_roundtrip_preserves_nested_locations_and_schedule():
    """巢狀 locations 與 character schedule 在 roundtrip 後一致。"""

def test_roundtrip_preserves_status_flag_lifecycle():
    """duration、clear_rule、permanent_reason 在 roundtrip 後一致。"""

def test_roundtrip_preserves_asset_vocabularies_and_aliases():
    """asset vocabularies 與 alias tables 在 roundtrip 後一致。"""
```

#### Parser 穩健性要求

Parser 必須：

- 只讀取指定的主要 YAML code block。
- 遇到沒有 YAML block 時明確失敗。
- 遇到多個 YAML block 時採用第一個主要 block，或明確要求 exporter 只輸出一個 block。
- 保留中文 label。
- 保留 list 順序。
- 保留 `schedule_order`。
- 保留巢狀資料，例如 `characters[].schedule`。

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| roundtrip 核心欄位一致 | 必須 |
| 巢狀 Location / Schedule roundtrip 一致 | 必須 |
| Status lifecycle roundtrip 一致 | 必須 |
| 中文 label 不遺失 | 必須 |

---

### 1-E CLI：產出 Setup Package MD

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/cli.py` | `tests/test_cli.py` |

#### 執行指令

```powershell
uv run pytest tests/test_cli.py -v
```

手動 smoke test：

```powershell
uv run python -m sandbox_dating_sim.cli export-setup tests/fixtures/setup_minimal.yaml --out examples/
```

預期產物：

```text
examples/<world_id>_setup_package.md
```

#### 必測項目

```python
def test_cli_export_setup_creates_file(tmp_path):
    """CLI 可從 fixture YAML 產出 Setup Package MD。"""

def test_cli_export_setup_output_can_parse(tmp_path):
    """CLI 產出的 MD 可被 SetupPackageParser 讀回。"""

def test_cli_invalid_input_returns_nonzero(tmp_path):
    """不合法 input 應回傳非 0 或丟出清楚錯誤。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| CLI 能產生檔案 | 必須 |
| 產物可 parse 回 `SetupPackage` | 必須 |
| invalid input 不產生半壞輸出 | 必須 |

---

### Phase 1-A 到 1-E 回歸測試

1-F 開始前必跑：

```powershell
uv run pytest `
  tests/test_ids.py `
  tests/test_setup_schema.py `
  tests/test_uiw_linter.py `
  tests/test_setup_exporter.py `
  tests/test_setup_parser.py `
  tests/test_cli.py `
  -v -m "not integration" --tb=short
```

通過標準：

| 條件 | 必要性 |
| :--- | :--- |
| 全部 PASSED | 必須 |
| 無 parser roundtrip 失敗 | 必須 |
| CLI 產物可 parse | 必須 |

> 只有這一關通過，才進入 1-F。

---

### 1-F Interactive UIW Prototype

#### 測試方式

1-F 包含 Streamlit UI，因此採用：

- `tests/test_uiw_defaults.py` 自動測試 defaults。
- Phase 1-A 到 1-E 自動測試保護資料契約。
- 手動驗收清單驗證 UI 行為。

#### 自動測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/uiw/defaults.py` | `tests/test_uiw_defaults.py` |

#### 執行指令

```powershell
uv run pytest tests/test_uiw_defaults.py -v
```

#### 必測項目

```python
def test_style_presets_have_id_and_label():
    """每個 style preset 都有 id 與中文 label。"""

def test_location_templates_have_required_fields():
    """每個 location template 都有 template_id、label、location_type。"""

def test_container_templates_suggest_sub_locations():
    """container template 必須提供 suggested_sub_locations。"""

def test_template_ids_are_unique():
    """template_id 不可重複。"""

def test_template_ids_are_valid_canonical_ids():
    """template_id 與建議 location_id 必須符合 ID 規則。"""
```

#### 啟動指令

```powershell
uv run streamlit run src/sandbox_dating_sim/ui/streamlit_uiw.py
```

預期：

```text
Local URL: http://localhost:8501
```

#### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 無錯啟動 | 執行 Streamlit 啟動指令 | 頁面正常顯示，終端無 traceback | [ ] |
| 2 | World | 填 world_id、title、日期 | 可產生 world YAML | [ ] |
| 3 | 風格參考詞 | 點選都市戀愛、黑色幽默等 preset | `world.global_style` 加入對應英文 ID | [ ] |
| 4 | Protagonist | 填主角資料與初始數值 | 數值進入 `protagonist.initial_stats` | [ ] |
| 5 | 地點模板 | 套用「學校」模板 | 建立 container 草稿，並提示建立子地點 | [ ] |
| 6 | Container 規則 | 建立 container 但不建子地點後按 Validate | validation report 顯示錯誤 | [ ] |
| 7 | 子地點 | 在學校下新增圖書室 | `parent_location_id` 自動帶入 `school` | [ ] |
| 8 | 獨立地點 | 新增主角家 | `location_type=standalone` 且沒有 parent | [ ] |
| 9 | Character | 建立角色並選表情、服裝、位置 | 白名單輸出 canonical ID | [ ] |
| 10 | Schedule | 新增角色 schedule | location 下拉只顯示可進入地點 | [ ] |
| 11 | Status Flag | 新增 overworked | 必須填 duration 與 clear_rule | [ ] |
| 12 | Ending | 新增 ending | 可填 required_flags、required_stats、route_tags | [ ] |
| 13 | Validate | 點驗證 | 顯示 UIW Linter report | [ ] |
| 14 | YAML Preview | 進入 Review | 顯示 SetupPackage YAML，`uiw_version: 1.2` | [ ] |
| 15 | Export | 點匯出 | 產出 `<world_id>_setup_package.md` | [ ] |
| 16 | Parser Roundtrip | 用 parser 讀回匯出的 MD | 成功 parse 為 SetupPackage | [ ] |

#### 1-F 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-E 回歸全過 | 必須 |
| `tests/test_uiw_defaults.py` 全過 | 必須 |
| 手動驗收 #1-#16 全過 | 必須 |
| 匯出的 MD 可由 parser 讀回 | 必須 |

#### 1-F 範圍提醒

1-F 是 Functional Prototype。

允許：

- 樸素表單。
- Tabs / expanders。
- 表格 + 新增按鈕。
- parent 下拉選單。

不要求：

- 正式產品級 UI。
- 拖拉地圖樹。
- Flowchart。
- AI API 串接。
- 完整 Dashboard。

---

### 1-G 使用者實測回饋迭代

1-G 是 1-F Functional Prototype 後的使用者操作回饋階段。每個子階段依需求範圍補自動測試與手動驗收。

#### 1-G-3 語意型欄位 id + label

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/schema/setup.py` | `tests/test_setup_schema.py` |
| `src/sandbox_dating_sim/uiw/defaults.py` | `tests/test_uiw_defaults.py` |
| `src/sandbox_dating_sim/uiw/linter.py` | `tests/test_uiw_linter.py` |
| `src/sandbox_dating_sim/pipeline/setup_exporter.py` | `tests/test_setup_exporter.py` |
| `src/sandbox_dating_sim/pipeline/setup_parser.py` | `tests/test_setup_parser.py` |
| `src/sandbox_dating_sim/cli.py` | `tests/test_cli.py` |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py` | 手動驗收 |

##### 必測項目

- `world.global_style` 可保存 `id + label` list。
- `protagonist.occupation` 可保存 `id + label`。
- `protagonist.personality` 可保存 `id + label`。
- `protagonist.secrets` 可保存最多 3 個 `id + label`。
- `characters[].personality_tags` 可保存 `id + label` list。
- `characters[].secrets` 可保存最多 3 個 `id + label`。
- 語意型欄位 `id` 必須符合 canonical ID 規則。
- 語意型欄位 `label` 不可為空。
- `protagonist.secrets` 超過 3 個時 UIW Linter 應回報 error。
- `characters[].secrets` 超過 3 個時 UIW Linter 應回報 error。
- `none = 沒有秘密` 不可與其他秘密並存。
- `world.global_style[].id` 不可重複。
- `characters[].personality_tags[].id` 不可重複。
- exporter 輸出 `id + label`。
- parser roundtrip 不遺失中文 label。
- CLI 產物仍可 parse 回 `SetupPackage`。

##### defaults 測試

`tests/test_uiw_defaults.py` 必須驗證：

- 主角職業預設選項總數為 16。
- 新增職業包含 `rich_heir`、`startup_founder`、`tech_worker`、`finance_professional`。
- 主角與 NPC 共用的秘密預設選項新增 8 個，並保留 `none`。
- 所有預設選項都有 `id` 與 `label`。
- 所有預設選項 ID 不重複且符合 canonical ID 規則。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 職業預設 | 點選「咖啡廳店員」 | 顯示「目前選擇：咖啡廳店員」，系統 ID 為 `cafe_staff` | [ ] |
| 2 | 職業新增預設 | 點選「富二代」「新創公司老闆」「科技業」「金融業」之一 | 寫入對應 `id + label` | [ ] |
| 3 | 自訂中文職業 | 在自訂職業名稱輸入中文並按「選擇」 | 自動產生系統 ID，下方欄位可進階修改 | [ ] |
| 4 | 主角性格 | 點選或自訂中文性格 | 寫入 `protagonist.personality.id` 與 `label` | [ ] |
| 5 | 主角秘密複選 | 選 1-3 個秘密 | 寫入 `protagonist.secrets` list | [ ] |
| 6 | 主角秘密上限 | 嘗試選第 4 個秘密 | UI 阻止或 linter 回報錯誤 | [ ] |
| 7 | 沒有秘密互斥 | 選「沒有秘密」後再選其他秘密 | UI 阻止或 linter 回報錯誤 | [ ] |
| 8 | 角色性格標籤 | 選預設與自訂中文 tag | 寫入 `characters[].personality_tags[].id/label` | [ ] |
| 9 | NPC 秘密複選 | 替角色選 1-3 個秘密 | 寫入 `characters[].secrets` list | [ ] |
| 10 | YAML Preview | 進入 Review | 語意型欄位皆顯示 `id + label` | [ ] |
| 11 | Export / Roundtrip | 匯出 MD 後用 parser 讀回 | 中文 label 不遺失 | [ ] |

##### 1-G-3 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| 1-G-3 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#11 全過 | 必須 |
| 匯出的 MD 可由 parser 讀回且中文 label 不遺失 | 必須 |

---

## Phase 1 全階段回歸測試

Phase 1-F 完成後，執行：

```powershell
uv run pytest tests/ -v -m "not integration" --tb=short
```

再手動執行：

```powershell
uv run python -m sandbox_dating_sim.cli export-setup tests/fixtures/setup_minimal.yaml --out examples/
uv run streamlit run src/sandbox_dating_sim/ui/streamlit_uiw.py
```

通過標準：

| 條件 | 必要性 |
| :--- | :--- |
| 自動測試全過 | 必須 |
| CLI 可輸出 Setup Package MD | 必須 |
| Streamlit UI 可輸出 Setup Package MD | 必須 |
| 兩種產物都可 parse 回 SetupPackage | 必須 |

> Phase 1 完成後，才進入 Phase 2：Event Blueprint MVP。

---

## Phase 2 之後的測試方向

Phase 2 後續應另行補完整測試細節。先保留方向：

### Phase 2：Event Blueprint MVP

必測：

- Event Blueprint schema。
- Prompt builder 不要求完整對話。
- Blueprint parser。
- Blueprint linter。
- 新 flag 必須列入 `new_flags_proposed`。
- expected assets 不超出白名單。

### Phase 3：Map Manager / Status Manager / Route Validator

必測：

- Status Flag lifecycle。
- Schedule Conflict Resolution。
- FreeRoamState。
- container / sub_location 進入權限。
- Critical Path Mode。

### Phase 4：Scene Draft / Final Script

必測：

- Scene Draft 不可修改 Event Blueprint 邏輯。
- choice_id 一致。
- result 一致。
- time_cost / goto 不變。
- 素材白名單檢查。

---

## 故障排除

| 問題 | 常見原因 | 解法 |
| :--- | :--- | :--- |
| `ModuleNotFoundError: No module named 'sandbox_dating_sim'` | `pythonpath` 未設或不在專案根目錄執行 | 確認 `pyproject.toml` 與執行目錄 |
| `ValidationError` 發生在 linter 前 | 缺 required field，被 Pydantic schema 擋下 | 改寫 schema 測試，不要放進 linter 測試 |
| Roundtrip 後 list 順序不同 | YAML dump/load 或 model dump 未保序 | 使用 Python dict/list 原順序，測試固定順序 |
| 中文 label 變亂碼 | 編碼不是 UTF-8 | 讀寫檔案指定 UTF-8 |
| `uiw_version` 不是 `1.2` | 常數或 schema 預設值未更新 | 檢查 `core/constants.py` 與 `SetupPackage` 預設值 |
| Streamlit schedule 下拉沒有地點 | 地點尚未存入 session state 或只顯示 container | 下拉應讀取 `is_visitable=true` 的 sub_location / standalone |
| container 驗證沒報錯 | linter 未檢查子地點 | 加上 container without sub_location 測試 |
