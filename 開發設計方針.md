# SandboxDatingSimDev 開發設計方針

## 版本紀錄

| 版本 | 日期 | 內容 |
| :--- | :--- | :--- |
| v1.22 | 2026-05-03 | 修正 Phase 1-G-8 `_parse_effect_yaml` 非字串輸入會卡死 PyYAML Reader 的問題：函式開頭新增 `isinstance(yaml_str, str)` 防線，避免 MagicMock 等 truthy 物件被當 file-like 進入 `stream.read(N)` 死迴圈；測試指南補純函式單測案例。 |
| v1.21 | 2026-05-03 | 補強 Phase 1-G-8 規格：挑邊「空 `targets` 由 schema `Field(min_length=1)` 拒絕」+「parser/`model_validator(mode="before")` 在 schema 層 normalize 舊 `target: str` 為 `targets: list[str]`」；明列既有 fixture / 測試遷移工作（`setup_minimal.yaml`、`test_setup_schema.py`、`test_uiw_linter.py`）並新增舊格式相容 fixture；明列 1-G-5 角色刪除防呆與 1-G-6 結局頁需同步改用 `targets contains`；新增 UIW Linter issue types `unknown_status_target` / `empty_status_targets` 名稱與訊息範例；引用比對挑邊「word-boundary regex」；`duration.type` 為 `until_event` / `until_cleared` 時 value 自動存 None 並由 linter warning；補純函式 helper（`_normalize_legacy_status` / `_parse_effect_yaml` / `_status_targets_options`）必跑單測；補 silent migration 端對端驗收鏈路；補 effect YAML corner cases、targets UI dedupe、duplicate widget key prefix 與通過標準明列 1-G-7 helper 簽章不變。 |
| v1.20 | 2026-05-03 | 新增 Phase 1-G-8 規格：旗標 / 狀態頁比照地點、角色與結局頁改為清單 + inline expander 編輯；旗標與狀態 ID 唯讀；新增刪除引用防呆與 keyed state 清理；`StatusFlag.target` 升級為 `targets: list[str]`，UI 以主角 + 角色複選；status 持續類型改為中文(英文) selectbox 並加說明；`effect` 採 YAML text_area 以保留 list[dict] 結構；`status_id` linter 補洞標記為已在 1-G-7 收尾先行修復。 |
| v1.19 | 2026-05-03 | 補強 Phase 1-G-7 規格：全域 ID 集合補入固定 `protagonist`；明示「自動 ID 路徑因 prefix 隔離不會跨類衝突，跨類唯一只在進階手動 ID／模板套用／匯入既有資料時觸發」；模板衝突挑邊為「自動 suffix 避讓」；預覽 ID 時機定為「submit 後 caption」；suffix 連鎖規則為「順序遞增 `_2`、`_3`、`_4`，不回填空缺」；測試要求補純函式 helper 單測必跑、suffix 連鎖至少 `_3`、自動 ID 失敗保留 draft、1-G-3 `_generate_system_id` 行為不變回歸、實機 end-to-end UIW → export → parse roundtrip。 |
| v1.18 | 2026-05-03 | 修正 Phase 1-G-7 規格：結局範例 ID 改為純拼音 `end_su_fei_hao_jie_ju`；旗標 ID 來源欄位改為 `description`；補 description slug 截斷、標點處理、`unnamed` fallback 與 `_make_unique_id` 包裝既有 `_generate_system_id` 的限制。 |
| v1.17 | 2026-05-03 | 新增 Phase 1-G-7 規格：地點 / NPC / 旗標 / 狀態 / 結局新增流程不再要求使用者輸入英文 ID，改由 UI 依中文名稱、標題、description 或 label 自動產生唯一 canonical ID；既有 ID rename / 引用搬移不納入本 phase。 |
| v1.16 | 2026-05-01 | 新增 Phase 1-G-6 規格：拆分旗標/狀態與結局為兩個分頁、結局頁 inline expander 編輯、priority 中文化 + 五級語意 caption、`target_character_id` 中文 selectbox、`required_flags` / `forbidden_flags` F-γ multiselect 快速加入、最小可用 Setup Package（flags=[] / status_flags=[]）匯出驗收；附帶修正 1-G-5 character_id 即時擋為跨類型唯一（UI 層含 `status_id`）；排程重排 1-G-7=新增流程自動產生唯一 canonical ID、1-G-8=旗標/狀態頁 inline 編輯、1-G-9=schedule `specific_date` schema 升級。 |
| v1.15 | 2026-05-01 | 補強 Phase 1-G-5 實作限制：禁止 nested `st.expander`、所有編輯 widget 採 per-character keyed pattern、新增角色時擋空/重複/非法 `character_id` 並禁止編輯期修改 `character_id`、刪除角色後清空該角色相關 session state。 |
| v1.14 | 2026-05-01 | 新增 Phase 1-G-5 規格：角色設定頁面 UX 完整化，包含角色清單顯示與刪除引用防呆、既有角色 inline 編輯、行程清單顯示與刪除、行程 enum 中文化、白名單顯示與進階設定收納；明確排除新增流程自動產生 ID（現排 1-G-7）與 schedule `specific_date` schema 升級。 |
| v1.13 | 2026-05-01 | 修正 Phase 1-G-4 規格缺口：補列既有 standalone 模板、明確子地點 ID 組成、區分同名模板顯示，並補強 1-G-4 回歸門檻。 |
| v1.12 | 2026-05-01 | 新增 Phase 1-G-4 規格：調整地點與角色頁順序、擴充地點模板、改善地點刪除防呆、中文化地點類型與時段選項，並將地點標籤改為 AI 劇情參考用進階設定。 |
| v1.11 | 2026-05-01 | 補充 Phase 1-G-3 完成後規則：`secrets: []` 表示沒有秘密，`none` 僅為 UI 清空操作；記錄 1-G-3 已完成與驗證項目。 |
| v1.10 | 2026-05-01 | 新增 Phase 1-G-3 規格：語意型欄位改為 `id + label`、主角與 NPC 秘密改為最多 3 個複選、新增職業與秘密預設選項，並改善中文自訂輸入 UX。 |
| v1.9 | 2026-04-28 | 新增 Phase 1-G-2 規格：主角 ID 固定隱藏、性別/性取向/定位選項中文化、主角預設年齡 29、角色性格標籤預設選項，以及自訂 canonical ID 欄位英文輸入說明。 |
| v1.8 | 2026-04-28 | 新增 Phase 1-G 使用者實測回饋迭代；補充 Phase 1-G-1 的 UIW 分頁順序、中文/英文導覽顯示、下一頁導覽、主角預設選項與債務等級模式。 |
| v1.7 | 2026-04-28 | 補充 Phase 1-F 使用者啟動方式需提供可雙擊的 `啟動_UIW.bat`；新增 Streamlit 匯出體驗待辦，要求 UI 內顯示驗證狀態與輸出檔案位置。 |
| v1.6 | 2026-04-27 | 新增 `已知問題.md` 作為規格、schema、fixture、測試與實作對齊問題的持續追蹤文件；進入新階段前需先檢查未解決項目。 |
| v1.5 | 2026-04-27 | 對齊初始設定 v1.2.3：`StatusFlag.effect` schema 改為 `list[dict[str, Any]]`；ending 條件以 `flag.<id> == <value>` 完整表達式為 canonical。 |
| v1.4 | 2026-04-27 | 補充 `測試指南.md` 為實作驗收文件，要求各 Phase 依測試指南逐階段通過後才能推進。 |
| v1.3 | 2026-04-27 | 補充 Schema validation 與 UIW Linter 的責任邊界；明確定義 Phase 1-F 為功能完整但介面樸素的 Functional Prototype。 |
| v1.2 | 2026-04-27 | 將實作常數、Schema 預設值與 Setup Package 範例中的 `uiw_version` 統一為 `1.2`；文件管線引用改為 v1.1。 |
| v1.1 | 2026-04-27 | 新增 Phase 1-F Interactive UIW Prototype，要求使用者可透過表單填寫/選擇並輸出 Setup Package MD；補充參考選項、地點模板與 UI 編排要求。 |
| v1.0 | 2026-04-27 | 初版，依正式規格 v1.2、初始設定 v1.1、文件管線規格 v1.0 拆分 Phase 0-7。 |

> **目的：** 本文件是給 AI Agent 或開發者的實作指引，依照下列文件整理：
>
> - `Sandbox_Dating_Sim_Dev_正式規格_v1.2.md`
> - `Sandbox_Dating_Sim_初始設定_v1.2.md`
> - `Sandbox_Dating_Sim_文件管線規格_v1.1.md`
> - `測試指南.md`
> - `已知問題.md`
>
> 閱讀本文件的實作者應可直接開始拆檔案、建資料模型、寫 exporter、寫 linter 與測試，不需要再回頭猜規格意圖。

---

## 全域規範（所有 Phase 共用）

### 專案根目錄

```text
C:\Users\User\OneDrive\桌面\AI_Work\ProjectGpt\SandboxDatingSimDev\
```

### 技術選型建議

Phase 1 先做核心工具、CLI 與輕量 Interactive UIW Prototype，不做完整 Dashboard。

- Python 3.12+
- 套件管理建議使用 `uv`
- 資料模型使用 `pydantic`
- YAML 讀寫使用 `pyyaml`
- CLI 使用 `typer`
- 測試使用 `pytest`

### pyproject.toml 建議

```toml
[project]
name = "sandbox-dating-sim-dev"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "typer>=0.12",
    "rich>=13.7",
    "streamlit>=1.37",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 120
```

### 型別與命名

- 所有公開函式必須有 type hint。
- 所有資料模型欄位必須對齊規格中的 canonical field。
- 系統 ID 必須使用小寫英文、數字、底線。
- 使用者介面可顯示中文，但 DSL、YAML、檔名與 validator 一律使用英文 ID。

ID 驗證規則：

```python
ID_PATTERN = r"^[a-z][a-z0-9_]*$"
```

### 文件輸出原則

工具內部以 JSON/YAML 物件作為 canonical data。

Markdown 只作為：

- 給使用者檢查
- 給 AI 讀取
- 給版本管理 diff
- 給人工修訂

Setup Package MD 與 Event Blueprint MD 都應採用：

```text
Markdown 外殼 + 單一主要 YAML code block
```

### 測試原則

- 每個 Phase 必須有對應測試。
- 每個 Phase / 子階段的詳細測試流程以 `測試指南.md` 為準。
- 進入任一新 Phase / 子階段前，必須先檢查 `已知問題.md` 是否有該階段的未解決對齊項目。
- Phase 1 不呼叫外部 AI API。
- Phase 1 的測試只驗證資料模型、linter、exporter、parser roundtrip。
- 所有 fixture 放在 `tests/fixtures/`。

### Schema Validation 與 Linter 責任邊界

實作者必須區分 Pydantic schema validation 與 UIW Linter 的責任。

Schema validation 負責資料形狀與基本型別：

- 缺少 required field。
- 欄位型別錯誤。
- 巢狀資料結構錯誤。
- enum 值不合法。
- 必填欄位完全不存在。

UIW Linter 負責語意、引用與跨欄位邏輯：

- ID 重複。
- ID 格式不合法。
- 欄位存在但值為空陣列或空字串。
- schedule 引用不存在的地點。
- schedule 引用不存在的 time slot。
- container 沒有任何可進入 sub_location。
- ending 引用不存在角色。
- ending 條件引用未宣告 flag。
- status flag 的 `clear_rule` 存在但為空陣列。
- status flag 的 `duration.type` 為 `permanent` 但沒有 `permanent_reason`。
- schedule 同角色、同時間段、同 priority 且條件可能同時成立。
- asset vocabulary 存在但不完整。

測試也必須分層。

範例：

```python
def test_schema_rejects_missing_schedule_order():
    """缺 required field 時，Pydantic validation fail。"""

def test_linter_rejects_unknown_schedule_location():
    """欄位存在，但引用不存在地點時，由 UIW Linter 回報 issue。"""

def test_linter_rejects_empty_clear_rule():
    """clear_rule 欄位存在但為空陣列時，由 UIW Linter 回報 issue。"""
```

不要在 UIW Linter 測試中期待「缺 required field」能進入 linter。這類錯誤應由 schema 測試覆蓋。

---

## 建議目錄結構

```text
SandboxDatingSimDev/
├── src/
│   └── sandbox_dating_sim/
│       ├── __init__.py
│       ├── core/
│       │   ├── __init__.py
│       │   ├── constants.py
│       │   ├── exceptions.py
│       │   └── ids.py
│       ├── schema/
│       │   ├── __init__.py
│       │   ├── setup.py
│       │   ├── blueprint.py
│       │   └── validation.py
│       ├── uiw/
│       │   ├── __init__.py
│       │   ├── linter.py
│       │   └── defaults.py
│       ├── pipeline/
│       │   ├── __init__.py
│       │   ├── markdown.py
│       │   ├── setup_exporter.py
│       │   └── setup_parser.py
│       ├── prompts/
│       │   ├── __init__.py
│       │   └── blueprint_prompt.py
│       ├── ui/
│       │   ├── __init__.py
│       │   └── streamlit_uiw.py
│       ├── map/
│       │   ├── __init__.py
│       │   └── manager.py
│       ├── status/
│       │   ├── __init__.py
│       │   └── manager.py
│       ├── validation/
│       │   ├── __init__.py
│       │   ├── linter.py
│       │   └── route_validator.py
│       └── cli.py
├── tests/
│   ├── fixtures/
│   │   ├── setup_minimal.yaml
│   │   └── setup_invalid.yaml
│   ├── test_ids.py
│   ├── test_setup_schema.py
│   ├── test_uiw_linter.py
│   ├── test_setup_exporter.py
│   └── test_setup_parser.py
├── examples/
│   └── summer_city_2026_setup_package.md
├── pyproject.toml
└── README.md
```

Phase 1 只需要建立與 Setup Package 相關的檔案。`map/`、`status/`、`validation/route_validator.py` 可在後續 Phase 再補。

---

## Phase 0：專案骨架與基礎工具

### 0-A 專案初始化

#### 要建的檔案

1. `pyproject.toml`
2. `README.md`
3. `src/sandbox_dating_sim/__init__.py`
4. `src/sandbox_dating_sim/core/__init__.py`
5. `src/sandbox_dating_sim/core/constants.py`
6. `src/sandbox_dating_sim/core/exceptions.py`
7. `src/sandbox_dating_sim/core/ids.py`
8. `tests/test_ids.py`

#### `core/constants.py`

```python
SETUP_PACKAGE_VERSION = "1.0"
UIW_VERSION = "1.2"
TARGET_GAME_SPEC_VERSION = "1.2"

TIME_SLOTS = ("morning", "afternoon", "evening")
DAY_TYPES = ("weekday", "weekend", "holiday", "specific_date", "any")
SCHEDULE_PRIORITIES = ("critical", "route", "normal", "ambient")
```

#### `core/exceptions.py`

```python
class SandboxDatingSimError(Exception): ...
class ValidationError(SandboxDatingSimError): ...
class SetupPackageError(SandboxDatingSimError): ...
class MarkdownParseError(SandboxDatingSimError): ...
```

#### `core/ids.py`

```python
import re

ID_PATTERN = re.compile(r"^[a-z][a-z0-9_]*$")

def is_valid_id(value: str) -> bool:
    """回傳 value 是否符合 canonical ID 格式。"""
    ...

def normalize_id(value: str) -> str:
    """
    將輸入轉為小寫底線格式。
    僅處理英文、數字、空格、連字號。
    中文不得自動音譯，應回報錯誤或要求使用者輸入英文 ID。
    """
    ...
```

#### 測試要求

```python
def test_valid_id():
    assert is_valid_id("sophie_route_started")

def test_invalid_id_starts_with_number():
    assert not is_valid_id("1sophie")

def test_invalid_id_contains_chinese():
    assert not is_valid_id("蘇菲")

def test_normalize_id():
    assert normalize_id("Summer City") == "summer_city"
```

---

## Phase 1：Setup Package MVP

Phase 1 是目前最優先的 MVP。

目標：

```text
輸入一份初始設定資料
-> 驗證資料
-> 產出 Setup Package MD
-> 再從 Setup Package MD parse 回資料模型
-> roundtrip 測試通過
```

此階段不需要 AI API、不需要 UI、不需要 Event Blueprint。

---

### 1-A Canonical Setup Schema

#### 要建的檔案

1. `src/sandbox_dating_sim/schema/__init__.py`
2. `src/sandbox_dating_sim/schema/validation.py`
3. `src/sandbox_dating_sim/schema/setup.py`
4. `tests/test_setup_schema.py`

#### `schema/validation.py`

```python
from pydantic import BaseModel

class Issue(BaseModel):
    severity: str  # "error" | "warning"
    type: str
    path: str
    message: str
    suggested_action: str | None = None

class ValidationReport(BaseModel):
    status: str  # "passed" | "failed"
    issues: list[Issue] = []

    @classmethod
    def from_issues(cls, issues: list[Issue]) -> "ValidationReport":
        ...
```

#### `schema/setup.py`

必須建立下列 Pydantic models：

```python
from pydantic import BaseModel, Field

class SemanticChoice(BaseModel):
    id: str
    label: str

class World(BaseModel):
    world_id: str
    title: str
    start_date: str
    end_date: str
    time_slots: list[str]
    total_days: int | None = None
    global_style: list[SemanticChoice] = []

class InitialStats(BaseModel):
    INT: int
    CHA: int
    STR: int
    MORAL: int
    Cash: int
    Debt: int = 0

class Protagonist(BaseModel):
    protagonist_id: str = "protagonist"
    name: str
    gender: str
    age: int
    occupation: SemanticChoice
    initial_stats: InitialStats
    personality: SemanticChoice
    secrets: list[SemanticChoice] = []

class Location(BaseModel):
    location_id: str
    name: str
    location_type: str  # container | sub_location | standalone
    parent_location_id: str | None = None
    is_visitable: bool
    base_cost: int = 0
    available_time_slots: list[str]
    tags: list[str] = []
    unlock_conditions: list[str] = []
    closed_conditions: list[str] = []
    map_priority: str = "normal"
    map_display_group: str | None = None
    default_npc_capacity: int = 0
    empty_behavior: str = "show_empty"
    ambient_text: str | None = None

class ScheduleEntry(BaseModel):
    schedule_id: str
    day_type: str
    time_slot: str
    location_id: str
    condition: list[str] = []
    priority: str = "normal"
    schedule_order: int

class AssetOption(BaseModel):
    id: str
    label: str

class Character(BaseModel):
    character_id: str
    display_name: str
    gender: str
    orientation: list[str]
    role: str
    identity: str
    personality_tags: list[SemanticChoice]
    secrets: list[SemanticChoice] = []
    initial_favor: int = 0
    schedule: list[ScheduleEntry] = []
    allowed_emotions: list[AssetOption]
    allowed_costumes: list[AssetOption]
    allowed_positions: list[AssetOption]

class FlagDef(BaseModel):
    flag_id: str
    type: str
    initial_value: bool | int | str
    description: str

class StatusDuration(BaseModel):
    type: str
    value: int | None = None

class StatusFlag(BaseModel):
    status_id: str
    label: str
    targets: list[str]
    effect: list[dict[str, Any]]
    duration: StatusDuration
    clear_rule: list[str]
    description: str
    permanent_reason: str | None = None

class Ending(BaseModel):
    ending_id: str
    title: str
    ending_type: str
    target_character_id: str | None = None
    description: str
    required_flags: list[str] = []
    required_stats: list[str] = []
    forbidden_flags: list[str] = []
    priority: str = "normal"
    route_tags: list[str] = []

class SetupPackage(BaseModel):
    setup_package_version: str = "1.0"
    uiw_version: str = "1.2"
    target_game_spec_version: str = "1.2"
    world: World
    protagonist: Protagonist
    locations: list[Location]
    characters: list[Character]
    flags: list[FlagDef] = []
    status_flags: list[StatusFlag] = []
    endings: list[Ending] = []
    asset_vocabularies: dict = Field(default_factory=dict)
    alias_tables: dict = Field(default_factory=dict)
    validation_report: ValidationReport | None = None
```

#### 測試要求

```python
def test_minimal_setup_package_loads():
    """fixture setup_minimal.yaml 可載入為 SetupPackage。"""

def test_character_requires_character_id():
    """角色缺 character_id 時 Pydantic validation fail。"""

def test_status_flag_requires_duration_and_clear_rule():
    """status flag 缺 duration 或 clear_rule 時 validation fail。"""

def test_schedule_requires_schedule_order:
    """schedule 缺 schedule_order 時 validation fail。"""
```

---

### 1-B UIW Linter

#### 要建的檔案

1. `src/sandbox_dating_sim/uiw/__init__.py`
2. `src/sandbox_dating_sim/uiw/linter.py`
3. `tests/test_uiw_linter.py`

#### `uiw/linter.py`

```python
from sandbox_dating_sim.schema.setup import SetupPackage
from sandbox_dating_sim.schema.validation import ValidationReport, Issue

class UIWLinter:
    """
    檢查 SetupPackage 是否符合 UIW v1.2 與遊戲規格 v1.2。
    """

    def validate(self, package: SetupPackage) -> ValidationReport:
        """執行所有檢查，回傳 ValidationReport。"""
        ...

    def _check_ids(self, package: SetupPackage) -> list[Issue]:
        """檢查 world_id、character_id、location_id、flag_id、ending_id 格式與重複。"""
        ...

    def _check_locations(self, package: SetupPackage) -> list[Issue]:
        """檢查 parent_location_id、is_visitable、available_time_slots。"""
        ...

    def _check_schedules(self, package: SetupPackage) -> list[Issue]:
        """檢查 schedule 引用地點、time_slot、schedule_order 與同優先權衝突。"""
        ...

    def _check_status_flags(self, package: SetupPackage) -> list[Issue]:
        """檢查 status flag 是否有 effect、duration、clear_rule。"""
        ...

    def _check_endings(self, package: SetupPackage) -> list[Issue]:
        """檢查 ending 是否引用存在角色與已宣告 flag。"""
        ...
```

#### 必做檢查

- `world_id` 合法。
- `end_date` 晚於 `start_date`。
- 角色 ID 不重複。
- 地點 ID 不重複。
- 子地點的 `parent_location_id` 存在。
- `is_visitable=true` 的地點至少有一個 time slot。
- schedule 引用存在地點。
- schedule 引用合法 time slot。
- schedule 必須有 `schedule_order`。
- 同角色、同 `day_type`、同 `time_slot`、同 `priority` 的 schedule 若條件可能同時成立，至少要有不同 `schedule_order`。
- status flag 必須有 `effect`、`duration`、`clear_rule`。
- 非永久 status flag 必須有可達成的解除條件。
- ending 引用存在角色或 `global`。
- ending 條件引用已宣告 flag。
- character 必須有 `allowed_emotions`、`allowed_costumes`、`allowed_positions`。

#### 測試要求

```python
def test_linter_passes_minimal_fixture():
    """setup_minimal.yaml 應通過。"""

def test_linter_rejects_duplicate_character_id():
    """重複 character_id 應產生 error。"""

def test_linter_rejects_unknown_schedule_location():
    """schedule location_id 不存在應產生 error。"""

def test_linter_rejects_status_without_clear_rule():
    """status flag 缺 clear_rule 應產生 error。"""

def test_linter_warns_schedule_tie_without_order():
    """同優先權 schedule 無法穩定排序時應產生 warning 或 error。"""
```

---

### 1-C Setup Package MD Exporter

#### 要建的檔案

1. `src/sandbox_dating_sim/pipeline/__init__.py`
2. `src/sandbox_dating_sim/pipeline/markdown.py`
3. `src/sandbox_dating_sim/pipeline/setup_exporter.py`
4. `tests/test_setup_exporter.py`

#### `pipeline/markdown.py`

```python
def wrap_yaml_markdown(title: str, yaml_text: str) -> str:
    """
    將 YAML 包成 Markdown。
    輸出必須只有一個主要 ```yaml code block。
    """
    ...

def extract_first_yaml_block(markdown_text: str) -> str:
    """
    從 Markdown 擷取第一個 ```yaml code block。
    找不到時拋出 MarkdownParseError。
    """
    ...
```

#### `pipeline/setup_exporter.py`

```python
from pathlib import Path
from sandbox_dating_sim.schema.setup import SetupPackage
from sandbox_dating_sim.uiw.linter import UIWLinter

class SetupPackageExporter:
    """
    將 SetupPackage 匯出成 Setup Package MD。
    """

    def __init__(self, linter: UIWLinter | None = None):
        ...

    def to_markdown(self, package: SetupPackage) -> str:
        """
        1. 執行 UIW Linter
        2. 將 validation_report 寫回 package
        3. serialize 成 YAML
        4. 包成 Markdown
        """
        ...

    def write_file(self, package: SetupPackage, output_dir: Path) -> Path:
        """
        輸出檔名：<world_id>_setup_package.md
        回傳寫入路徑。
        """
        ...
```

#### 輸出格式

```md
# Setup Package: 夏日債務街

```yaml
setup_package_version: "1.0"
uiw_version: "1.2"
target_game_spec_version: "1.2"
world:
  world_id: summer_city_2026
...
validation_report:
  status: passed
  issues: []
```
```

#### 測試要求

```python
def test_exporter_outputs_markdown_with_yaml_block():
    """輸出包含 # Setup Package 與單一 yaml code block。"""

def test_exporter_includes_validation_report():
    """輸出 YAML 包含 validation_report。"""

def test_write_file_uses_world_id_filename(tmp_path):
    """輸出檔名為 <world_id>_setup_package.md。"""
```

---

### 1-D Setup Package Parser 與 Roundtrip

#### 要建的檔案

1. `src/sandbox_dating_sim/pipeline/setup_parser.py`
2. `tests/test_setup_parser.py`

#### `pipeline/setup_parser.py`

```python
from pathlib import Path
from sandbox_dating_sim.schema.setup import SetupPackage

class SetupPackageParser:
    """
    從 Setup Package MD 讀回 SetupPackage。
    """

    def parse_markdown(self, markdown_text: str) -> SetupPackage:
        """擷取 YAML block 並轉為 SetupPackage。"""
        ...

    def parse_file(self, path: Path) -> SetupPackage:
        """讀取檔案並 parse。"""
        ...
```

#### 測試要求

```python
def test_parse_exported_markdown_roundtrip():
    """SetupPackage -> Markdown -> SetupPackage 後核心欄位一致。"""

def test_parse_rejects_missing_yaml_block():
    """沒有 yaml code block 時拋出 MarkdownParseError。"""

def test_parse_rejects_invalid_schema():
    """YAML 欄位不完整時 validation fail。"""
```

---

### 1-E CLI：產出 Setup Package MD

#### 要建的檔案

1. `src/sandbox_dating_sim/cli.py`
2. `tests/test_cli.py`，可選

#### CLI 指令

```text
python -m sandbox_dating_sim.cli export-setup tests/fixtures/setup_minimal.yaml --out examples/
```

#### `cli.py`

```python
from pathlib import Path
import typer

app = typer.Typer()

@app.command("export-setup")
def export_setup(input_yaml: Path, out: Path = Path("examples")) -> None:
    """
    讀取 YAML 初始設定，驗證後輸出 Setup Package MD。
    """
    ...

if __name__ == "__main__":
    app()
```

#### 驗收標準

- 可從 fixture YAML 產出 `examples/<world_id>_setup_package.md`。
- MD 中含單一主要 YAML 區塊。
- YAML 中含 `validation_report.status`。
- Parser 能讀回該 MD。
- `pytest` 全綠。

---

### 1-F Interactive UIW Prototype：表單填寫並輸出 Setup Package MD

1-F 完成後，一般使用者應可透過表單填寫或點選參考選項，最後產出 Setup Package MD。

此階段仍不是完整 Dashboard，只做 User Input Wizard 原型。建議使用 Streamlit，因為能快速建立表單、下拉選單、多選 chip、資料預覽與下載/輸出按鈕。

1-F 的交付標準是 Functional Prototype：功能完整但介面可以樸素。實作者應優先確保資料契約正確、能驗證、能輸出與能 parse 回來，不需要在此階段追求正式產品級 UI。

#### 要建的檔案

1. `src/sandbox_dating_sim/ui/__init__.py`
2. `src/sandbox_dating_sim/ui/streamlit_uiw.py`
3. `src/sandbox_dating_sim/uiw/defaults.py`
4. `tests/test_uiw_defaults.py`

#### `uiw/defaults.py`

```python
STYLE_PRESETS = [
    {"id": "urban_romance", "label": "都市戀愛"},
    {"id": "school_life", "label": "校園生活"},
    {"id": "black_humor", "label": "黑色幽默"},
    {"id": "class_anxiety", "label": "階級焦慮"},
    {"id": "coming_of_age", "label": "青春成長"},
    {"id": "melancholy", "label": "淡淡憂鬱"},
    {"id": "comedy", "label": "喜劇"},
    {"id": "mystery", "label": "懸疑"},
]

LOCATION_TEMPLATES = [
    {
        "template_id": "school_container",
        "label": "學校",
        "location_type": "container",
        "suggested_sub_locations": ["classroom", "library", "rooftop", "sports_ground"],
    },
    {
        "template_id": "shopping_street_container",
        "label": "商店街",
        "location_type": "container",
        "suggested_sub_locations": ["cafe", "convenience_store", "arcade"],
    },
    {
        "template_id": "home_standalone",
        "label": "主角家",
        "location_type": "standalone",
        "suggested_sub_locations": [],
    },
]
```

#### `ui/streamlit_uiw.py`

```python
import streamlit as st

def main() -> None:
    """
    Interactive User Input Wizard prototype.

    頁面流程：
    1. World Meta
    2. Protagonist
    3. Locations & Map
    4. Characters & Schedule
    5. Flags / Status / Endings
    6. Review & Export
    """
    ...

if __name__ == "__main__":
    main()
```

#### UI 必做功能

- 使用者可建立 World、Protagonist、Location、Character、Flag、Status Flag、Ending。
- 使用者可手動輸入，也可點選參考選項帶入資料。
- 風格關鍵字欄位旁必須顯示參考詞，可點選加入。
- 地點建立必須提供模板：
  - 主地點 / 區域，例如學校、商店街、車站周邊。
  - 獨立地點，例如主角家、電影院、便利商店。
  - 子地點，例如教室、圖書室、咖啡廳、屋頂。
- 若建立 `container` 主地點，UI 必須引導使用者至少建立一個 `is_visitable=true` 的子地點。
- UI 應以「左側地點樹 + 右側編輯面板」或等效方式呈現地點層級。
- 子地點建立時，`parent_location_id` 應由目前選取的主地點自動帶入。
- 角色表情、服裝、位置使用多選白名單。
- Schedule 編輯時只能選擇已存在且可進入的地點。
- 按「驗證」後顯示 UIW Linter 結果。
- 按「匯出」後產出 `<world_id>_setup_package.md`。

#### 1-F 範圍限制

1-F 必須做到：

- 提供可雙擊啟動 Streamlit UIW 的 `啟動_UIW.bat`，讓使用者不必手動輸入啟動指令。
- 使用 Streamlit tabs、forms、expanders 或等效樸素元件完成輸入。
- 能填寫 World。
- 能填寫 Protagonist。
- 能新增 Location。
- 能新增 Character。
- 能選 style presets。
- 能套用 location templates。
- 能新增基本 schedule。
- 能新增 basic flag、status flag、ending。
- 能按 Validate 顯示 validation report。
- 能預覽 YAML。
- 能在 Streamlit 內按鈕匯出 Setup Package MD，並在 UI 內清楚顯示驗證狀態、成功/失敗訊息與輸出檔案位置。
- 匯出的 MD 能被 Phase 1-D Parser 讀回。

1-F 暫時不要求：

- 精美正式 Wizard UI。
- 拖拉式地圖樹。
- 完整 undo / redo。
- 複雜動態巢狀編輯體驗。
- 視覺化 Flowchart。
- Dashboard。
- 多使用者管理。
- AI API 串接。

#### 地點 UI 編排要求

地點頁面建議拆成三區：

```text
左側：地圖樹
  - 學校
    - 2年A班
    - 圖書室
    - 操場
  - 主角家
  - 商店街
    - 咖啡廳
    - 便利商店

中間：模板與新增按鈕
  - 新增主地點
  - 新增子地點
  - 新增獨立地點
  - 套用地點模板

右側：目前選取地點的欄位編輯器
```

規則：

- `container` 不可直接進入，`is_visitable=false`。
- `container` 至少需要一個子地點，否則 UIW Linter 必須報錯。
- `sub_location` 必須有 `parent_location_id`。
- `standalone` 不可有 `parent_location_id`。
- 玩家實際可移動目標只應是 `sub_location` 或 `standalone`。

Phase 1-F 可先使用簡化版地點介面：

```text
地點清單表格
+ 新增地點表單
+ parent_location_id 下拉選單
+ 套用模板按鈕
+ Validate 時檢查 container 是否有 sub_location
```

完整地圖樹可以留到 Phase 5 Dashboard。

#### 手動驗收清單

- [ ] 可啟動 `streamlit run src/sandbox_dating_sim/ui/streamlit_uiw.py`。
- [ ] 可點選風格參考詞並加入 `world.global_style`。
- [ ] 可套用「學校」模板並自動建立建議子地點草稿。
- [ ] 建立主地點但未建立子地點時，驗證會顯示錯誤。
- [ ] 建立角色時可選表情、服裝、位置白名單。
- [ ] Schedule 下拉選單只出現可進入地點。
- [ ] Review 頁能顯示 SetupPackage YAML 預覽。
- [ ] Export 能產出 Setup Package MD。
- [ ] 匯出的 MD 可被 Phase 1-D Parser 讀回。

---

### 1-G 使用者實測回饋迭代

1-G 是 Phase 1-F Functional Prototype 交付後的實際使用者操作回饋階段。

此階段可能包含多個子階段，例如 `1-G-1`、`1-G-2`。每個子階段都應以「使用者實測後提出的具體需求」為單位，優先修正 UI 操作阻力、表單理解成本、導覽順序、預設選項與資料輸入模式。

1-G 的原則：

- 不改變 Setup Package canonical schema，除非需求明確指出資料契約需要升級。Phase 1-G-3 屬於明確資料契約升級。
- 優先改善 Streamlit UIW 的使用流程與顯示文字。
- 使用者介面可顯示中文；純系統 ID 仍使用英文，語意型欄位需保存英文 `id` 與中文 `label`。
- 新增預設選項時，應放在 `src/sandbox_dating_sim/uiw/defaults.py`。
- 新增或變更預設選項時，應補 `tests/test_uiw_defaults.py`。
- 若只是 UI 導覽與文案調整，不需要改 exporter、parser 或 schema。

#### 1-G-1 使用者操作回饋：導覽、主角預設值與債務等級

1-G-1 需求來源為使用者實際操作 UIW 後的第一批回饋。

##### 分頁順序

上方設定項目的順序必須改為：

```text
World
-> Protagonist
-> Characters
-> Locations
-> Flags/Status/Endings
-> Review & Export
```

##### 分頁顯示文字

上方設定項目必須以「中文名稱(英文名稱)」顯示：

```text
世界觀與曆法(World)
主角設定(Protagonist)
角色設定(Characters)
地點與地圖(Locations)
旗標/狀態/結局(Flags/Status/Endings)
預覽與匯出(Review & Export)
```

##### 下一頁導覽

每一個設定頁面最下面都必須新增「下一頁」按鈕，點擊後移動到下一個設定項目。

例外：

- `Review & Export` 是最後一頁，不需要「下一頁」按鈕。

若 Streamlit 原生 `st.tabs` 無法程式化切換，實作可改用 `st.radio(horizontal=True)`、segmented control 或等效導覽元件，只要使用者看到的是上方水平導覽即可。

##### World 頁面

`世界觀與曆法` 頁面中，最下面的「已選風格：」後方已選項目必須顯示中文 label，不顯示英文 canonical ID。

Phase 1-G-1 原本只要求 UI 顯示中文 label、資料保存英文 ID；Phase 1-G-3 起，`world.global_style` 改為保存 `id + label`：

```yaml
global_style:
  - id: urban_romance
    label: 都市戀愛
  - id: black_humor
    label: 黑色幽默
```

UI 顯示：

```text
已選風格：都市戀愛、黑色幽默
```

##### Protagonist 頁面

`主角設定` 的以下欄位必須提供 6-12 個可點選預設選項：

- 職業
- 性格描述
- 秘密

點選預設選項後，應寫入對應欄位。使用者仍可手動輸入或修改。

預設選項必須放在 `src/sandbox_dating_sim/uiw/defaults.py`：

```python
PROTAGONIST_OCCUPATION_PRESETS = [...]
PROTAGONIST_PERSONALITY_PRESETS = [...]
PROTAGONIST_SECRET_PRESETS = [...]
```

每個選項至少包含：

```python
{"id": "student", "label": "學生"}
```

##### 初始債務設定

1-G-1 的 Interactive UIW 中，初始債務設定使用等級模式，不再讓一般使用者直接輸入任意債務金額。

UI 顯示債務等級，內部映射為 canonical `protagonist.initial_stats.Debt`。

建議等級：

```python
DEBT_TIER_PRESETS = [
    {"id": "none", "label": "無債務", "debt": 0},
    {"id": "low", "label": "低債務", "debt": 10000},
    {"id": "medium", "label": "中債務", "debt": 50000},
    {"id": "high", "label": "高債務", "debt": 120000},
    {"id": "desperate", "label": "絕望級債務", "debt": 300000},
]
```

輸出的 Setup Package canonical data 只需保存最終 `Debt` 數值，不必把 `debt_tier` 寫入 `SetupPackage` schema。

##### 測試要求

`tests/test_uiw_defaults.py` 必須補充：

- 主角職業、性格、秘密預設選項皆為 6-12 個。Phase 1-G-3 起，主角職業與秘密預設選項可擴充為 16 個，並需補對應測試。
- 主角預設選項 ID 不重複，且符合 canonical ID 規則。
- 債務等級預設值包含 `id`、`label`、`debt`。
- 債務等級 `debt` 必須為非負整數。

#### 1-G-2 使用者操作回饋：欄位簡化、中文選項與自訂 ID 說明

1-G-2 需求來源為使用者實際操作 UIW 後的第二批回饋。此階段仍屬 UIW 操作體驗調整，不實作遊戲邏輯、不改 exporter/parser/schema，除非後續另有明確資料契約升級需求。

##### 主角 ID 固定隱藏

主角 ID 在遊戲設計上應固定為 `protagonist`。

UIW 不需要顯示「主角 ID」輸入欄位，避免一般使用者誤以為此欄位需要設計。

實作要求：

- `protagonist.protagonist_id` 預設固定為 `protagonist`。
- UI 不顯示主角 ID 欄位。
- Setup Package 輸出仍保留 `protagonist_id: protagonist`。

##### 主角預設年齡

主角年齡的 UI 預設值改為 `29`。

此變更只影響 UI 初始值；使用者仍可手動調整年齡。

##### 中文化選項顯示

主角與角色相關的 enum / option 選單，在 UI 中應顯示中文選項，不直接顯示英文 ID。

適用欄位：

- 主角性別 `protagonist.gender`
- 角色性別 `characters[].gender`
- 角色性取向 `characters[].orientation`
- 角色定位 `characters[].role`

資料保存仍使用英文 canonical ID。

範例：

```python
GENDER_OPTIONS = [
    {"id": "male", "label": "男性"},
    {"id": "female", "label": "女性"},
    {"id": "non_binary", "label": "非二元"},
]
```

UI 顯示：

```text
男性
女性
非二元
```

輸出資料：

```yaml
gender: male
```

##### 角色性格標籤預設選項

角色的 `personality_tags` 應和主角職業、性格、秘密一樣，提供可點選預設選項。

預設選項應放在 `src/sandbox_dating_sim/uiw/defaults.py`：

```python
CHARACTER_PERSONALITY_TAG_PRESETS = [
    {"id": "guarded", "label": "戒心重"},
    {"id": "proud", "label": "自尊心強"},
    {"id": "secretly_kind", "label": "其實很溫柔"},
]
```

UI 顯示中文 label。Phase 1-G-3 起，寫入 `characters[].personality_tags` 時保存 `id + label` list。

使用者仍可新增自訂性格標籤。Phase 1-G-3 起，使用者輸入中文 label，由工具產生 canonical ID。

##### 自訂職業與性格描述的輸入說明

凡是 UI 欄位名稱包含 `ID`，或其值會作為 canonical tag / canonical ID 寫入 Setup Package 時，自訂輸入必須使用英文 canonical ID。

Phase 1-G-3 起，職業、主角性格、主角秘密、NPC 秘密、世界風格與角色性格標籤等語意型欄位，不應要求一般使用者直接輸入英文 ID。UI 應提供中文自訂輸入框，按下「選擇」後由工具產生系統 ID，並保留可進階手動修改的系統 ID 欄位。

自訂 canonical ID 規則：

- 小寫英文、數字、底線。
- 必須以英文字母開頭。
- 不可使用中文。
- 不可使用空格。

UI 必須在相關欄位旁顯示說明文字，避免使用者以中文輸入造成驗證失敗。

建議欄位文字：

```text
自訂職業 ID（英文，小寫英文/數字/底線）
自訂性格 ID（英文，小寫英文/數字/底線）
自訂性格標籤 ID（英文，小寫英文/數字/底線）
```

補充規則：

- 顯示名稱、角色姓名、地點名稱、故事描述、秘密描述等純顯示或敘事文字欄位可以輸入中文。
- 純 canonical ID / tag 欄位不可輸入中文。
- 語意型欄位可輸入中文 label，但內部必須保存 `id + label`。

##### 測試要求

`tests/test_uiw_defaults.py` 必須補充：

- 主角/角色 enum 選項都有 `id` 與 `label`。
- enum 選項 ID 符合 canonical ID 規則。
- 角色性格標籤預設選項 ID 不重複。
- 角色性格標籤預設選項 ID 符合 canonical ID 規則。
- 主角預設年齡若被抽成常數，應驗證其值為 `29`。

#### 1-G-3 使用者操作回饋：語意型欄位保存 id + label

1-G-3 需求來源為使用者實際操作 UIW 後對自訂中文輸入的回饋。此階段要解決「使用者以中文理解、AI 需要中文語意、系統需要英文 ID」的落差。

##### 資料契約升級

下列欄位從純英文 ID 字串改為 `SemanticChoice` 或 `list[SemanticChoice]`：

```python
class SemanticChoice(BaseModel):
    id: str
    label: str
```

適用欄位：

- `world.global_style: list[SemanticChoice]`
- `protagonist.occupation: SemanticChoice`
- `protagonist.personality: SemanticChoice`
- `protagonist.secrets: list[SemanticChoice]`
- `characters[].personality_tags: list[SemanticChoice]`
- `characters[].secrets: list[SemanticChoice]`

純系統 ID 維持字串，例如 `world_id`、`character_id`、`location_id`、`flag_id`、`status_id`、`ending_id`、`schedule_id`、`parent_location_id`、`target_character_id`。

##### UI 模式

上述語意型欄位應採用一致輸入模式：

- 上方提供預設中文按鈕；點選後帶入對應 `id + label`。
- 中間提供自訂中文輸入框與「選擇」按鈕。
- 下方提供系統 ID 欄位；由工具自動填入，可進階手動修改。
- 結果區需清楚顯示目前選擇，例如「目前選擇：咖啡廳店員」與「系統 ID：cafe_staff」。

自訂中文輸入不是 AI 翻譯。工具應以 deterministic 方法產生穩定系統 ID：預設選項使用手寫 ID；自訂中文可先查內建對照表，找不到時使用拼音 ID 或 hash ID。實作時必須明確選定 fallback 策略並測試。

##### 職業預設選項擴充

保留既有 8 個主角職業預設選項，新增 8 個中到高社會地位選項，使總數為 16 個：

```python
{"id": "rich_heir", "label": "富二代"}
{"id": "startup_founder", "label": "新創公司老闆"}
{"id": "tech_worker", "label": "科技業"}
{"id": "finance_professional", "label": "金融業"}
{"id": "doctor", "label": "醫師"}
{"id": "lawyer", "label": "律師"}
{"id": "university_lecturer", "label": "大學講師"}
{"id": "graphic_designer", "label": "平面設計師"}
```

##### 主角與 NPC 秘密複選

`protagonist.secret` 改為 `protagonist.secrets`。
`characters[].secret` 改為 `characters[].secrets`。

- 每個主角或 NPC 最多 3 個。
- 可選預設項目。
- 可新增自訂中文秘密。
- 每個秘密保存 `id + label`。
- `none = 沒有秘密` 僅為 UI 操作，用來清空秘密清單。
- canonical data 以 `secrets: []` 表示沒有秘密，不保存 `{id: none, label: 沒有秘密}`。

`secrets: []` 對 AI 的語意是「使用者未指定秘密」。後續生成事件或角色伏筆時，不得把 `none` 當成可使用的秘密資料；若需要邏輯條件，應建立獨立 flag。

新增 8 個秘密預設選項，供主角與 NPC 共用：

```python
{"id": "hidden_wealth", "label": "其實家境富裕"}
{"id": "criminal_record", "label": "曾有犯罪紀錄"}
{"id": "secret_childhood_friend", "label": "隱瞞童年舊識"}
{"id": "family_scandal", "label": "家族醜聞"}
{"id": "forbidden_relationship", "label": "曾有禁忌戀情"}
{"id": "fake_education", "label": "學歷造假"}
{"id": "underground_job", "label": "從事地下工作"}
{"id": "terminal_illness_in_family", "label": "家人身患重病"}
```

##### Linter 與測試要求

實作 1-G-3 時應更新：

- `src/sandbox_dating_sim/schema/setup.py`
- `src/sandbox_dating_sim/uiw/defaults.py`
- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- `tests/fixtures/setup_minimal.yaml`
- `tests/test_setup_schema.py`
- `tests/test_uiw_linter.py`
- `tests/test_uiw_defaults.py`
- `tests/test_setup_exporter.py`
- `tests/test_setup_parser.py`
- `tests/test_cli.py`

必測：

- 語意型欄位可載入為 `SetupPackage`。
- exporter 輸出 `id + label`。
- parser roundtrip 不遺失中文 label。
- Streamlit UI 可用中文預設與自訂輸入，並輸出合法 canonical data。
- 語意型欄位的 `id` 符合 canonical ID，`label` 不可為空。
- `protagonist.secrets` 最多 3 個。
- `characters[].secrets` 最多 3 個。
- 「沒有秘密」在 UI 中清空 secrets list，輸出 `secrets: []`。
- `world.global_style[].id` 與 `characters[].personality_tags[].id` 不可重複。

##### 完成紀錄

Phase 1-G-3 已完成並驗證：

- schema、fixture、exporter、parser、CLI 與 linter 已支援語意型欄位 `id + label`。
- 主角與 NPC 秘密已改為最多 3 個複選。
- UI 可用中文預設與中文自訂輸入產生合法 canonical ID。
- 「沒有秘密」已定義為清空 secrets list，輸出 `secrets: []`。
- Streamlit 角色頁的語意型按鈕已移出 form，避免 `st.button()` / `st.form()` 衝突。
- 自動測試 regression 已通過；人工驗收抽查已確認職業、自訂職業、秘密上限、沒有秘密、角色性格標籤與 NPC 秘密寫入行為。

#### 1-G-4 使用者操作回饋：地點與地圖項目調整

1-G-4 需求來源為使用者實際操作 UIW 後，發現「角色設定」會引用地點，因此地點資料應先於角色資料完成。此階段不改 canonical schema，重點是 Streamlit UIW 的地點建立順序、地點模板、刪除防呆與中文顯示。

##### 分頁順序

上方設定項目與「下一頁」導覽順序必須同步改為：

```text
世界觀與曆法(World)
-> 主角設定(Protagonist)
-> 地點與地圖(Locations)
-> 角色設定(Characters)
-> 旗標/狀態/結局(Flags/Status/Endings)
-> 預覽與匯出(Review & Export)
```

##### 地點模板

原本既有模板必須保留；若文件已有但實作尚未提供，應補齊。

保留與補齊：

- `學校`: `container`，保留既有子地點。
- `商店街`: `container`，保留既有子地點。
- `主角家`: `standalone`。
- `電影院`: `standalone`。
- `便利商店`: `standalone`；若 UI 會同時呈現商店街子地點的便利商店，獨立模板顯示文字應可區分，例如 `便利商店(獨立)`。
- `車站周邊`: `container`，子地點為 `月台`、`站前廣場`、`地下街`。
- 其他實作中已存在的 standalone 模板也必須保留，不得因 1-G-4 擴充模板而刪除。

新增模板：

- `主角公司`: `standalone`
- `公園`: `standalone`
- `遊樂園`: `container`，子地點為 `摩天輪`、`雲霄飛車`、`鬼屋`、`紀念品店`
- `百貨公司`: `container`，子地點為 `美食街`、`精品樓層`、`空中花園`
- `醫院`: `standalone`
- `海邊`: `container`，子地點為 `沙灘`、`海之家`、`觀景台`
- `溫泉旅館`: `container`，子地點為 `大廳`、`客房`、`露天溫泉`
- `夜市`: `standalone`
- `健身房`: `standalone`
- `圖書館`: `standalone`
- `警察局`: `standalone`
- `美術館`: `standalone`
- `河岸步道`: `standalone`
- `酒吧`: `standalone`

每個 `sub_location` 與 `standalone` 模板應提供合理的 `available_time_slots`；所有模板都應提供 `tags`。`tags` 可使用中文或英文，目的在於提供 AI 生成劇情時的地點氣氛、用途、社交屬性、危險程度、約會適合度等參考，不視為 canonical ID。

子地點實際 `location_id` 必須由父地點 ID 與子地點 suffix 組成：

```text
<parent_location_id>_<location_id_suffix>
```

例如商店街模板中的便利商店子地點應產生 `shopping_street_convenience_store`，不可直接產生 `convenience_store`。如此可避免子地點與獨立地點模板 ID 衝突。

若子地點與獨立地點名稱相同，UI 顯示時應提供脈絡或副標，例如 `便利商店(商店街內)` 與 `便利商店(獨立)`；canonical `location_id` 仍依上述規則保存英文 ID。

##### 套用模板防呆

套用模板前必須檢查即將建立的 `location_id` 是否已存在。

- 若主地點或獨立地點 ID 已存在，不建立任何地點，並提示使用者。
- 若 container 的任一預計子地點 ID 已存在，不建立任何地點，並提示使用者。
- 此階段優先採取「阻止並提示」策略，不自動產生 `school_2` 之類的新 ID，避免使用者不知道自己建立了重複地點。

##### 地點清單與刪除

地點清單中每個項目後方應直接顯示刪除按鈕，使用者不必展開 JSON 才能刪除。

原本展開區內的刪除按鈕可移除。

刪除時必須檢查引用：

- 若刪除的是 container，且仍有任何 `parent_location_id` 指向該地點，阻止刪除並提示有哪些子地點引用。
- 若任何角色 schedule 引用該地點，阻止刪除並提示引用來源，例如角色 ID、schedule ID 或 time slot。
- 若沒有引用，才可刪除。

##### 中文顯示與 canonical data

UI 中下列選項應顯示中文與英文 ID：

```text
主地點/區域(container)
子地點(sub_location)
獨立地點(standalone)
早上(morning)
下午(afternoon)
晚上(evening)
```

輸出資料仍保存原本英文 canonical ID，例如：

```yaml
location_type: container
available_time_slots:
  - morning
  - afternoon
```

##### 進階設定：地點標籤

原本的「標籤 (逗號分隔)」應移入「進階設定」。

UI 說明文字應明確表示：

- 可輸入中文或英文描述。
- 這些標籤只作為 AI 生成劇情時的參考，不是系統 ID。
- 可填寫地點氣氛、用途、社交屬性、危險程度、約會適合度等。

##### 測試要求

實作 1-G-4 時應更新：

- `src/sandbox_dating_sim/uiw/defaults.py`
- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- `tests/test_uiw_defaults.py`
- 必要時補 `tests/test_uiw_linter.py`

必測：

- 地點與角色分頁順序正確，下一頁順序同步正確。
- 新增模板 ID 不重複，且符合 canonical ID 規則。
- container 模板會建立指定子地點。
- 套用模板遇到既有 ID 時不建立重複地點。
- 刪除有子地點引用的 container 會被阻止。
- 刪除被角色 schedule 引用的地點會被阻止。
- `location_type` 與 `available_time_slots` UI 顯示中文(英文)，canonical data 仍保存英文 ID。
- 地點 tags 可保存中文或英文描述，且 UI 說明其用途為 AI 劇情生成參考。

#### 1-G-5 使用者操作回饋：角色設定頁面 UX 完整化

1-G-5 需求來源為使用者實際操作 UIW 角色設定頁後的回饋。痛點集中在：

- 角色清單只用 `st.json(ch)` 呈現原始 JSON，一般使用者難以掃讀。
- 沒有 inline 編輯，要修改角色任何欄位都必須整個刪除重建。
- 行程設定欄位仍是英文 enum（`weekday` / `morning` / `normal`），與 1-G-2、1-G-4 已建立的中文化原則不一致。
- 行程加入後沒有清單，也不能單獨刪除，只能整角色刪除。
- 表情、服裝、位置白名單顯示為 `"label (id)"` 帶英文 ID 雜訊；預設值寫死前 N 個。
- 刪除角色未做引用防呆，會在 endings、status_flags 留下 dangling reference。

此階段對齊 1-G-1 / 1-G-2 / 1-G-4 慣例，**不改 canonical schema**，純改 Streamlit UIW 角色頁 UX。

##### 範圍排除

下列項目刻意排除於 1-G-5，以保持 phase 範圍收斂：

- **新增流程自動產生唯一 canonical ID（B 群組）**：地點、NPC 角色、旗標、狀態與結局目前仍要求使用者輸入英文 ID。Phase 1-G-7 將比照 1-G-3「中文 label → 工具產 canonical ID」原則，把這些新增流程改為自動產生唯一系統 ID。
- **`day_type = specific_date` schema 缺日期欄位（D2）**：目前 `ScheduleEntry` 沒有 `specific_date` 欄位，選了 `specific_date` 也沒地方填日期。1-G-5 處理方式為「在 UI 行程 `day_type` 選項中暫時不顯示 `specific_date`」，避免使用者選了等於壞掉的 enum；schema 升級獨立成後續 phase（待確認編號），追蹤於 `已知問題.md`。
- **既有行程 inline 編輯**：1-G-5 行程修改 = 刪除 + 重新新增。完整 inline 編輯留待後續 phase。

##### 角色清單顯示與刪除

角色清單每個項目以 `st.expander` 呈現，expander label 對齊 1-G-4 地點頁顯示原則：

```text
{display_name} · {性別中文} · {定位中文}
```

- 性別中文：對齊 1-G-2 已建立的 `GENDER_OPTIONS` label（男 / 女 / 非二元）。
- 定位中文：對齊 1-G-2 已建立的 `ROLE_OPTIONS` label（主要攻略對象 / 關鍵配角）。
- 不再於 expander label 顯示 `(character_id)` 英文 ID。系統 ID 改在編輯 expander 展開後的「進階設定」區作為唯讀欄位顯示，方便除錯，但不直接出現在角色掃讀路徑上。

每個角色右側必須直接顯示「刪除」按鈕，不需展開 expander 才能刪除。版面比照 1-G-4 地點頁的 `st.columns([5, 1])` 排版。

刪除角色時必須檢查引用：

- 若任何 `endings[].target_character_id == 角色 character_id`，阻止刪除，顯示 toast：

  ```text
  無法刪除：被結局 <ending_id> 引用
  ```

- 若任何 `status_flags[].targets` 包含角色 `character_id`，阻止刪除，顯示 toast：

  ```text
  無法刪除：被狀態旗標 <status_id> 引用
  ```

  比對時必須排除 `protagonist`、`global` 與空字串等特殊 target 值，只在 targets 命中真正的 NPC `character_id` 時阻擋。匯入舊格式 `status_flags[].target` 時，1-G-8 parser 相容層需先正規化為 `targets`。

- 多筆引用同時存在時，toast 訊息應列出所有引用來源，例如「無法刪除：被結局 sophie_good_end、狀態旗標 sophie_route_lock 引用」。
- 顯示方式採 `st.toast(..., icon="🚨")`，對齊 1-G-4 已建立的 toast pattern，避免錯誤訊息打亂角色清單排版。

##### 既有角色編輯（Inline Expander）

採 inline expander 編輯範式：每個角色 expander 展開後即為完整編輯表單，多角色可同時展開、互不干擾。Expander 內版面分四區：

```text
[基本資訊]
  顯示名稱 / 性別 / 性取向(多選) / 定位 / 身分描述 / 初始好感度

[性格與秘密]   (form 外 widget, keyed temp state)
  性格標籤 (allow_multiple, 沿用 _semantic_choice_input)
  角色秘密 (最多 3 個, separate_none, 沿用 _semantic_choice_input)

[角色行程]
  既有行程清單（每筆右側可刪除）
  新增行程表單

[進階設定 (Advanced Settings)]   (st.expander 內展開區)
  💡 受控素材說明
  表情白名單 / 服裝白名單 / 位置白名單
  系統 ID（character_id，唯讀）

[儲存修改]   [取消]
```

性格標籤、角色秘密因為使用 `st.button()` 觸發語意型輸入，無法置於 `st.form()` 內。每個角色獨立的暫存 state 採用 keyed pattern：

```text
temp_ch_tags_<character_id>
temp_ch_secrets_<character_id>
```

行為規則：

- 第一次 open expander 時，若該 key 不存在於 `st.session_state`，使用該角色當前 `personality_tags` / `secrets` 初始化。
- 使用者編輯期間僅修改 keyed temp state，不立即寫回 `st.session_state["characters"][i]`。
- 按下「儲存修改」時將 keyed temp state 連同 form 內基本資訊與白名單一併寫回該角色。
- 按下「取消」時清空 keyed temp state；下次開啟 expander 時重新依該角色當前值初始化。
- 多角色同時編輯時，因 key 帶 `character_id`，state 不會跨角色混用。

新增角色仍維持頁面下方獨立「新增角色」表單，沿用既有 `temp_ch_tags`、`temp_ch_secrets`（無後綴 key），避免與既有角色編輯流程混淆。

`character_id` 在編輯 expander 中以唯讀文字顯示於進階設定區，1-G-5 不開放修改 NPC ID。Phase 1-G-7 僅處理「新增角色時自動產生 ID」，既有角色 ID rename 與引用搬移另行處理。

##### 行程清單顯示與刪除

每個角色 expander 內「角色行程」區塊應列出該角色既有 `schedule`，每筆行程顯示格式：

```text
{day_type 中文} {time_slot 中文} @ {地點中文 name} [{priority 中文}] order={schedule_order}
```

範例：

```text
平日 早上 @ 蘇菲家 [一般] order=20
週末 下午 @ 中央公園 [路線] order=10
```

- 地點中文 name 由 `location_id` 反查 `st.session_state["locations"]` 對應 `name` 欄位。若該 location 已被刪除（dangling reference），顯示原 `location_id` 並加註「(地點已刪除)」字樣以利使用者察覺。
- 每筆行程右側有「刪除」按鈕，按下後直接從該角色 `schedule` 移除該筆。對齊 1-G-4 地點清單刪除 pattern。
- 1-G-5 不提供行程 inline 編輯，使用者要修改某筆行程的內容必須先刪除再重新新增。

##### 行程新增搬入編輯 expander

「新增行程」表單搬入該角色編輯 expander 內「角色行程」區塊下方，作為該角色專屬功能。

- 不再透過 `selectbox` 選擇要綁定的角色，因為 expander 已限定該角色。
- 表單欄位與既有「新增角色行程」表單一致：日期類型 / 時間段 / 地點 / 優先權 / 排序值 / 條件 / 行程 ID。
- 表單欄位顯示文字依 1-G-5 行程 enum 中文化規格（見下節）。
- 頁面下方既有共用「新增角色行程」表單必須移除，避免兩處新增入口分歧。

##### 行程 enum 中文化映射

行程相關 enum UI 改為「中文(英文)」混顯，canonical data 仍保存原本英文 enum：

```text
day_type:
  weekday        -> 平日(weekday)
  weekend        -> 週末(weekend)
  holiday        -> 假日(holiday)
  any            -> 任意(any)
  specific_date  -> 1-G-5 暫不顯示於 UI 選項

time_slot:
  morning        -> 早上(morning)
  afternoon      -> 下午(afternoon)
  evening        -> 晚上(evening)

priority:
  critical       -> 必定(critical)
  route          -> 路線(route)
  normal         -> 一般(normal)
  ambient        -> 環境(ambient)
```

輸出資料仍保存英文 enum，例如：

```yaml
schedule:
  - day_type: weekday
    time_slot: morning
    priority: normal
```

`time_slot` 中文化已於 1-G-4 在地點頁建立；1-G-5 必須讓行程頁與地點頁使用相同的中文映射，避免兩處顯示不一致。

行程 `day_type` 選項在 1-G-5 暫時不包含 `specific_date`。`specific_date` 行程的 schema 補完（新增 `specific_date: date` 欄位、UIW Linter 驗證、exporter / parser 對齊）排在後續 phase。已知問題追蹤於 `已知問題.md`。

##### 行程地點選單中文化

行程「地點」`selectbox` 顯示地點中文 name，底層 value 仍是 `location_id`：

```python
sch_loc = st.selectbox(
    "地點",
    options=visitable_location_ids,
    format_func=lambda lid: location_name_map.get(lid, lid),
)
```

- 對齊 1-G-3 中文 label / 英文 canonical 原則。
- 若使用者只看到中文，無法直接判斷 system ID，可在 selectbox 下方以 caption 顯示「目前選擇 ID：<location_id>」協助除錯（非必須）。

##### 白名單顯示與進階設定收納

表情、服裝、位置白名單收進編輯 expander 內「進階設定 (Advanced Settings)」展開區，與 1-G-4 地點頁進階 tags 收納 pattern 對齊。

`st.multiselect` 顯示文字只剩中文 label，移除目前 `"label (id)"` 的英文 ID 後綴：

```python
st.multiselect(
    "表情白名單",
    options=[e["id"] for e in EMOTION_PRESETS],
    default=[...],
    format_func=lambda eid: _preset_label(eid, EMOTION_PRESETS),
)
```

- 底層仍保存 `[{"id": ..., "label": ...}, ...]` 結構，與 1-G-3 語意型欄位一致。
- 預設選擇邏輯保留：新增角色時可預填前 N 個常用 preset；既有角色編輯時依該角色當前 `allowed_emotions` / `allowed_costumes` / `allowed_positions` 預填。

進階設定區頂部加入 `st.markdown` 常駐說明：

```text
💡 受控素材清單：表情、服裝、位置白名單用於限制 AI 生成劇情時可用的角色素材，避免 AI 任意創造。預設值可依角色定位調整。
```

##### Streamlit 實作限制

下列限制是 1-G-5 實作層必須遵守的硬性條件，目的是避開 Streamlit 的 widget key 與 expander 限制，並保證 keyed session state 後綴穩定：

###### 禁止 nested `st.expander`

Streamlit 不允許 `st.expander` 內再放 `st.expander`，會 raise `StreamlitAPIException`。本規格中「進階設定 (Advanced Settings)」展開區不得使用 nested `st.expander`。

實作允許下列任一替代：

- `st.checkbox("顯示進階設定", key=f"show_adv_{character_id}")` 為 `True` 時，於 `st.container` 內顯示白名單區塊。
- `st.toggle("顯示進階設定", key=f"show_adv_{character_id}")` 同上邏輯。

兩種替代都必須使用 per-character keyed state（見下節），避免不同角色的「進階設定展開狀態」互相影響。

###### 編輯 widget 全採 per-character keyed pattern

Streamlit 強制 widget key 全 app 唯一。多個角色 expander 同時展開時，若 widget 不帶 key，Streamlit 會自動產生 key 但其 state 會在重 render 時被合併或衝突。1-G-5 編輯 expander 內**所有** widget 必須使用顯式 key，後綴為該角色 `character_id`：

```text
基本資訊：
  edit_display_name_<character_id>
  edit_gender_<character_id>
  edit_orientation_<character_id>
  edit_role_<character_id>
  edit_identity_<character_id>
  edit_initial_favor_<character_id>

進階設定（白名單）：
  show_adv_<character_id>
  edit_emotions_<character_id>
  edit_costumes_<character_id>
  edit_positions_<character_id>

行程新增表單：
  add_sch_id_<character_id>
  add_sch_day_<character_id>
  add_sch_slot_<character_id>
  add_sch_loc_<character_id>
  add_sch_prio_<character_id>
  add_sch_order_<character_id>
  add_sch_cond_<character_id>

性格 / 秘密 keyed temp state（已於前節定義）：
  temp_ch_tags_<character_id>
  temp_ch_secrets_<character_id>

刪除按鈕、儲存修改按鈕、取消按鈕：
  del_ch_<character_id>
  save_ch_<character_id>
  cancel_ch_<character_id>
  del_sch_<character_id>_<schedule_index>
```

頁面下方共用「新增角色」表單仍使用無後綴 key（`temp_ch_tags`、`temp_ch_secrets` 等），與編輯 expander key 不互相干擾。

###### `character_id` 唯一性與生命週期

per-character keyed pattern 仰賴 `character_id` 在 expander 生命週期內唯一且不變，因此：

- **新增角色時必須擋下**：
  - `character_id` 為空字串。
  - `character_id` 與 `st.session_state["characters"]` 中既有 `character_id` 重複。
  - `character_id` 不符 canonical ID 格式（小寫英數底線、不可中文、不可空格）。
  - 失敗時顯示 `st.error(...)`，不寫入 `st.session_state["characters"]`。
- **編輯 expander 不開放修改 `character_id`**：1-G-5 範圍內 `character_id` 一旦建立即不可變，UI 上以唯讀文字顯示於進階設定區。
  - Phase 1-G-7 僅處理新增角色時自動產生 `character_id`；既有 NPC `character_id` 修改屬 ID rename / reference migration，不納入 1-G-7。
- 此 add-time validation 與 UIW Linter 的 character_id 驗證重疊；UI 即時擋是把 fail-fast 提前，避免使用者匯出 Setup Package 才發現問題。

###### 刪除角色時清空相關 session state

刪除角色（通過 endings / status_flags 引用防呆檢查後）必須同時清掉該角色相關 keyed session state，避免殘留 widget state 在後續 rerender 中誤套到別處：

```python
def _purge_character_state(character_id: str) -> None:
    """刪除該角色相關所有 keyed session state。"""
    suffix = f"_{character_id}"
    schedule_prefix = f"del_sch_{character_id}_"
    keys_to_drop = [
        k for k in list(st.session_state.keys())
        if k.endswith(suffix) or k.startswith(schedule_prefix)
    ]
    for k in keys_to_drop:
        del st.session_state[k]
```

刪除流程：

1. 引用防呆檢查（endings / status_flags）。
2. 通過 → `st.session_state["characters"].pop(i)`。
3. `_purge_character_state(character_id)`。
4. `st.rerun()`。

防呆未通過 → 顯示 toast，不執行 2–4。

##### 不變項

- canonical schema 完全不動：`Character`、`ScheduleEntry`、`AssetOption`、`SemanticChoice` 模型維持現狀。
- exporter / parser 不需修改。
- UIW Linter 規則不需新增（A3 引用防呆是刪除當下的 UI 阻擋，不是 export 時驗證；新增角色 character_id validation 是 UI 層即時擋，linter 仍保留 export 時驗證作為最終防線）。
- 1-G-3 的 `id + label` semantic choice、1-G-4 的地點頁排版、1-G-2 的中文化選項全部保留。
- 主角設定頁（Protagonist tab）與旗標/結局頁不在此 phase 範圍。

##### 測試要求

實作 1-G-5 時應更新：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`（角色頁 `_tab_characters` 重構）
- 必要時補 `tests/test_streamlit_uiw.py`，使用 Streamlit `AppTest` 驗證互動行為
- 不應修改 `src/sandbox_dating_sim/schema/setup.py`、`src/sandbox_dating_sim/uiw/linter.py`、`src/sandbox_dating_sim/exporter/`、`src/sandbox_dating_sim/parser/`；如有變更請對照「範圍排除」確認是否誤入後續 phase 範圍。

必測：

- 角色清單 expander label 顯示「{display_name} · {性別中文} · {定位中文}」，不再顯示英文 `character_id`。
- 角色清單每列右側可直接刪除，不需展開 expander。
- 刪除被 endings 引用的角色被阻擋，toast 列出所有引用 ending_id。
- 刪除被 status_flags 引用的角色被阻擋，toast 列出所有引用 status_id；`protagonist`、`global` 等特殊 target 不被誤判為角色引用。
- 編輯既有角色：修改 `display_name` / `gender` / `orientation` / `role` / `identity` / `initial_favor` 並按「儲存修改」後，`st.session_state["characters"][i]` 對應欄位更新。
- 編輯既有角色性格 / 秘密：修改 `personality_tags` / `secrets` 後儲存，對應 keyed temp state 寫回該角色，且不影響其他角色。
- 編輯既有角色白名單（進階設定）：修改 `allowed_emotions` / `allowed_costumes` / `allowed_positions` 後儲存，對應欄位更新；底層仍保存 `{id, label}` 結構。
- 「取消」按鈕清空該角色 keyed temp state；下次開啟 expander 重新依當前值初始化。
- 行程 `day_type` / `time_slot` / `priority` UI 顯示「中文(英文)」，canonical 仍英文 enum。
- 行程 `day_type` 選項在 1-G-5 不含 `specific_date`。
- 行程地點 `selectbox` 顯示 `location.name`，value 仍 `location_id`。
- 行程「新增」與「刪除」於每個角色編輯 expander 內運作；頁面下方既有共用「新增角色行程」表單已移除。
- 行程清單顯示遇到已被刪除的 `location_id` 時，顯示原 ID 並加註「(地點已刪除)」。
- 白名單 `multiselect` 顯示僅中文 label；底層保存 `{id, label}`。
- 多角色同時開啟 expander 編輯，keyed temp state 不互相污染。

實作限制（Streamlit 層）必測：

- 多角色同時開啟編輯 expander 不可 raise Streamlit duplicate widget key 例外。
- 進階設定區不得使用 nested `st.expander`；採 `st.checkbox` / `st.toggle` 切換顯示，且該切換 state 帶 per-character key。
- 新增角色 `character_id` 為空字串時被擋下並顯示錯誤，不寫入 `characters`。
- 新增角色 `character_id` 與既有重複時被擋下並顯示錯誤。
- 新增角色 `character_id` 含中文、空格或大寫等不符 canonical ID 格式時被擋下。
- 編輯 expander 不提供修改 `character_id` 的 widget。
- 刪除角色（通過引用防呆後）會清掉該角色所有 `*_<character_id>` keyed session state；後續操作或新增同名角色不殘留舊 state。

#### 1-G-6 使用者操作回饋：拆分旗標/結局頁與結局頁 UX 完整化

1-G-6 需求來源為使用者實際操作 UIW 後的回饋：

- 「旗標 / 狀態 / 結局」原本三個區塊塞在同一個分頁，視覺擁擠、難以聚焦。
- 結局設定完後只能看到清單條目摘要，無法檢視完整內容、不能編輯，要改只能刪除重建（與 1-G-5 之前的角色頁同樣痛點）。
- 結局 `priority` 選單只列英文 enum，使用者無法判斷各值優先程度。
- 結局 `required_flags` / `forbidden_flags` 是 free-form 字串輸入，使用者要自己拼 `flag.<id> == <value>` 表達式語法，容易打錯。
- 「不設定旗標 / 狀態，但仍能匯出可用 Setup Package MD」這個目標雖然 schema 與 UIW Linter 已允許，但缺乏明確驗收路徑與測試覆蓋。

此階段對齊 1-G-1 / 1-G-2 / 1-G-4 / 1-G-5 慣例，**不改 canonical schema、不改 Setup Package 輸出格式、不改 UIW Linter 規則**，純改 Streamlit UIW 結局頁 UX 與分頁結構。

##### 範圍

包含：

- 拆分原「旗標 / 狀態 / 結局」分頁為「旗標與狀態(Flags & Status)」與「結局(Endings)」兩個獨立分頁。
- 結局頁清單顯示與 inline expander 編輯。
- 結局 `priority` 中文化與五級語意 caption。
- `target_character_id` selectbox 中文顯示。
- `required_flags` / `forbidden_flags` F-γ multiselect 快速加入互動。
- 最小可用 Setup Package（flags=[] / status_flags=[]）匯出驗收。
- 附帶修正 1-G-5 character_id 即時擋為跨類型唯一（與 UIW Linter `_check_ids` 行為對齊）。

排除：

- **新增流程自動產生唯一 canonical ID（B 群組）**：原排 1-G-6 的 B 群組擴大為 **1-G-7**，涵蓋地點、NPC 角色、旗標、狀態與結局新增流程；新增時不再要求使用者輸入英文 ID。
- **旗標 / 狀態頁 inline 編輯**：原合併頁中的旗標 / 狀態區塊在 1-G-6 內**僅執行分頁搬移，不動 UX**。inline 編輯與中文化排 **1-G-8**。
- **schedule `specific_date` schema 升級（D2）**：重排為 **1-G-9**。1-G-5 仍維持「`day_type` 選項暫不顯示 `specific_date`」的處理。
- **`required_stats` 改 multiselect**：1-G-6 內維持 free-form text_input，不動。1-G-8 也維持此排除，僅處理旗標 / 狀態頁；stat 多選互動另排後續 phase。

##### 分頁順序

`PAGE_LABELS` 更新為：

```python
PAGE_LABELS = [
    "世界觀與曆法(World)",
    "主角設定(Protagonist)",
    "地點與地圖(Locations)",
    "角色設定(Characters)",
    "旗標與狀態(Flags & Status)",
    "結局(Endings)",
    "預覽與匯出(Review & Export)",
]
```

對應實作：

- `_tab_flags()` 拆成 `_tab_flags()` 與 `_tab_endings()` 兩個函式。
- 旗標（Flags）與狀態旗標（Status Flags）區塊保留在 `_tab_flags()`，1-G-6 內**僅做分頁搬移，不動 UX**。
- 結局（Endings）區塊搬到新建立的 `_tab_endings()`，並在此進行本 phase 的 inline 編輯改造。
- `_next_page_button` 邏輯依 `PAGE_LABELS.index` 自動串接，不需修改。
- 主程式 dispatcher（`elif selected_page == PAGE_LABELS[X]:`）對應新增一條分支。

##### 結局清單顯示

每個結局以 `st.expander` 呈現，expander label：

```text
{title or ending_id} · {priority 中文} · {target 顯示}
```

規則：

- `title` 為空時 fallback 顯示 `ending_id`。
- `priority 中文`：採下節中文化映射，但 expander label 內顯示精簡中文（不含括弧英文，避免 label 過長）。例：`必定觸發` / `主線` / `路線` / `一般` / `背景`。
- `target 顯示`：
  - `target_character_id is None` → `（無）`
  - `target_character_id == "global"` → `全局結局`
  - `target_character_id` 命中既有角色 → 該角色 `display_name`（中文）
  - `target_character_id` 為 dangling reference（僅限歷史 / 外部匯入資料；正常 UI 刪除角色時會被 ending 引用防呆阻擋）→ 原 ID 並加註 `(角色已刪除)`

每個結局右側必須直接顯示「刪除」按鈕，不需展開 expander 即可刪除。版面對齊 1-G-5 角色頁 / 1-G-4 地點頁的 `st.columns([5, 1])` 排版。

##### 結局刪除

ending 沒被任何 setup package 欄位 reference（Phase 1 的 `endings[].ending_id` 不出現在其他欄位中），刪除**不需引用防呆 toast**。流程：

1. `st.session_state["endings"].pop(i)`。
2. `_purge_ending_state(ending_id)`（清空所有 `*_<ending_id>` keyed session state，避免殘留）。
3. `st.rerun()`。

##### 既有結局 inline 編輯（Inline Expander）

採與 1-G-5 角色頁相同的 inline expander 編輯範式：每個結局 expander 展開後即為完整編輯表單，多結局可同時展開、互不干擾。Expander 內版面：

```text
[基本資訊]   (form 內)
  結局 ID (ending_id, 唯讀)
  結局標題 (title)
  結局類型 (ending_type, free-form 例 "character_good")
  關聯角色 (target_character_id, 中文 selectbox)
  結局描述 (description)
  優先權 (priority, 中文化 selectbox + caption)
  路線標籤 (route_tags, free-form 逗號分隔)

[條件]
  ── 必須條件 (required_flags) ──
  [form 外] multiselect: 從現有旗標快速選
  [form 外] [加入到必須條件] button → 展開為 placeholder 字串 append 到下方
  [form 內] required_flags free-form text_area
            預填 keyed temp_req_flags_<ending_id>
            ※ 修改條件值（true → false 等）直接編輯此欄位

  ── 禁止條件 (forbidden_flags) ──
  [form 外] multiselect + [加入到禁止條件] button
  [form 內] forbidden_flags free-form text_area
            預填 keyed temp_forb_flags_<ending_id>

  ── 必須數值 (required_stats) ──
  [form 內] required_stats free-form text_input
            (1-G-6 不動，保持 free-form)

[儲存修改]   [取消]
```

「加入到必須條件 / 加入到禁止條件」按鈕觸發 `st.button()`，不能放在 `st.form()` 內，因此 multiselect 與 button 必須位於 form 外，與 form 外 keyed temp state 互動。儲存 / 取消按鈕為 `st.form_submit_button()`，位於 form 內。

##### F-γ multiselect 互動規則

「加入到必須條件 / 加入到禁止條件」button 點擊後：

1. 讀取 multiselect 當前選中的 `flag_id` 清單。
2. 對每個 flag 產生 placeholder：
   - `boolean` flag → `flag.<id> == true` / `flag.<id> == false`
   - `integer` flag → `flag.<id> == <initial_value>`（不加引號）
   - `string` / `enum` flag → `flag.<id> == "<initial_value>"`（使用雙引號；值內若已有 `"` 或 `\`，需 escape）
3. 將 placeholder 字串 append 到 `temp_req_flags_<ending_id>` / `temp_forb_flags_<ending_id>`（list[str] 暫存），同時更新 form 內 text_area 顯示為「逗號分隔字串」。
4. 清空 multiselect 選擇（避免使用者再點一次按鈕重複加入）。

multiselect 與 free-form text_area 之間是「單向 append」關係：multiselect 取消勾選**不會**自動移除已加入的字串。修改條件值（例 `== true` → `== false`、`>= 100` → `>= 200`）必須直接編輯 text_area。

flags=[] 或沒任何 flag 可選時，multiselect 顯示空清單並在下方顯示 caption「尚未設定旗標。可在『旗標與狀態』分頁建立後再回此處快速加入」。此狀況下使用者仍可：

- 留 `required_flags` / `forbidden_flags` 為空 → 匯出 Setup Package（符合「最小可用」目標）。
- `required_stats` 仍可在下方 free-form text_input 輸入 `stat.*` 條件；`required_flags` / `forbidden_flags` 只放 `flag.*` 條件。

##### Priority 中文化映射

結局頁 priority 採「中文(英文)」混顯：

```text
priority:
  critical -> 必定觸發(critical)
  main     -> 主線(main)
  route    -> 路線(route)
  normal   -> 一般(normal)
  ambient  -> 背景(ambient)
```

selectbox 下方常駐 caption（依正式規格 v1.2 §5 排序）：

```text
優先權由高到低：
  critical: 強制觸發，覆蓋其他所有結局
  main:     主線結局
  route:    角色路線結局
  normal:   一般結局
  ambient:  背景/支線結局，最低優先
```

canonical data 仍保存英文 enum：

```yaml
endings:
  - ending_id: sophie_good_end
    priority: route
```

##### `target_character_id` 中文 selectbox

selectbox 三類選項：

```python
options = [None, "global"] + [ch["character_id"] for ch in st.session_state["characters"]]
# 若目前 ending.target_character_id 是 dangling reference，額外 append 該原 ID，
# 只為了讓歷史 / 外部匯入資料可顯示並保留；正常 UI 刪除角色時應被引用防呆阻擋。

format_func:
  None         -> 「（無 / 不指定）」
  "global"     -> 「全局結局 (global)」
  <character_id> -> 該角色 display_name（中文）
                   ※ dangling reference（歷史 / 外部匯入資料）時顯示原 ID 並加註「(角色已刪除)」
```

底層 value 仍存 `character_id` / `"global"` / `None`，與 schema 一致。對齊 1-G-3 中文 label / 英文 canonical 原則。

##### `ending_id` 唯一性與生命週期

per-ending keyed pattern 仰賴 `ending_id` 唯一且不變。

新增結局時必須擋下：

- `ending_id` 為空字串。
- `ending_id` 與既有 **world_id / character_id / location_id / flag_id / status_id / ending_id** 任一重複（**跨類型唯一**；1-G-6 UI 即時擋納入 `status_id`，linter 的 `status_id` 補洞另排後續 phase）。
- `ending_id` 不符 canonical ID 格式（小寫英數底線、不可中文、不可空格）。

失敗時顯示 `st.error(...)`，不寫入 `st.session_state["endings"]`。

實作抽 helper：

```python
def _collect_existing_ids() -> set[str]:
    s: set[str] = set()
    world = st.session_state.get("world") or {}
    if world.get("world_id"):
        s.add(world["world_id"])
    s.update(c["character_id"] for c in st.session_state.get("characters", []) if c.get("character_id"))
    s.update(l["location_id"] for l in st.session_state.get("locations", []) if l.get("location_id"))
    s.update(f["flag_id"] for f in st.session_state.get("flags", []) if f.get("flag_id"))
    s.update(sf["status_id"] for sf in st.session_state.get("status_flags", []) if sf.get("status_id"))
    s.update(e["ending_id"] for e in st.session_state.get("endings", []) if e.get("ending_id"))
    return s
```

新增結局時若 `ending_id in _collect_existing_ids()` → 擋。

編輯 expander 內 `ending_id` 唯讀顯示，1-G-6 不開放修改。

##### 附帶修正：1-G-5 character_id 即時擋擴為跨類型唯一

1-G-5 規格中「新增角色 character_id 與既有 character_id 重複時擋下」**未涵蓋跨類型重複**（例 character_id `"sophie"` 與 location_id `"sophie"` 衝突）。雖然 UIW Linter export 時會擋為 `duplicate_id` error 作為最終防線，但 UI 層即時擋與 linter 行為不一致，會讓使用者匯出時才看到問題。

1-G-6 順帶修正：1-G-5 角色頁的「新增角色」流程改用同一個 `_collect_existing_ids` helper，擋下 character_id 與**任何類型**既有 ID 重複，包含既有 `status_id`。其他 1-G-5 規格不變。

對應修改：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`：抽出 `_collect_existing_ids` helper，新增角色與新增結局共用；helper 收 `world_id / character_id / location_id / flag_id / status_id / ending_id`。
- 1-G-5 規格內「`character_id` 唯一性與生命週期」節在 `開發設計方針.md` / `Sandbox_Dating_Sim_初始設定_v1.2.md` 文字保持原狀；1-G-6 在規格內明確聲明此行為已被擴充，並在測試指南補測。

##### Streamlit 實作限制

對齊 1-G-5 已建立的 Streamlit pattern：

###### 禁止 nested `st.expander`

結局編輯 expander 內**不**得再放 `st.expander`。1-G-6 結局頁 form 結構平鋪，沒有「進階設定」展開區的需求；若日後新增進階區，採 `st.checkbox("顯示進階設定", key=f"show_adv_end_{ending_id}")` 或 `st.toggle` 切換。

###### 編輯 widget 全採 per-ending keyed pattern

結局編輯 expander 內所有 widget 必須使用顯式 key，後綴為該結局 `ending_id`：

```text
基本資訊：
  edit_title_<ending_id>
  edit_ending_type_<ending_id>
  edit_target_<ending_id>
  edit_description_<ending_id>
  edit_priority_<ending_id>
  edit_route_tags_<ending_id>

條件：
  ms_req_flags_<ending_id>          ← multiselect 快速選
  add_req_flags_<ending_id>         ← button「加入到必須條件」
  edit_req_flags_<ending_id>        ← form 內 text_area
  ms_forb_flags_<ending_id>
  add_forb_flags_<ending_id>
  edit_forb_flags_<ending_id>
  edit_req_stats_<ending_id>

按鈕：
  del_end_<ending_id>
  save_end_<ending_id>
  cancel_end_<ending_id>

Form-外暫存 state（list[str]）：
  temp_req_flags_<ending_id>
  temp_forb_flags_<ending_id>
```

頁面下方共用「新增結局」表單仍使用無後綴 key（`new_ending_id`、`new_ending_title` 等）。

###### 多結局並行編輯

多結局可同時展開 expander 編輯，因 widget key 帶 `ending_id` 後綴，state 不互相污染。

###### 刪除結局時清空相關 session state

```python
def _purge_ending_state(ending_id: str) -> None:
    suffix = f"_{ending_id}"
    keys_to_drop = [k for k in list(st.session_state.keys()) if k.endswith(suffix)]
    for k in keys_to_drop:
        del st.session_state[k]
```

##### 「儲存修改」與「取消」行為

對齊 1-G-5 角色 inline 編輯：

- 編輯期間僅修改 keyed temp state 與 form 內 widget value，不立即寫回 `st.session_state["endings"][i]`。
- 「儲存修改」→ keyed temp state 與 form 內欄位合併寫回該 ending：
  - `required_flags` 從 `temp_req_flags_<ending_id>` 解析（list[str] 直接寫入）；同時把 form 內 free-form text_area 的內容（逗號分隔字串）覆寫到 list（form 內為使用者最後編輯結果，優先採用）。
  - `forbidden_flags` 同理。
  - `required_stats` 從 form 內 free-form text_input 解析（逗號分隔 → list[str]）。
- 「取消」→ 清空 `temp_req_flags_<ending_id>` / `temp_forb_flags_<ending_id>`；下次開啟 expander 重新依當前 ending 值初始化。

##### 不變項

- canonical schema 完全不動：`Ending`、`FlagDef`、`StatusFlag`、`SetupPackage` 模型維持現狀。
- exporter / parser 不需修改。
- UIW Linter 規則不需新增（跨類型 ID 唯一檢查已存在於 `_check_ids`）。
- 1-G-3 / 1-G-4 / 1-G-5 既有行為全部保留。
- 主角頁、地點頁、角色頁、世界頁 1-G-6 不動。
- 旗標 / 狀態頁 1-G-6 內僅分頁搬移，UX 內容原樣。

##### 最小可用 Setup Package 驗收

定義「最小可用」Setup Package：

```text
最小可用條件：
  - World 必填欄位齊全
  - Protagonist 必填欄位齊全
  - locations 至少 1 筆（其中至少 1 筆 is_visitable=True）
  - characters 至少 1 筆
  - endings 至少 1 筆
  - flags = []
  - status_flags = []

預期結果：
  - UIW Linter 不報 error（warnings 可接受）
  - SetupPackageExporter.to_markdown() 成功產生 MD
  - MD 可被 parser 還原為 SetupPackage
  - 該 ending 的 required_flags / forbidden_flags / required_stats 全為空
```

實作層補測試（細節見 `測試指南.md`）。

##### 測試要求

實作 1-G-6 時應更新：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`（拆 `_tab_flags()` 與 `_tab_endings()`、結局頁 inline 編輯改造、`_collect_existing_ids` helper、`_purge_ending_state` helper、修補 1-G-5 character add 跨類型唯一檢查）
- 必要時補 `tests/test_streamlit_uiw.py`，使用 Streamlit `AppTest` 驗證 UI 行為
- 補 `tests/test_exporter.py` 或 `tests/test_uiw_linter.py` 對「最小可用」case 的覆蓋
- 不應修改 `src/sandbox_dating_sim/schema/setup.py`、`src/sandbox_dating_sim/uiw/linter.py`、`src/sandbox_dating_sim/exporter/`、`src/sandbox_dating_sim/parser/`

必測：

分頁拆分：

- `PAGE_LABELS` 第 5、6 項分別為「旗標與狀態(Flags & Status)」與「結局(Endings)」，順序在「角色設定」之後、「預覽與匯出」之前。
- 「下一頁」按鈕從旗標與狀態頁進入結局頁，從結局頁進入預覽與匯出頁。
- 旗標 / 狀態頁內容與 1-G-5 之前合併頁的旗標 / 狀態區塊功能完全一致（旗標清單、新增旗標、狀態旗標清單、新增狀態旗標）。
- 結局頁內容為 1-G-6 改造後的版本，原合併頁中的結局區塊已移出。

結局清單顯示：

- 結局清單 expander label 顯示「{title or ending_id} · {priority 中文} · {target 顯示}」。
- title 為空時 expander label fallback 顯示 `ending_id`。
- target 為 None 顯示「（無）」、`"global"` 顯示「全局結局」、character_id 顯示對應 `display_name`；歷史 / 外部匯入資料若有 dangling reference，需保留原 ID 並加註「(角色已刪除)」。
- 結局清單每列右側可直接刪除，不需展開 expander。
- 刪除後 `_purge_ending_state(ending_id)` 清空所有 `*_<ending_id>` session state。

inline 編輯：

- 編輯既有結局：修改 title / ending_type / target_character_id / description / priority / route_tags 並按「儲存修改」後，`st.session_state["endings"][i]` 對應欄位更新。
- 編輯既有結局 required_flags / forbidden_flags：multiselect 選擇 boolean flag → 點「加入到必須條件 / 禁止條件」→ free-form text_area 對應字串被 append；儲存後 ending 對應 list 更新；取消後 keyed temp state 清空、free-form 重置為當前值。
- 編輯既有結局 required_stats：直接修改 free-form text_input → 儲存後 list 更新。
- 「取消」按鈕清空該結局 keyed temp state；下次開啟 expander 重新依當前值初始化。
- 多結局同時開啟 expander 編輯，keyed temp state 不互相污染，無 Streamlit duplicate widget key 例外。
- `ending_id` 在編輯 expander 為唯讀，1-G-6 不可修改。

中文化：

- priority selectbox 顯示「中文(英文)」：必定觸發(critical) / 主線(main) / 路線(route) / 一般(normal) / 背景(ambient)。
- priority selectbox 下方顯示五級語意 caption，順序由高到低。
- target_character_id selectbox 顯示中文 / 「（無 / 不指定）」/「全局結局 (global)」；底層仍存 None / "global" / character_id。
- 匯出後 canonical 仍保存英文 priority enum。

F-γ multiselect 行為：

- multiselect 顯示現有旗標清單（依 `st.session_state["flags"]`）。
- 點擊「加入到必須條件」按鈕後，所選 flag 展開為 placeholder 字串 append 到 free-form text_area，multiselect 清空。
- boolean flag 展開為 `flag.<id> == true` / `flag.<id> == false`；integer flag 不加引號；string / enum flag 以雙引號包住 initial_value。
- multiselect 取消勾選**不會**自動移除已加入的字串。
- flags=[] 時 multiselect 空，下方顯示「尚未設定旗標」caption。

新增結局即時擋（跨類型唯一）：

- 新增 ending_id 為空字串時被擋下並顯示 `st.error`，不寫入 `endings`。
- 新增 ending_id 與既有 world_id / character_id / location_id / flag_id / status_id / ending_id 任一重複時被擋下。
- 新增 ending_id 含中文、空格或大寫等不符 canonical ID 格式時被擋下。

附帶修正（1-G-5 character_id 跨類型唯一）：

- 新增 character_id 與既有 world_id / location_id / flag_id / status_id / ending_id / character_id 任一重複時被擋下。
- 既有 1-G-5 character_id 空 / 重複 / 非法格式擋下行為不被破壞。

最小可用 Setup Package 匯出：

- fixture：`flags=[]` / `status_flags=[]`，其他 World / Protagonist / locations / characters / endings 全有，且該 ending `required_flags=[]` / `forbidden_flags=[]` / `required_stats=[]`。
- UIW Linter 對此 fixture 不報 error。
- `SetupPackageExporter.to_markdown()` 成功產出 MD。
- MD 透過 parser 可還原為 `SetupPackage`，roundtrip 一致。

實作限制（Streamlit 層）必測：

- 多結局同時開啟編輯 expander 不可 raise duplicate widget key 例外。
- 結局編輯 expander 不使用 nested `st.expander`。
- 編輯 expander 不提供修改 `ending_id` 的 widget。
- 刪除結局後清空所有 `*_<ending_id>` keyed session state；新增同名 ending_id 不殘留舊 state。

---

#### 1-G-7 使用者操作回饋：新增流程自動產生唯一 canonical ID

1-G-7 需求來源為使用者確認：除主角固定 `protagonist` 以外，地點、NPC 角色、旗標、狀態與結局的英文系統 ID 都不應成為一般使用者必填項目。這些 ID 是工具內部引用鍵，應由 UI 依使用者填寫的中文名稱、標題或 label 自動產生，並確保全域唯一。

##### 範圍

包含下列新增流程：

- 手動新增地點：`location_id`
- 新增 NPC 角色：`character_id`
- 新增旗標：`flag_id`
- 新增狀態旗標：`status_id`
- 新增結局：`ending_id`

不包含：

- `world_id`：仍由 World 頁處理，因為它同時是專案 / 遊戲識別碼。
- `protagonist_id`：固定為 `protagonist`。
- 既有資料的 ID rename：不在 1-G-7 內修改已建立項目的 ID，也不做 reference migration。
- template 內部定義的固定 ID 結構重設計：套用地點模板時可沿用模板建議 ID，但仍必須經過同一個唯一性檢查與衝突避讓。

##### 使用者體驗

新增表單應以使用者理解的中文欄位為主：

```text
地點：地點名稱 / 地點類型 / 描述 / tags
NPC：角色姓名 / 性別 / 定位 / 身分 / 描述
旗標：用途描述 / 型別 / 初始值 / 說明
狀態：狀態名稱 / 作用對象 / effect / duration / clear_rule / 說明
結局：結局標題 / 類型 / 關聯角色 / 描述 / 優先權
```

英文 ID 欄位不應再是一般使用者必填欄位。UI 可在「進階設定」或 caption 顯示系統 ID：

```text
系統 ID：ch_lin_xiao_yu
```

若提供手動調整 ID 的入口，必須放在進階設定中，且套用與目前一致的 canonical ID 格式檢查與全域唯一檢查。驗證失敗時不新增資料、不清空使用者已填內容。

##### ID 產生規則

新增時依下列來源產生 base slug：

| 類型 | 來源欄位 | prefix | 範例 |
| :--- | :--- | :--- | :--- |
| 地點 | `name` | `loc_` | `loc_ka_fei_ting` |
| NPC 角色 | `display_name` | `ch_` | `ch_lin_xiao_yu` |
| 旗標 | `description` | `flag_` | `flag_shi_fou_shi_ye` |
| 狀態旗標 | `label` | `status_` | `status_guo_lao` |
| 結局 | `title` | `end_` | `end_su_fei_hao_jie_ju` |

產生流程：

1. 取來源欄位的使用者可讀文字。
2. 中文轉拼音；英文轉小寫；空白與標點（包含中文標點如 `、`、`，`、`。`、`：`）轉 `_`。
3. 移除不符合 canonical ID 的字元。
4. 合併重複 `_`，去掉頭尾 `_`。
5. 將 slug 依 `_` 切成 token，最多保留前 8 個 token，避免 description 產生過長 ID；截斷後再次合併重複 `_` 並去掉頭尾 `_`。
6. 若結果空白，使用不含 prefix 的中性 fallback `unnamed`。
7. 加上類型 prefix。
8. 與既有全域 ID 比對；若衝突，依序嘗試 `_2`、`_3`、`_4`，直到唯一。

suffix 連鎖規則：suffix 必須**順序遞增**，即從 `_2` 開始依序 `_3`、`_4`、`_5`…，**不回填空缺**。例如既有 `loc_ka_fei_ting`、`loc_ka_fei_ting_2`、`loc_ka_fei_ting_4` 時，新增同 slug 應給出 `loc_ka_fei_ting_3`（最小未占用），但實作可選擇單純取最大數加 1 改為 `loc_ka_fei_ting_5`；本期統一採「**順序遞增找第一個未占用**」以保持與使用者直覺一致，並避免無上限往上跳。

產出的 ID 必須符合既有 canonical ID 規則：

```text
^[a-z][a-z0-9_]*$
```

##### 全域唯一性

1-G-7 應建立共用 helper，讓所有新增流程使用同一套 ID 產生與唯一性邏輯。全域 ID 集合至少包含：

```text
world.world_id
固定 protagonist_id（即字面 "protagonist"）
characters[].character_id
locations[].location_id
flags[].flag_id
status_flags[].status_id
endings[].ending_id
```

注意：
- `protagonist` 為固定 ID，不在 `characters[]` 內；自動路徑因 prefix 隔離永不衝突，但**進階手動 ID** 入口可能輸入 `protagonist`，必須擋下。
- 目前 UI 層 `_collect_existing_ids` 已納入 `status_id`，但**尚未納入 `protagonist`**；本 phase 須補。
- UIW Linter 的 `_check_ids` 原排 1-G-8 補 `status_id`；目前已在 1-G-7 收尾先行修復並補測，1-G-8 只需確認文件與測試狀態。

##### 跨類唯一性的觸發場景

自動 ID 路徑下，地點固定 `loc_*`、NPC 固定 `ch_*`、旗標固定 `flag_*`、狀態固定 `status_*`、結局固定 `end_*`，**prefix 不同永不可能跨類衝突**。跨類衝突只在以下情境會真的發生，測試與實作必須對齊這三個場景：

1. **進階手動 ID 入口**：使用者繞過自動產生路徑，自行輸入完整 ID。
2. **地點模板套用**：1-G-4 既有「沿用模板建議 ID」流程，若模板帶來的 ID 與其他類型既有 ID 撞（例如某地點模板建議 `lin_xiao_yu`，正好與 NPC `lin_xiao_yu` 撞）。
3. **既有匯入 / 手寫 Setup Package**：外部資料的 ID 不一定遵循 prefix 慣例。

三個場景一律經過 `_make_unique_id` 的全域唯一性檢查；場景 (1) 應顯示錯誤並要求改名，場景 (2) 應自動 suffix 避讓（見下節），場景 (3) 維持匯入結果原狀，由 UIW Linter 後續處理。

##### 模板套用衝突處理（挑邊）

地點模板套用時，模板建議 ID 必須經過 `_make_unique_id` 的全域唯一性檢查。若衝突：

- 採「**自動 suffix 避讓**」：模板 ID 為 `loc_home`，已存在 `loc_home` 時，新增結果為 `loc_home_2`；不阻止使用者重複套用模板。
- 不採「阻止新增 + toast」：因為地點模板（家、學校、便利商店等）天生需要重複套用。

此挑邊覆蓋 1-G-4 既有「沿用模板建議 ID」行為，1-G-7 不另立旗標控制。

##### 預覽 ID 顯示時機

UI 採「**submit 後在新增結果以 caption 顯示自動產生的系統 ID**」，不在 form 內 widget callback 中即時預覽。原因：

- form 內 callback 即時產生 ID 會導致中途切換來源欄位、token 截斷、衝突 suffix 等行為頻繁變動，反而干擾使用者輸入。
- submit 後 caption 顯示已穩定的最終 ID，並可同時顯示在「進階設定」區供除錯。

不允許將「自動 ID 預覽」做成 form 內必填或可編輯欄位。

##### 引用與不變項

- canonical schema 不變：各 ID 欄位仍是英文 `str`。
- exporter / parser 不需修改：輸出的 Setup Package MD 仍保存英文 ID。
- 使用者可見 label/name/title 可中文，系統引用欄位仍英文。
- 新增後，其他欄位引用該項目時仍使用 canonical ID 作為底層 value，但 UI 顯示中文名稱。
- 不做既有 ID rename，因此不需要搬移 `parent_location_id`、schedule `location_id`、ending `target_character_id`、status target/targets、flag condition text 或 session state keys。

##### 實作建議

在 `src/sandbox_dating_sim/ui/streamlit_uiw.py` 中新增一層共用 helper：

```python
def _make_unique_id(label: str, prefix: str, fallback: str, existing_ids: set[str]) -> str:
    ...
```

`fallback` 傳入不含 prefix 的中性 slug，預設可用 `"unnamed"`。此 helper 內部應呼叫既有 `_generate_system_id(label)` 取得 base slug，再負責截斷、prefix 與唯一性 suffix。

也可拆為兩層：

```python
def _slugify_label(label: str, fallback: str) -> str:
    ...

def _make_unique_id(label: str, prefix: str, fallback: str, existing_ids: set[str]) -> str:
    ...
```

必須沿用既有 `_generate_system_id`、`_collect_existing_ids` 與 `is_valid_id` 行為，不重新發明 ID 規則。不得修改 `_generate_system_id` 的簽章與既有行為，因為 1-G-3 semantic choice 仍依賴它產生不帶類型 prefix 的系統 ID。若 `pypinyin` 已在 dependencies 中，中文轉拼音應使用該套件。

##### 測試要求

應補 `tests/test_streamlit_uiw.py` 與 helper 層測試（建議 `tests/test_ids.py` 或同檔案內 helper section），分兩層覆蓋：

**純函式單測（必跑、ROI 最高、不需 Streamlit AppTest）**：

- `_slugify_label("咖啡廳") == "ka_fei_ting"`
- `_slugify_label("") == "unnamed"`
- `_slugify_label` 處理中文標點 `、，。：` 轉 `_` 並合併重複。
- `_slugify_label` 對長 description 截斷至前 8 個 token。
- `_make_unique_id("咖啡廳", "loc_", "unnamed", set())` 產生 `loc_ka_fei_ting`。
- `_make_unique_id` 在 `existing_ids = {"loc_ka_fei_ting"}` 時產生 `loc_ka_fei_ting_2`。
- suffix 連鎖：`existing_ids = {"loc_x", "loc_x_2", "loc_x_3"}` 產生 `loc_x_4`。
- suffix 順序遞增不回填：`existing_ids = {"loc_x", "loc_x_2", "loc_x_4"}` 產生 `loc_x_3`（最小未占用）。
- 跨類前綴隔離：`_make_unique_id("林小雨", "ch_", ..., {"loc_lin_xiao_yu"})` 產生 `ch_lin_xiao_yu`，不受 `loc_*` 影響。

**UI 行為測試（Streamlit AppTest）**：

- 中文地點 `name` 自動產生合法 `location_id`。
- 中文角色 `display_name` 自動產生合法 `character_id`。
- 中文旗標 `description` 自動產生合法 `flag_id`。
- 中文狀態 `label` 自動產生合法 `status_id`。
- 中文結局 `title` 自動產生合法 `ending_id`。
- 自動產生 ID 後，新增成功才清空 draft state；**自動 ID 路徑驗證失敗（必填欄位空、衝突無解等）也必須保留使用者輸入**。
- 進階手動 ID 入口：輸入非法格式 / 重複 ID 被擋下，原輸入保留。
- 進階手動 ID 入口：輸入 `protagonist` 必須被擋下（與固定主角 ID 衝突）。
- 跨類型衝突場景：以進階手動 ID 在類型 A 建立 `xxx`，再以進階手動 ID 在類型 B 試輸入 `xxx`，後者被擋下。
- 模板套用衝突：連續套用相同地點模板，第二次自動產生 `_2` suffix，不阻止建立。
- 匯出後 canonical data 仍保存英文 ID，中文 `name` / `display_name` / `label` / `title` / `description` 不遺失。
- submit 後 caption 顯示自動產生的系統 ID。

**回歸測試**：

- 1-G-3 自訂語意選項（personality / occupation / secret）回歸：產出 ID 與 1-G-3 完成時一致；確認 `_generate_system_id` 簽章與行為**未被修改**（必要時加 `inspect.signature` 斷言）。
- Phase 1-A 到 1-F 全測試通過。
- Phase 1-G-1 到 1-G-6 全測試通過。
- 實機 end-to-end：啟動 UIW、走完最小可用設定、export → parse roundtrip 一致。

---

#### 1-G-8 旗標 / 狀態頁 inline 編輯與 status targets 複選

Phase 1-G-8 目標是補完「旗標與狀態(Flags & Status)」分頁的使用者操作體驗，使其對齊 1-G-4 地點頁、1-G-5 角色頁與 1-G-6 結局頁已建立的清單 + inline expander 編輯模式。

##### 範圍

包含：

- 旗標清單顯示、inline expander 編輯、刪除與引用防呆。
- 狀態旗標清單顯示、inline expander 編輯、刪除與引用防呆。
- `flag_id` / `status_id` 編輯期唯讀，不做既有 ID rename。
- `StatusFlag.target` canonical schema 升級為 `targets: list[str]`，UI 以主角 + 角色複選。
- 持續類型 `duration.type` UI 改為「中文(英文)」選項，並加說明 caption。
- `StatusFlag.effect` 以 YAML text_area 編輯，保留 list[dict] 結構。
- 已先行修復的 `status_id` linter 補洞補入 1-G-8 驗收狀態。
- **同步更新範圍（schema migration 連動）**：
  - 1-G-5 角色刪除防呆對 `status_flags[].target == 角色 character_id` 的引用比對改為「`targets` 包含該 `character_id`」，比對前先經 normalize（避免舊格式逃過檢查）。
  - 1-G-6 結局頁、1-G-4 地點頁等任何掃描 `status_flags[].target` 的位置同步改用 `targets`。
  - 既有 fixture 與 unit test 遷移：`tests/fixtures/setup_minimal.yaml`、`tests/test_setup_schema.py`、`tests/test_uiw_linter.py` 內所有 `target="..."` 構造改為 `targets=[...]`。
  - 另新增 `tests/fixtures/setup_legacy_status_target.yaml`（保留舊 `target:` 欄位）作為 parser 相容層測試 fixture。

排除：

- 不做完整 Status Manager runtime。
- 不解析 `effect` 的語意內容，只驗證資料形狀為非空 `list[dict]`。
- 不做既有 `flag_id` / `status_id` rename 與 reference migration。
- 不掃描 Phase 2 之後產出的 Event Blueprint / Scene Draft 外部引用。
- 不把 ending `required_stats` 改為 status multiselect；該項若要做，另列後續 phase。
- 不處理 NPC `character_id == "protagonist"` 的歷史衝突資料（1-G-7 已在新增路徑擋下；舊資料修補留 UIW Linter）。

##### Canonical Schema 變更：`target` → `targets`

1-G-8 將 `StatusFlag` 的作用對象由單一字串改成多選清單，**空 targets 由 schema 層直接拒絕**（`Field(min_length=1)`）：

```python
from typing import Any
from pydantic import BaseModel, Field, model_validator

class StatusFlag(BaseModel):
    status_id: str
    label: str
    targets: list[str] = Field(min_length=1)
    effect: list[dict[str, Any]]
    duration: StatusDuration
    clear_rule: list[ClearRule]
    description: str
    permanent_reason: str | None = None

    @model_validator(mode="before")
    @classmethod
    def _normalize_legacy_target(cls, data):
        """相容舊格式：把 target: str 轉成 targets: [str]。"""
        if not isinstance(data, dict):
            return data
        if "targets" not in data and "target" in data:
            target = data.pop("target")
            data["targets"] = [target] if isinstance(target, str) and target else []
        return data
```

YAML 範例：

```yaml
status_flags:
  - status_id: overworked
    label: 過勞
    targets:
      - protagonist
      - sophie
    effect:
      - block_time_slot: evening
    duration:
      type: time_slots
      value: 1
    clear_rule:
      - on_time_advance
      - on_rest
    description: 太累，晚上無法外出。
```

定義：

- `targets` 表示此 status 可作用的對象清單，不表示 runtime 一定同時套用到所有對象。
- 可選值為固定 `protagonist` 加上 `characters[].character_id`。
- runtime / Event Blueprint result 仍可用 `status.<target_id>.<status_id> = true` 指定某一對象。
- UI 新增與編輯時必須至少選一個 target；不可空清單（schema 層 `Field(min_length=1)` 也會擋）。
- 匯入舊資料若仍使用 `target: protagonist`，由 `model_validator(mode="before")` 在 schema 層 normalize 為 `targets: ["protagonist"]`；exporter 一律輸出新格式 `targets`。
- 1-G-8 須同步更新 schema、fixture、parser/exporter roundtrip 測試與 UIW Linter。

##### 挑邊：normalize 落點與空 targets 處理

兩條挑邊明示如下，避免實作三選一：

- **normalize 落點挑 schema 層 `model_validator(mode="before")`**：好處是所有進入點（直接 `SetupPackage(**old_dict)`、parser `SetupPackage(**data)`、fixture load）通吃；不需要在 `pipeline/setup_parser.py` 重複處理。Pydantic v2 已支援 `mode="before"` validator，且不影響 `model_dump` 輸出（exporter 仍輸出新格式）。
- **空 targets 挑 schema 層拒絕**：以 `Field(min_length=1)` 直接 `ValidationError`。UIW Linter 可重複保險，但 schema 是最後一道防線；UI 層 submit 前先擋一次給友善訊息。
- 不挑「parser 層手動 walk + Linter 報 error + schema 接受」的組合，避免相容邏輯散落三處。

##### 旗標清單與 inline 編輯

旗標列表每筆顯示：

```text
{description or flag_id} · {type} · 初始值 {initial_value}
```

每筆使用 `st.expander` 編輯：

```text
[基本資訊]
  旗標 ID (flag_id, 唯讀)
  型別 (type)
  初始值 (initial_value)
  說明 (description)

[儲存修改] [取消]
```

`initial_value` 依 `type` 使用不同 widget：

- `boolean`：selectbox `true` / `false`，canonical 存 bool。
- `integer`：number_input，canonical 存 int。
- `string`：text_input，canonical 存 str。
- `enum`：本期仍用 text_input 存 str，不新增 enum choices schema。

儲存才寫回 `st.session_state["flags"]`。取消只清空該 flag 的 keyed edit state，不修改 canonical。

##### 旗標刪除防呆

刪除 flag 前需掃描目前 Setup Package 內部引用：

- `endings[].required_flags`
- `endings[].forbidden_flags`
- `characters[].schedule[].condition`

引用比對採 **word-boundary regex**（不可用 substring `in`，會誤判 `flag.over_x` 撞 `flag.over`）：

```python
FLAG_REF_PATTERN = re.compile(r"\bflag\.([a-zA-Z0-9_]+)\b")

def _has_flag_reference(text: str, flag_id: str) -> bool:
    return any(m.group(1) == flag_id for m in FLAG_REF_PATTERN.finditer(text))
```

對齊 1-G-6 既有 `_check_flags` 風格（`re.match(r"^flag\.([a-zA-Z0-9_]+)\s*==", ...)`），但 1-G-8 改用 `finditer + \b` 以容許條件字串中段引用（例如 `not flag.x and flag.y`）。

若任一字串引用 `flag.<flag_id>`，阻止刪除並以 toast 或 error 列出引用位置。沒有引用才刪除，並呼叫 `_purge_flag_state(flag_id)` 清空 `*_<flag_id>` keyed state。

##### 狀態清單與 inline 編輯

狀態列表每筆顯示：

```text
{label or status_id} · {targets 顯示} · {duration.type 中文} · {clear_rule 摘要}
```

每筆使用 `st.expander` 編輯：

```text
[基本資訊]
  狀態 ID (status_id, 唯讀)
  顯示名稱 (label)
  作用對象 (targets, 主角 + 角色複選)
  說明 (description)

[效果]
  effect YAML (list of dict)

[生命週期]
  持續類型 (duration.type, 中文(英文) selectbox + caption)
  持續值 (duration.value)
  解除條件 (clear_rule, multiselect)
  永久原因 (permanent_reason, duration.type == permanent 時必填)

[儲存修改] [取消]
```

##### `targets` 複選 UI

選項來源（須 dedupe 以免歷史資料中 NPC `character_id == "protagonist"` 出現重複選項）：

```python
def _status_targets_options(characters: list[dict], current_targets: list[str]) -> list[tuple[str, str]]:
    """回傳 [(label, id), ...]；label 給 UI 顯示，id 是寫回 canonical 的值。
    options 順序：固定 protagonist 在前、NPC 接續、dangling target 在最後。
    含 dedupe：若 NPC character_id == "protagonist" 只保留固定 protagonist 一筆。
    """
    seen: set[str] = set()
    out: list[tuple[str, str]] = []
    out.append(("主角(protagonist)", "protagonist"))
    seen.add("protagonist")
    for ch in characters:
        cid = ch.get("character_id")
        if not cid or cid in seen:
            continue
        out.append((f"{ch.get('display_name', cid)}({cid})", cid))
        seen.add(cid)
    for tgt in current_targets:
        if tgt and tgt not in seen:
            out.append((f"{tgt}(角色已刪除)", tgt))
            seen.add(tgt)
    return out
```

顯示規則：

```text
protagonist      -> 主角(protagonist)
<character_id>   -> {display_name}(<character_id>)
dangling target  -> {raw_id}(角色已刪除)
```

若既有資料含 dangling target，編輯 UI 需保留該 raw ID 作為額外選項，避免使用者開啟頁面就遺失資料。儲存時：

- 使用者未取消勾選 dangling target → 原樣保留。
- 使用者取消勾選 → 移除。
- `targets` 為空 → 不儲存，顯示錯誤（schema `Field(min_length=1)` 也會在最終匯出時擋住）。

##### `duration.type` 中文化與說明

selectbox 顯示「中文(英文)」，canonical 仍存英文：

| canonical | UI 顯示 | 說明 |
| :--- | :--- | :--- |
| `time_slots` | 時段數(time_slots) | 經過指定數量的早上 / 下午 / 晚上後解除。適合短暫疲勞、臨時限制。 |
| `days` | 天數(days) | 經過指定天數後解除。適合多日狀態。 |
| `until_event` | 直到事件(until_event) | 直到特定劇情事件處理後解除；事件 ID 或條件先寫在 effect / description 中，本期不解析。 |
| `until_cleared` | 直到解除(until_cleared) | 持續到某個 clear_rule 被觸發。適合需玩家行動解除的狀態。 |
| `permanent` | 永久(permanent) | 長期狀態；必須填 `permanent_reason`，通常只能由劇情或系統明確解除。 |

UI caption 常駐顯示目前選項說明，避免使用者只看到英文 enum。

規則：

- `duration.type == "permanent"` 時，`duration.value` 存 `None`，`permanent_reason` 必填。
- `duration.type ∈ {"time_slots", "days"}` 時，UI **顯示** `duration.value` number_input 並必填正整數。
- `duration.type ∈ {"until_event", "until_cleared"}` 時，UI **不顯示** `duration.value` widget，存 `None`；若舊資料帶數字 value 則 normalize 為 `None`。
- `duration.type != "permanent"` 時，若 `clear_rule` 空，阻止儲存。
- 非永久狀態若 `clear_rule == ["manual_only"]`，允許儲存但顯示 warning，與 UIW Linter 的 `hard_to_clear` warning 對齊。
- `duration.type ∈ {"until_event", "until_cleared"}` 但 `duration.value` 非 `None` 時，UIW Linter 補一條 `unexpected_duration_value` warning（不擋匯出）。

##### `effect` YAML 編輯

`effect` 不做 key/value 單列 UI，因為會破壞既有或匯入資料中的多筆效果。1-G-8 採 YAML text_area：

```yaml
- block_time_slot: evening
- stat_multiplier:
    stat: CHA
    value: 0.8
```

儲存時以 `yaml.safe_load` 解析並驗證：

- 函式開頭必須先擋 `isinstance(yaml_str, str)`：非字串輸入（MagicMock、None、dict、list、int…）一律 `raise ValueError`。**這是 runtime 防線**：PyYAML `Reader` 會把任何有 `.read` 屬性的物件當 file-like，呼叫 `stream.read(N)` 直到拿到空字串視為 EOF；MagicMock 等 truthy 物件永遠回非空 MagicMock，會陷入死迴圈卡死整個 process。type hint 是 `str` 不夠，必須在 runtime 也擋下。
- 必須是非空 list。
- list 每個元素必須是非空 dict。
- dict key 必須是非空字串。
- 不解析 dict value 的 domain 語意；Phase 3 Status Manager 再處理。

解析失敗或形狀不合法時，不寫回 canonical，保留使用者輸入並顯示錯誤。

##### 狀態刪除防呆

刪除 status 前需掃描目前 Setup Package 內部引用：

- `endings[].required_stats`
- `characters[].schedule[].condition`
- `characters[].deletion_check`：1-G-5 角色刪除防呆比對 `status_flags[].targets`（含 NPC `character_id`）。

引用判斷採 **word-boundary regex**（不可 substring 比對）：

```python
STATUS_QUALIFIED_PATTERN = re.compile(r"\bstatus\.([a-zA-Z0-9_]+)\.([a-zA-Z0-9_]+)\b")
STATUS_BARE_PATTERN = re.compile(r"\bstatus\.([a-zA-Z0-9_]+)\b")

def _has_status_reference(text: str, status_id: str) -> bool:
    if any(m.group(2) == status_id for m in STATUS_QUALIFIED_PATTERN.finditer(text)):
        return True
    # bare 形式須排除已被 qualified pattern 覆蓋的位置
    qualified_spans = [m.span() for m in STATUS_QUALIFIED_PATTERN.finditer(text)]
    for m in STATUS_BARE_PATTERN.finditer(text):
        if m.group(1) != status_id:
            continue
        if any(qs <= m.start() < qe for qs, qe in qualified_spans):
            continue
        return True
    return False
```

匹配兩種寫法：

- `status.<target_id>.<status_id>`（指定對象）
- `status.<status_id>`（不指定對象，由 runtime 套到當前 actor）

若找到引用，阻止刪除並列出引用位置。沒有引用才刪除，並呼叫 `_purge_status_state(status_id)` 清空 `*_<status_id>` keyed state。

##### 1-G-5 / 1-G-6 同步更新（schema migration 連動）

1-G-8 的 `target → targets` 變更會影響其他頁面既有引用比對：

- **1-G-5 角色刪除防呆**（[streamlit_uiw.py 角色刪除流程]）：原本比對 `status_flags[].target == 角色 character_id`，須改為「`status_flags[].targets` 包含該 `character_id`」。比對前 status 必須已經過 normalize（`model_validator(mode="before")` 處理過），不可同時遇到 `target` 與 `targets` 兩種形狀。
- **1-G-6 結局頁、1-G-4 地點頁等其他可能掃描 `status_flags[].target` 的位置**：實作前以 `Grep target` 掃過確認無遺漏，全部改為 `targets`。
- 同步更新範圍視為 1-G-8 內部工作，不另列 phase。

##### Streamlit 實作限制

- 禁止 nested `st.expander`。
- 每筆 flag widget key 採 prefix + 後綴：`flag_<field>_<flag_id>`（例 `flag_desc_overworked`）。
- 每筆 status widget key 採 prefix + 後綴：`status_<field>_<status_id>`（例 `status_targets_overworked`）。
- prefix 隔離：flag 與 status 即使後綴 ID 撞名（理論上 1-G-7 已擋，但保守處理），不可發生 widget key collision。
- 多筆 flag / status 同時展開不可 duplicate widget key。
- `flag_id` / `status_id` 唯讀，不做 rename。
- 新增 flag / status 表單保留 1-G-7 自動 ID 行為與 `_make_unique_id` / `_generate_system_id` 簽章不變。
- 新增 status 表單也使用 `targets` 複選，不再使用單一 `target` text_input。

##### UIW Linter 新增 issue types

`_check_status_flags` 補入：

| issue type | severity | path | message 範例 |
| :--- | :--- | :--- | :--- |
| `unknown_status_target` | error | `status_flags[i].targets[j]` | `狀態旗標 <status_id> 的 targets 引用了不存在的對象：<raw_id>。` |
| `empty_status_targets` | error | `status_flags[i].targets` | `狀態旗標 <status_id> 的 targets 不可為空。` |
| `unexpected_duration_value` | warning | `status_flags[i].duration.value` | `狀態旗標 <status_id> 的 duration.type=<type> 不需要 value，請留空。` |

注意：

- `unknown_status_target` 比對範圍為 `{"protagonist"} ∪ {ch.character_id for ch in package.characters}`；不複用 endings 既有 `unknown_target_character`，因為 path 與訊息語境不同。
- `empty_status_targets` 與 schema `Field(min_length=1)` 重複；schema 是最後一道防線，UIW Linter 提供使用者更友善的訊息與 path。
- 新增 issue type 須補 `tests/test_uiw_linter.py` 對應 case。

##### 測試要求

應分三層覆蓋：純函式單測（必跑、不依賴 Streamlit AppTest）、UI 行為測試（AppTest）、回歸測試。

**純函式單測**（必跑、ROI 最高）：

- `_normalize_legacy_status({"target": "protagonist", ...})` → `{"targets": ["protagonist"], ...}`，不殘留 `target` key。
- `_normalize_legacy_status({"target": "", ...})` → `{"targets": [], ...}`（保留給 schema `min_length=1` 拒絕）。
- `_normalize_legacy_status({"targets": ["a"], "target": "b", ...})` → 不覆寫已有 `targets`（新格式優先）。
- `_parse_effect_yaml("")` → 拒絕（None 不是 list）。
- `_parse_effect_yaml("foo: bar")` → 拒絕（dict 不是 list）。
- `_parse_effect_yaml("- foo")` → 拒絕（list 元素非 dict）。
- `_parse_effect_yaml("- 123: x")` → 拒絕（dict key 非 str）。
- `_parse_effect_yaml(MagicMock())` → 立刻拒絕（避免 PyYAML Reader 死迴圈）。
- `_parse_effect_yaml(None)` / `_parse_effect_yaml(123)` / `_parse_effect_yaml([])` / `_parse_effect_yaml({"foo": "bar"})` → 全部拒絕（非字串輸入 type guard）。
- `_parse_effect_yaml("- block_time_slot: evening\n- stat_multiplier:\n    stat: CHA\n    value: 0.8")` → 兩筆合法。
- 合法 round-trip：`yaml.safe_load(yaml.safe_dump(parsed)) == parsed`。
- `_status_targets_options(characters=[{"character_id": "sophie", "display_name": "蘇菲"}], current_targets=[])` 結果首項為 `("主角(protagonist)", "protagonist")`，第二項為 `("蘇菲(sophie)", "sophie")`。
- `_status_targets_options(characters=[{"character_id": "protagonist", "display_name": "怪資料"}], current_targets=[])` 只回一項 `protagonist`（dedupe）。
- `_status_targets_options(characters=[], current_targets=["dangling_x"])` 含 `("dangling_x(角色已刪除)", "dangling_x")`。
- `_has_flag_reference("not flag.over and flag.over_x", "over")` → True。
- `_has_flag_reference("flag.over_x only", "over")` → False（word boundary）。
- `_has_status_reference("status.protagonist.overworked", "overworked")` → True（qualified）。
- `_has_status_reference("status.overworked == true", "overworked")` → True（bare）。
- `_has_status_reference("status.overworked_x", "overworked")` → False（word boundary）。

**Schema / parser / exporter / linter**：

- `StatusFlag(targets=["protagonist", "sophie"])` 合法。
- `StatusFlag(targets=[])` 由 schema `Field(min_length=1)` 拋 `ValidationError`。
- `StatusFlag(target="protagonist", label=..., effect=..., duration=..., clear_rule=..., description=...)`（舊格式直接 kwarg）由 `model_validator(mode="before")` 正規化為 `targets=["protagonist"]`，不拋例外。
- 既有 `tests/fixtures/setup_minimal.yaml`（已遷移為 `targets:`）通過 schema validation。
- 新增 `tests/fixtures/setup_legacy_status_target.yaml`（保留舊 `target:`）能被 parser 載入並 normalize 為 `targets: ["protagonist"]`。
- exporter roundtrip 一律輸出 `targets`，不再輸出 `target`。
- multi-targets `["protagonist", "sophie"]` export → parse 後仍為兩元素 list，順序保留。
- UIW Linter 對 `targets=["unknown_id"]` 報 `unknown_status_target`。
- UIW Linter 對 `duration.type ∈ {"until_event", "until_cleared"}` 但 `value=3` 報 `unexpected_duration_value`（warning，不擋匯出）。
- UIW Linter 對 invalid / duplicate `status_id` 仍正常擋下（1-G-7 收尾的回歸覆蓋）。

**UI 行為（Streamlit AppTest）**：

- flag inline 編輯儲存後 canonical 更新；取消不寫回，下次開啟回到原值。
- flag 刪除若被 ending `required_flags` / `forbidden_flags` / schedule `condition` 引用（word boundary 命中）則阻止；列出引用位置。
- status inline 編輯儲存後 canonical 更新；取消不寫回。
- status `targets` multiselect 選項含「主角(protagonist)」與所有 NPC，底層保存 ID list。
- 多選 `targets` 含 dangling target 時保留原 raw ID 為額外選項；取消勾選即移除。
- `duration.type` UI 顯示中文(英文)，並對每個選項提供說明 caption。
- `duration.type ∈ {"time_slots", "days"}` 顯示 `value` widget；`until_event` / `until_cleared` 不顯示且儲存為 None；`permanent` 同上 + 必填 `permanent_reason`。
- permanent status 缺 `permanent_reason` 不儲存，保留輸入並顯示 error。
- effect YAML 非 `list[dict]`、空 list、語法錯誤時不儲存。
- 多筆 effect roundtrip 不遺失，順序保留。
- status 刪除若被 `required_stats` 或 schedule `condition` 引用（兩種 status 寫法皆命中）則阻止。
- 多筆 flag/status 同時開啟 expander 不產生 duplicate widget key（widget key 帶 `flag_<field>_<flag_id>` / `status_<field>_<status_id>` prefix）。
- 至少同時展開 2 flag + 2 status，斷言所有 widget key 不重複。
- 刪除 flag/status 後清空 `*_<flag_id>` / `*_<status_id>` keyed session state。
- 新增 status 表單使用 `targets` 複選，不再有 `target` text_input。

**Silent migration 端對端鏈路**：

- 載入 `setup_legacy_status_target.yaml`（含 `target: protagonist`）→ 進入 UI 編輯任一欄位（如 `description`）→ 儲存 → 匯出 → 重新 parse → 確認結果為 `targets: ["protagonist"]`，不殘留 `target` key。

**回歸**：

- 1-G-7 自動 ID 新增流程不退化；`_make_unique_id` / `_generate_system_id` 簽章與行為與 1-G-7 完成時一致（必要時加 `inspect.signature` 斷言）。
- 1-G-6 結局頁 F-γ flag multiselect 不退化。
- 1-G-5 角色刪除防呆對 status 引用比對改用 `targets` 後仍能阻擋被引用的角色刪除。
- 最小可用 Setup Package（flags=[] / status_flags=[]）仍可 export → parse roundtrip。
- Phase 1-A 到 1-G-7 全測試通過。
- 實機 end-to-end：啟動 UIW、走完最小可用設定 + 至少一個 flag + 一個 status（含複數 targets）、export → parser roundtrip 一致。

##### 不變項

- `flag_id` / `status_id` 仍為英文 canonical ID。
- 旗標與狀態新增流程仍不要求一般使用者輸入英文 ID。
- status 的 runtime 套用語法仍可使用 `status.<target_id>.<status_id>` 指向單一對象。
- 不做 Event Blueprint / Scene Draft 外部引用 migration。
- `status_id` linter 補洞已先行修復；1-G-8 不重複實作，只納入回歸驗收。

---

## Phase 2：Event Blueprint MVP

Phase 2 目標是讓 AI 或 prompt 產出的事件骨架有固定格式，且能被驗證。

---

### 2-A Event Blueprint Schema

#### 要建的檔案

1. `src/sandbox_dating_sim/schema/blueprint.py`
2. `tests/test_blueprint_schema.py`

#### 主要模型

```python
class BlueprintChoice(BaseModel):
    choice_id: str
    choice_intent: str
    result: list[str]

class ExpectedCharacterAsset(BaseModel):
    character_id: str
    costume: str
    emotion: str
    position: str

class ExpectedAssets(BaseModel):
    background: str | None = None
    bgm: str | None = None
    characters: list[ExpectedCharacterAsset] = []

class BlueprintEvent(BaseModel):
    event_id: str
    title: str
    location_id: str
    time_slot: str
    priority: str
    route_tags: list[str] = []
    conditions: list[str]
    event_purpose: str
    cast: list[str] = []
    expected_assets: ExpectedAssets | None = None
    choices: list[BlueprintChoice]
    time_cost: int

class EventBlueprint(BaseModel):
    event_blueprint_version: str = "1.0"
    source_setup_package: str
    target_game_spec_version: str = "1.2"
    events: list[BlueprintEvent]
    new_flags_proposed: list[FlagDef] = []
```

---

### 2-B Event Blueprint Prompt Builder

#### 要建的檔案

1. `src/sandbox_dating_sim/prompts/blueprint_prompt.py`
2. `tests/test_blueprint_prompt.py`

#### 功能

```python
def build_event_blueprint_prompt(setup_package_markdown: str) -> str:
    """
    產生給 AI 的 prompt。
    要求 AI 只生成 Event Blueprint，不生成完整對話。
    """
    ...
```

Prompt 必須明確限制：

- 只使用 Setup Package 中的角色與地點。
- 不生成完整對話。
- 每個事件都要有 conditions、choices、result、time_cost。
- 新 Flag 必須列入 `new_flags_proposed`。
- Event Blueprint 是邏輯合約。

---

### 2-C Event Blueprint Parser / Linter

#### 要建的檔案

1. `src/sandbox_dating_sim/pipeline/blueprint_parser.py`
2. `src/sandbox_dating_sim/validation/blueprint_linter.py`
3. `tests/test_blueprint_linter.py`

#### Linter 必做

- event_id 合法且不重複。
- location_id 存在於 Setup Package。
- cast 角色存在。
- expected assets 不超出角色白名單。
- choice_id 合法且不重複。
- result 不引用未知角色。
- result 中新增 flag 必須已存在或列於 `new_flags_proposed`。
- goto 必須存在或為 `free_roam`。
- time_cost > 0。

---

## Phase 3：Map Manager、Status Manager 與 Route Validator

Phase 3 才開始處理路線驗證與 free_roam。

### 3-A Status Manager

#### 要建的檔案

1. `src/sandbox_dating_sim/status/manager.py`
2. `tests/test_status_manager.py`

#### 核心職責

- 套用 status flag。
- 根據 `duration` 與 `clear_rule` 解除 status flag。
- 禁止無 lifecycle 的負面狀態進入 runtime state。

### 3-B Map Manager

#### 要建的檔案

1. `src/sandbox_dating_sim/map/manager.py`
2. `tests/test_map_manager.py`

#### 核心職責

- 根據 day、time_slot、location、schedule 計算 NPC 分佈。
- 解析 `goto: free_roam`。
- 使用 Schedule Conflict Resolution 處理同優先權行程。
- 輸出 FreeRoamState。

### 3-C Route Validator

#### 要建的檔案

1. `src/sandbox_dating_sim/validation/route_validator.py`
2. `tests/test_route_validator.py`

#### 核心職責

- 從 Event Blueprint 建狀態圖。
- 檢查不可達事件、死路、結局不可達。
- 支援 Exhaustive Mode、Bounded Mode、Critical Path Mode、Character Route Mode。

---

## Phase 4：Scene Draft 與 Final Script

Phase 4 目標是把已通過驗證的 Event Blueprint 拆成單事件或小批次，生成對話、旁白與選項文字。

### 4-A Scene Draft Schema

#### 要建的檔案

1. `src/sandbox_dating_sim/schema/scene.py`
2. `tests/test_scene_schema.py`

Scene Draft 可包含對話與演出，但不得修改 Event Blueprint 的邏輯。

### 4-B Scene Draft Validator

#### 要建的檔案

1. `src/sandbox_dating_sim/validation/scene_linter.py`
2. `tests/test_scene_linter.py`

必做檢查：

- choice_id 與 Event Blueprint 一致。
- result 未被修改。
- time_cost 未被修改。
- goto 未被修改。
- 素材標籤在白名單內。

### 4-C Final Script Assembly

#### 要建的檔案

1. `src/sandbox_dating_sim/pipeline/final_script.py`
2. `tests/test_final_script.py`

將通過驗證的 Scene Draft 組合成最終 Markdown DSL。

---

## Phase 5：Dashboard 與 UI

Phase 5 才開始做正式 UI。

可選技術：

- Streamlit，適合快速工具型 Dashboard。
- React / Vue，適合長期產品化。

建議先用 Streamlit 或 CLI + Markdown 完成前四個 Phase，再決定 UI 技術。

### 5-A User Input Wizard UI

功能：

- 表單建立 World / Protagonist / Locations / Characters / Flags / Endings。
- 顯示中文 label。
- 輸出英文 canonical ID。
- 可預覽 Setup Package MD。

### 5-B Validation Dashboard

功能：

- 顯示 UIW Linter 結果。
- 顯示 Event Blueprint Linter 結果。
- 顯示 Route Validator 結果。
- 點擊 issue 跳到對應事件。

### 5-C Flowchart

功能：

- 顯示事件節點。
- 顯示 choice 分支。
- 顯示 free_roam 後地點節點。
- 標示 unreachable / warning / error。

---

## Phase 6：AI Provider 整合

Phase 6 才接入真實 LLM API。

### 6-A Provider-neutral AI Client

#### 建議檔案

```text
src/sandbox_dating_sim/ai/
  __init__.py
  provider_base.py
  openai_provider.py
  anthropic_provider.py
  gemini_provider.py
```

核心原則：

- 生成管線不直接綁定單一 provider。
- Phase 2 的 prompt builder 應可先手動複製到外部 AI 使用。
- 接 API 後，也必須把 AI 輸出交給 parser 與 linter，不可直接信任。

---

## Phase 7：資產管理

目標：

- 從 Scene Draft / Final Script 擷取素材需求。
- 檢查角色表情、服裝、位置是否超出白名單。
- 產出 Asset Request MD。
- 支援圖片命名與缺漏報告。

---

## 開發順序建議

嚴格按以下順序：

```text
Phase 0
-> Phase 1-A
-> Phase 1-B
-> Phase 1-C
-> Phase 1-D
-> Phase 1-E
-> Phase 1-F
-> Phase 1-G
-> Phase 2
-> Phase 3
-> Phase 4
-> Phase 5+
```

目前最應該實作的是：

```text
Phase 1：Setup Package MVP
```

Phase 1 完成的定義：

- 能讀取 fixture YAML。
- 能載入為 `SetupPackage`。
- 能跑 UIW Linter。
- 能輸出 `<world_id>_setup_package.md`。
- 輸出的 MD 只有一個主要 YAML block。
- 能 parse 回 `SetupPackage`。
- 能透過 Interactive UIW Prototype 手動填寫/選擇並輸出 Setup Package MD。
- roundtrip 測試通過。

---

## Phase 1 最小 Fixture 範圍

`tests/fixtures/setup_minimal.yaml` 至少包含：

- 1 個 world
- 1 個 protagonist
- 2 個 location
  - 1 個 container
  - 1 個 sub_location 或 standalone
- 1 個 character
- 1 條 schedule
- 1 個 flag
- 1 個 status_flag，含 duration 與 clear_rule
- 1 個 ending
- emotion / costume / position 白名單
- alias table

---

## 實作者注意事項

1. 不要先做完整 UI。
2. 不要先接 AI API。
3. 不要先做完整遊戲引擎。
4. 不要讓 Markdown 成為內部唯一資料來源。
5. 不要讓 AI 修改已通過驗證的 Event Blueprint 邏輯。
6. Phase 1 的價值是證明資料模型、驗證與 Setup Package MD 可以穩定 roundtrip。

只要 Phase 1 跑通，後續 Event Blueprint、Route Validator 與 Scene Draft 都能沿同一套資料契約往下接。
