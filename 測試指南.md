# SandboxDatingSimDev 測試指南

## 版本紀錄

| 版本 | 日期 | 內容 |
| :--- | :--- | :--- |
| v1.10 | 2026-05-03 | 補強 Phase 1-G-7 測試要求：補 `_slugify_label` / `_make_unique_id` 純函式單測為必跑項目；suffix 連鎖測試擴充至 `_3`、`_4` 並挑邊「順序遞增取最小未占用」；驗收 #6/#7/#13 改寫為可實際操作的步驟；新增驗收 #14（自動 ID 失敗保留 draft）、#15（submit 後 caption 預覽）、#16（1-G-3 `_generate_system_id` 行為不變回歸）、#17（end-to-end UIW → export → parse roundtrip）；模板衝突挑邊為「自動 suffix 避讓」；補進階手動 ID 擋 `protagonist`。 |
| v1.9 | 2026-05-03 | 修正 Phase 1-G-7 測試要求：旗標來源欄位限定為 `description`，補 description slug 截斷、中文標點、`unnamed` fallback、結局純拼音範例與不得修改 `_generate_system_id` 既有行為。 |
| v1.8 | 2026-05-03 | 新增 Phase 1-G-7 測試要求：地點 / NPC / 旗標 / 狀態 / 結局新增流程自動產生唯一 canonical ID，覆蓋中文來源、合法格式、跨類型唯一、同名遞增 suffix、匯出 canonical ID 不變。 |
| v1.7 | 2026-05-01 | 新增 Phase 1-G-6 測試要求：分頁拆分、結局清單顯示、inline expander 編輯、priority 與 target 中文化、F-γ multiselect、跨類型 ID 唯一即時擋（UI 層含 `status_id`）、最小可用 Setup Package 匯出 roundtrip；附帶補測 1-G-5 character_id 跨類型唯一即時擋。 |
| v1.6 | 2026-05-01 | 補強 Phase 1-G-5 Streamlit 實作限制相關測試：禁止 nested `st.expander`、多角色同時編輯不可 duplicate widget key、新增角色擋空/重複/非法 `character_id`、編輯期不可改 `character_id`、刪除角色清空相關 session state。 |
| v1.5 | 2026-05-01 | 新增 Phase 1-G-5 測試要求：角色清單顯示、刪除引用防呆、inline expander 編輯、行程清單與新增搬入編輯區、行程 enum 中文化、白名單顯示與進階設定收納；明確 1-G-5 不顯示 `specific_date` 行程選項。 |
| v1.4 | 2026-05-01 | 修正 Phase 1-G-4 測試規格：區分 container 與可進入模板的 time slot 要求、補手動驗收前置條件，並加入 Phase 1-A 到 1-G-3 回歸門檻。 |
| v1.3 | 2026-05-01 | 新增 Phase 1-G-4 測試要求：地點與角色頁順序、地點模板擴充、模板 ID 防重、地點刪除引用檢查、中文化地點類型與時段選項、地點 tags 進階設定。 |
| v1.2 | 2026-05-01 | 補充 Phase 1-G-3 驗收結果與「沒有秘密」canonical 行為：UI 清空 secrets list，輸出 `secrets: []`。 |
| v1.1 | 2026-05-01 | 新增 Phase 1-G-3 測試要求：語意型欄位 `id + label`、主角與 NPC 秘密複選、職業/秘密預設擴充、中文自訂輸入與系統 ID 驗證。 |
| v1.0 | 2026-04-27 | 初版，定義 Phase 0 與 Phase 1-A 到 1-F 的測試流程、執行指令、通過標準與手動驗收清單。 |

> **目的：** 本文件定義 SandboxDatingSimDev 每個 Phase / 子階段的測試流程。
> 包含：環境準備、執行指令、預期結果、通過/失敗判定標準、故障排除。
> **每個子階段測試全數通過，才能進入下一個子階段。**

---

## 測試環境準備

### Step 1：安裝依賴

```powershell
cd "C:\Users\User\OneDrive\桌面\AI_Work\ProjectGpt\SandboxDatingSimDev"
uv sync --extra dev
```

若尚未安裝 `uv`：

```powershell
irm https://astral.sh/uv/install.ps1 | iex
```

驗證：

```powershell
uv --version
uv run python -c "import pydantic, yaml, typer, rich; print('ALL OK')"
```

預期輸出：

```text
ALL OK
```

### Step 2：確認 pytest 設定

`pyproject.toml` 應包含：

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "integration: 需要外部 API、瀏覽器或網路連線的測試",
]
```

Phase 1 不需要外部 AI API。

---

## 測試指令速查表

| 用途 | 指令 |
| :--- | :--- |
| 跑全部單元測試 | `uv run pytest tests/ -v -m "not integration"` |
| 跑全部測試 | `uv run pytest tests/ -v` |
| 跑指定測試檔 | `uv run pytest tests/test_setup_exporter.py -v` |
| 跑單一測試 | `uv run pytest tests/test_setup_parser.py::test_parse_exported_markdown_roundtrip -v` |
| 顯示簡短錯誤 | `uv run pytest tests/ -v --tb=short` |
| 顯示完整錯誤 | `uv run pytest tests/ -v --tb=long` |
| 只收集測試 | `uv run pytest tests/ --collect-only` |
| 覆蓋率 | `uv run pytest tests/ --cov=src --cov-report=term-missing -m "not integration"` |

---

## 測試分層原則

### Schema Validation

Pydantic schema validation 負責資料形狀與基本型別。

應由 schema 測試覆蓋：

- 缺 required field。
- 欄位型別錯誤。
- 巢狀資料結構錯誤。
- enum 值不合法。
- 必填欄位完全不存在。

範例：

```python
def test_schema_rejects_missing_schedule_order():
    """schedule_order 缺欄位時，Pydantic validation fail。"""
```

### UIW Linter

UIW Linter 負責語意、引用與跨欄位邏輯。

應由 linter 測試覆蓋：

- ID 重複。
- ID 格式不合法。
- 欄位存在但值為空陣列或空字串。
- schedule 引用不存在地點。
- container 沒有任何可進入 sub_location。
- ending 引用不存在角色。
- ending 條件引用未宣告 flag。
- status flag 的 `clear_rule` 存在但為空陣列。
- `duration.type: permanent` 但沒有 `permanent_reason`。
- schedule 同角色、同時間段、同 priority 且條件可能同時成立。

範例：

```python
def test_linter_rejects_empty_clear_rule():
    """clear_rule 欄位存在但為空陣列時，由 UIW Linter 回報 issue。"""
```

不要在 UIW Linter 測試中期待「缺 required field」能進入 linter。這類錯誤應由 schema 測試覆蓋。

---

## Phase 0：專案骨架與基礎工具

### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/core/ids.py` | `tests/test_ids.py` |

### 前置條件

- [ ] `pyproject.toml` 已建立。
- [ ] `src/sandbox_dating_sim/core/constants.py` 已建立。
- [ ] `src/sandbox_dating_sim/core/exceptions.py` 已建立。
- [ ] `src/sandbox_dating_sim/core/ids.py` 已建立。

### 執行指令

```powershell
uv run pytest tests/test_ids.py -v
```

預期：

```text
tests/test_ids.py::test_valid_id PASSED
tests/test_ids.py::test_invalid_id_starts_with_number PASSED
tests/test_ids.py::test_invalid_id_contains_chinese PASSED
tests/test_ids.py::test_normalize_id PASSED
```

### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| `tests/test_ids.py` 全部 PASSED | 必須 |
| 中文 ID 不得被自動音譯 | 必須 |

---

## Phase 1：Setup Package MVP

Phase 1 的核心目標：

```text
fixture YAML
-> SetupPackage schema
-> UIW Linter
-> Setup Package MD export
-> Setup Package MD parse
-> roundtrip
-> 1-F 表單原型輸出同樣格式
```

1-F 前置門檻：

> **Phase 1-A 到 1-E 的單元測試必須先全數通過，才能開始 1-F。**

理由：1-F 會輸出複雜巢狀 YAML。如果 parser、exporter 與 roundtrip 尚未穩定，UI 問題會和資料問題混在一起，debug 成本會很高。

---

### 1-A Canonical Setup Schema

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/schema/setup.py` | `tests/test_setup_schema.py` |
| `src/sandbox_dating_sim/schema/validation.py` | `tests/test_setup_schema.py` |

#### 固定資料

| Fixture | 用途 |
| :--- | :--- |
| `tests/fixtures/setup_minimal.yaml` | 合法最小設定 |
| `tests/fixtures/setup_invalid_missing_required.yaml` | 缺 required field |

#### 執行指令

```powershell
uv run pytest tests/test_setup_schema.py -v
```

#### 必測項目

```python
def test_minimal_setup_package_loads():
    """setup_minimal.yaml 可載入為 SetupPackage。"""

def test_schema_rejects_missing_character_id():
    """角色缺 character_id 時 Pydantic validation fail。"""

def test_schema_rejects_missing_schedule_order():
    """schedule 缺 schedule_order 時 Pydantic validation fail。"""

def test_schema_rejects_missing_status_duration():
    """status flag 缺 duration 時 Pydantic validation fail。"""

def test_schema_rejects_missing_clear_rule():
    """status flag 缺 clear_rule 時 Pydantic validation fail。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| 合法 fixture 可載入 | 必須 |
| 缺 required field 由 Pydantic 擋下 | 必須 |
| `uiw_version == "1.2"` | 必須 |

---

### 1-B UIW Linter

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/uiw/linter.py` | `tests/test_uiw_linter.py` |

#### 執行指令

```powershell
uv run pytest tests/test_uiw_linter.py -v
```

#### 必測項目

```python
def test_linter_passes_minimal_fixture():
    """setup_minimal.yaml 應通過。"""

def test_linter_rejects_duplicate_character_id():
    """重複 character_id 應產生 error。"""

def test_linter_rejects_unknown_schedule_location():
    """schedule location_id 不存在應產生 error。"""

def test_linter_rejects_container_without_sub_location():
    """container 沒有可進入 sub_location 應產生 error。"""

def test_linter_rejects_empty_clear_rule():
    """clear_rule 欄位存在但為空陣列時應產生 error。"""

def test_linter_rejects_permanent_without_reason:
    """duration.type=permanent 但缺 permanent_reason 應產生 error。"""

def test_linter_detects_schedule_conflict():
    """同角色同時間段同 priority 且條件可能同時成立時應產生 issue。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| 合法 fixture `status: passed` | 必須 |
| 語意錯誤產生 `ValidationReport.issues` | 必須 |
| issue 至少包含 `severity`、`type`、`path`、`message` | 必須 |

---

### 1-C Setup Package MD Exporter

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/pipeline/markdown.py` | `tests/test_setup_exporter.py` |
| `src/sandbox_dating_sim/pipeline/setup_exporter.py` | `tests/test_setup_exporter.py` |

#### 執行指令

```powershell
uv run pytest tests/test_setup_exporter.py -v
```

#### 必測項目

```python
def test_exporter_outputs_markdown_with_single_yaml_block():
    """輸出包含 # Setup Package，且只有一個主要 yaml code block。"""

def test_exporter_includes_validation_report():
    """輸出 YAML 包含 validation_report。"""

def test_exporter_writes_uiw_version_1_2:
    """輸出 YAML 的 uiw_version 必須是 1.2。"""

def test_write_file_uses_world_id_filename(tmp_path):
    """輸出檔名為 <world_id>_setup_package.md。"""

def test_exporter_preserves_chinese_labels():
    """中文 label 在 YAML 中保留，canonical ID 不被中文取代。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Markdown 可讀 | 必須 |
| YAML 可 parse | 必須 |
| `validation_report` 必定存在 | 必須 |
| `uiw_version` 必定為 `"1.2"` | 必須 |

---

### 1-D Setup Package Parser 與 Roundtrip

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/pipeline/setup_parser.py` | `tests/test_setup_parser.py` |
| `src/sandbox_dating_sim/pipeline/markdown.py` | `tests/test_setup_parser.py` |

#### 執行指令

```powershell
uv run pytest tests/test_setup_parser.py -v
```

#### 必測項目

```python
def test_parse_exported_markdown_roundtrip():
    """SetupPackage -> Markdown -> SetupPackage 後核心欄位一致。"""

def test_parse_rejects_missing_yaml_block():
    """沒有 yaml code block 時拋出 MarkdownParseError。"""

def test_parse_rejects_invalid_schema():
    """YAML 欄位不完整時 validation fail。"""

def test_roundtrip_preserves_nested_locations_and_schedule():
    """巢狀 locations 與 character schedule 在 roundtrip 後一致。"""

def test_roundtrip_preserves_status_flag_lifecycle():
    """duration、clear_rule、permanent_reason 在 roundtrip 後一致。"""

def test_roundtrip_preserves_asset_vocabularies_and_aliases():
    """asset vocabularies 與 alias tables 在 roundtrip 後一致。"""
```

#### Parser 穩健性要求

Parser 必須：

- 只讀取指定的主要 YAML code block。
- 遇到沒有 YAML block 時明確失敗。
- 遇到多個 YAML block 時採用第一個主要 block，或明確要求 exporter 只輸出一個 block。
- 保留中文 label。
- 保留 list 順序。
- 保留 `schedule_order`。
- 保留巢狀資料，例如 `characters[].schedule`。

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| roundtrip 核心欄位一致 | 必須 |
| 巢狀 Location / Schedule roundtrip 一致 | 必須 |
| Status lifecycle roundtrip 一致 | 必須 |
| 中文 label 不遺失 | 必須 |

---

### 1-E CLI：產出 Setup Package MD

#### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/cli.py` | `tests/test_cli.py` |

#### 執行指令

```powershell
uv run pytest tests/test_cli.py -v
```

手動 smoke test：

```powershell
uv run python -m sandbox_dating_sim.cli export-setup tests/fixtures/setup_minimal.yaml --out examples/
```

預期產物：

```text
examples/<world_id>_setup_package.md
```

#### 必測項目

```python
def test_cli_export_setup_creates_file(tmp_path):
    """CLI 可從 fixture YAML 產出 Setup Package MD。"""

def test_cli_export_setup_output_can_parse(tmp_path):
    """CLI 產出的 MD 可被 SetupPackageParser 讀回。"""

def test_cli_invalid_input_returns_nonzero(tmp_path):
    """不合法 input 應回傳非 0 或丟出清楚錯誤。"""
```

#### 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| CLI 能產生檔案 | 必須 |
| 產物可 parse 回 `SetupPackage` | 必須 |
| invalid input 不產生半壞輸出 | 必須 |

---

### Phase 1-A 到 1-E 回歸測試

1-F 開始前必跑：

```powershell
uv run pytest `
  tests/test_ids.py `
  tests/test_setup_schema.py `
  tests/test_uiw_linter.py `
  tests/test_setup_exporter.py `
  tests/test_setup_parser.py `
  tests/test_cli.py `
  -v -m "not integration" --tb=short
```

通過標準：

| 條件 | 必要性 |
| :--- | :--- |
| 全部 PASSED | 必須 |
| 無 parser roundtrip 失敗 | 必須 |
| CLI 產物可 parse | 必須 |

> 只有這一關通過，才進入 1-F。

---

### 1-F Interactive UIW Prototype

#### 測試方式

1-F 包含 Streamlit UI，因此採用：

- `tests/test_uiw_defaults.py` 自動測試 defaults。
- Phase 1-A 到 1-E 自動測試保護資料契約。
- 手動驗收清單驗證 UI 行為。

#### 自動測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/uiw/defaults.py` | `tests/test_uiw_defaults.py` |

#### 執行指令

```powershell
uv run pytest tests/test_uiw_defaults.py -v
```

#### 必測項目

```python
def test_style_presets_have_id_and_label():
    """每個 style preset 都有 id 與中文 label。"""

def test_location_templates_have_required_fields():
    """每個 location template 都有 template_id、label、location_type。"""

def test_container_templates_suggest_sub_locations():
    """container template 必須提供 suggested_sub_locations。"""

def test_template_ids_are_unique():
    """template_id 不可重複。"""

def test_template_ids_are_valid_canonical_ids():
    """template_id 與建議 location_id 必須符合 ID 規則。"""
```

#### 啟動指令

```powershell
uv run streamlit run src/sandbox_dating_sim/ui/streamlit_uiw.py
```

預期：

```text
Local URL: http://localhost:8501
```

#### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 無錯啟動 | 執行 Streamlit 啟動指令 | 頁面正常顯示，終端無 traceback | [ ] |
| 2 | World | 填 world_id、title、日期 | 可產生 world YAML | [ ] |
| 3 | 風格參考詞 | 點選都市戀愛、黑色幽默等 preset | `world.global_style` 加入對應英文 ID | [ ] |
| 4 | Protagonist | 填主角資料與初始數值 | 數值進入 `protagonist.initial_stats` | [ ] |
| 5 | 地點模板 | 套用「學校」模板 | 建立 container 草稿，並提示建立子地點 | [ ] |
| 6 | Container 規則 | 建立 container 但不建子地點後按 Validate | validation report 顯示錯誤 | [ ] |
| 7 | 子地點 | 在學校下新增圖書室 | `parent_location_id` 自動帶入 `school` | [ ] |
| 8 | 獨立地點 | 新增主角家 | `location_type=standalone` 且沒有 parent | [ ] |
| 9 | Character | 建立角色並選表情、服裝、位置 | 白名單輸出 canonical ID | [ ] |
| 10 | Schedule | 新增角色 schedule | location 下拉只顯示可進入地點 | [ ] |
| 11 | Status Flag | 新增 overworked | 必須填 duration 與 clear_rule | [ ] |
| 12 | Ending | 新增 ending | 可填 required_flags、required_stats、route_tags | [ ] |
| 13 | Validate | 點驗證 | 顯示 UIW Linter report | [ ] |
| 14 | YAML Preview | 進入 Review | 顯示 SetupPackage YAML，`uiw_version: 1.2` | [ ] |
| 15 | Export | 點匯出 | 產出 `<world_id>_setup_package.md` | [ ] |
| 16 | Parser Roundtrip | 用 parser 讀回匯出的 MD | 成功 parse 為 SetupPackage | [ ] |

#### 1-F 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-E 回歸全過 | 必須 |
| `tests/test_uiw_defaults.py` 全過 | 必須 |
| 手動驗收 #1-#16 全過 | 必須 |
| 匯出的 MD 可由 parser 讀回 | 必須 |

#### 1-F 範圍提醒

1-F 是 Functional Prototype。

允許：

- 樸素表單。
- Tabs / expanders。
- 表格 + 新增按鈕。
- parent 下拉選單。

不要求：

- 正式產品級 UI。
- 拖拉地圖樹。
- Flowchart。
- AI API 串接。
- 完整 Dashboard。

---

### 1-G 使用者實測回饋迭代

1-G 是 1-F Functional Prototype 後的使用者操作回饋階段。每個子階段依需求範圍補自動測試與手動驗收。

#### 1-G-3 語意型欄位 id + label

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/schema/setup.py` | `tests/test_setup_schema.py` |
| `src/sandbox_dating_sim/uiw/defaults.py` | `tests/test_uiw_defaults.py` |
| `src/sandbox_dating_sim/uiw/linter.py` | `tests/test_uiw_linter.py` |
| `src/sandbox_dating_sim/pipeline/setup_exporter.py` | `tests/test_setup_exporter.py` |
| `src/sandbox_dating_sim/pipeline/setup_parser.py` | `tests/test_setup_parser.py` |
| `src/sandbox_dating_sim/cli.py` | `tests/test_cli.py` |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py` | 手動驗收 |

##### 必測項目

- `world.global_style` 可保存 `id + label` list。
- `protagonist.occupation` 可保存 `id + label`。
- `protagonist.personality` 可保存 `id + label`。
- `protagonist.secrets` 可保存最多 3 個 `id + label`。
- `characters[].personality_tags` 可保存 `id + label` list。
- `characters[].secrets` 可保存最多 3 個 `id + label`。
- 語意型欄位 `id` 必須符合 canonical ID 規則。
- 語意型欄位 `label` 不可為空。
- `protagonist.secrets` 超過 3 個時 UIW Linter 應回報 error。
- `characters[].secrets` 超過 3 個時 UIW Linter 應回報 error。
- `none = 沒有秘密` 為 UI 清空操作；輸出應為 `secrets: []`，不得和其他秘密並存。
- `world.global_style[].id` 不可重複。
- `characters[].personality_tags[].id` 不可重複。
- exporter 輸出 `id + label`。
- parser roundtrip 不遺失中文 label。
- CLI 產物仍可 parse 回 `SetupPackage`。

##### defaults 測試

`tests/test_uiw_defaults.py` 必須驗證：

- 主角職業預設選項總數為 16。
- 新增職業包含 `rich_heir`、`startup_founder`、`tech_worker`、`finance_professional`。
- 主角與 NPC 共用的秘密預設選項新增 8 個，並保留 `none`。
- 所有預設選項都有 `id` 與 `label`。
- 所有預設選項 ID 不重複且符合 canonical ID 規則。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 職業預設 | 點選「咖啡廳店員」 | 顯示「目前選擇：咖啡廳店員」，系統 ID 為 `cafe_staff` | [ ] |
| 2 | 職業新增預設 | 點選「富二代」「新創公司老闆」「科技業」「金融業」之一 | 寫入對應 `id + label` | [ ] |
| 3 | 自訂中文職業 | 在自訂職業名稱輸入中文並按「選擇」 | 自動產生系統 ID，下方欄位可進階修改 | [ ] |
| 4 | 主角性格 | 點選或自訂中文性格 | 寫入 `protagonist.personality.id` 與 `label` | [ ] |
| 5 | 主角秘密複選 | 選 1-3 個秘密 | 寫入 `protagonist.secrets` list | [ ] |
| 6 | 主角秘密上限 | 嘗試選第 4 個秘密 | UI 阻止或 linter 回報錯誤 | [ ] |
| 7 | 沒有秘密互斥 | 選「沒有秘密」後再選其他秘密 | UI 清空 secrets list；輸出 `secrets: []`，不保存 `none` | [x] |
| 8 | 角色性格標籤 | 選預設與自訂中文 tag | 寫入 `characters[].personality_tags[].id/label` | [ ] |
| 9 | NPC 秘密複選 | 替角色選 1-3 個秘密 | 寫入 `characters[].secrets` list | [x] |
| 10 | YAML Preview | 進入 Review | 語意型欄位皆顯示 `id + label` | [ ] |
| 11 | Export / Roundtrip | 匯出 MD 後用 parser 讀回 | 中文 label 不遺失 | [ ] |

##### 1-G-3 驗證紀錄

2026-05-01 驗證結果：

- 自動測試：`67 passed`。
- Streamlit `AppTest` 抽查：角色設定頁無 exception。
- 主角職業預設「咖啡廳店員」寫入 `id + label`。
- 自訂中文職業可產生合法 canonical ID。
- 主角秘密最多保留 3 個。
- 「沒有秘密」會清空 secrets list，canonical output 為 `secrets: []`。
- 角色性格標籤與 NPC 秘密可寫入角色資料，新增角色後暫存選項會清空。

##### 1-G-3 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| 1-G-3 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#11 全過 | 必須 |
| 匯出的 MD 可由 parser 讀回且中文 label 不遺失 | 必須 |

#### 1-G-4 地點與地圖項目調整

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/uiw/defaults.py` | `tests/test_uiw_defaults.py` |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py` | 手動驗收 / Streamlit AppTest |
| `src/sandbox_dating_sim/uiw/linter.py` | 必要時補 `tests/test_uiw_linter.py` |

##### 必測項目

- 分頁順序為 World -> Protagonist -> Locations -> Characters -> Flags/Status/Endings -> Review & Export。
- 每頁「下一頁」按鈕順序與上方導覽一致。
- 原本地點模板保留，且補齊 `車站周邊`。
- 新增地點模板皆存在，template ID 與 location ID suggestion 不重複且符合 canonical ID 規則。
- container 模板會建立指定子地點。
- 每個 sub_location 與 standalone 模板有合理 `available_time_slots`；所有模板都有 AI 劇情參考用 `tags`。
- 子地點實際 `location_id` 由 `<parent_location_id>_<location_id_suffix>` 組成，避免與 standalone 模板 ID 衝突。
- 套用模板時若主地點、獨立地點或預計子地點 ID 已存在，不建立重複地點並顯示提示。
- 名稱相同或接近的模板在 UI 顯示時有脈絡或副標，例如 `便利商店(獨立)` 與 `便利商店(商店街內)`。
- 地點清單每個項目旁可直接刪除，不需展開 JSON。
- 刪除仍有子地點引用的 container 會被阻止，並提示引用的子地點。
- 刪除被角色 schedule 引用的地點會被阻止，並提示引用來源。
- `location_type` UI 顯示中文(英文)，canonical data 仍保存英文 ID。
- `available_time_slots` UI 顯示中文(英文)，canonical data 仍保存英文 ID。
- `tags` 位於進階設定，允許中文或英文描述，並清楚說明用途是提供 AI 劇情生成參考。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 導覽順序 | 啟動 UIW 查看上方導覽 | 地點與地圖在主角設定後、角色設定前 | [ ] |
| 2 | 下一頁順序 | 先完成主角必填欄位後，從主角設定按下一頁 | 進入地點與地圖 | [ ] |
| 3 | 車站周邊模板 | 套用車站周邊 | 建立 container 與月台、站前廣場、地下街 | [ ] |
| 4 | 新增 standalone 模板 | 套用主角公司、公園、醫院、夜市等任一模板 | 建立 standalone 地點 | [ ] |
| 5 | 新增 container 模板 | 套用遊樂園、百貨公司、海邊或溫泉旅館 | 建立指定子地點 | [ ] |
| 6 | 模板防重 | 連續套用同一模板兩次 | 第二次提示 ID 已存在且不新增地點 | [ ] |
| 7 | 地點類型中文顯示 | 開啟手動新增地點類型選單 | 顯示主地點/區域(container) 等文字 | [ ] |
| 8 | 時段中文顯示 | 開啟可使用時段選單 | 顯示早上(morning)、下午(afternoon)、晚上(evening) | [ ] |
| 9 | 刪除子地點防呆 | 嘗試刪除仍有子地點的父地點 | 阻止刪除並列出子地點 | [ ] |
| 10 | 刪除 schedule 引用防呆 | 建立角色 schedule 後刪除該地點 | 阻止刪除並提示引用來源 | [ ] |
| 11 | 進階 tags | 在進階設定輸入中文/英文標籤 | YAML 保存為 `locations[].tags`，且 UI 說明其為 AI 劇情參考 | [ ] |

##### 1-G-4 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| Phase 1-G-1 到 1-G-3 回歸不破壞 | 必須 |
| 1-G-4 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#11 全過 | 必須 |
| 匯出的 Setup Package 中地點資料 canonical ID 維持英文 | 必須 |

---

#### 1-G-5 角色設定頁面 UX 完整化

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py` | 手動驗收 / 必要時補 `tests/test_streamlit_uiw.py`（Streamlit `AppTest`） |

1-G-5 不應修改 schema、linter、exporter、parser；如測試需新增 fixture 僅限 UI 行為驗證。

##### 必測項目

清單顯示與刪除：

- 角色清單 expander label 顯示「{display_name} · {性別中文} · {定位中文}」，不再顯示英文 `(character_id)`。
- 角色清單每列右側可直接刪除，不需展開 expander。
- 刪除被 `endings[].target_character_id` 引用的角色被阻擋，toast 列出所有引用 `ending_id`。
- 刪除被 `status_flags[].target` 引用的角色被阻擋，toast 列出所有引用 `status_id`；`protagonist`、`global` 與空字串等特殊 target 不被誤判為角色引用。
- 多筆引用同時存在時 toast 訊息合併列出。

既有角色 inline 編輯：

- 編輯既有角色基本資訊：修改 `display_name` / `gender` / `orientation` / `role` / `identity` / `initial_favor` 並按「儲存修改」後，`st.session_state["characters"][i]` 對應欄位更新。
- 編輯既有角色性格與秘密：修改 `personality_tags` / `secrets` 後儲存，對應 keyed temp state 寫回該角色，不影響其他角色。
- 編輯既有角色受控素材白名單：修改 `allowed_emotions` / `allowed_costumes` / `allowed_positions` 後儲存，對應欄位更新；底層仍保存 `{id, label}` 結構。
- 「取消」按鈕清空該角色 keyed temp state；下次開啟 expander 重新依當前值初始化。
- 多角色同時開啟 expander 編輯，keyed temp state 不互相污染。
- `character_id` 於編輯 expander 進階設定區以唯讀文字顯示，1-G-5 不可修改。

行程管理：

- 行程 `day_type` / `time_slot` / `priority` UI 顯示「中文(英文)」，canonical 仍英文 enum。
- 行程 `day_type` 選項在 1-G-5 不含 `specific_date`。
- 行程地點 `selectbox` 顯示 `location.name`，value 仍 `location_id`。
- 每個角色 expander 內「角色行程」區塊列出該角色既有 `schedule`，每筆顯示「{day_type 中文} {time_slot 中文} @ {地點 name} [{priority 中文}] order=N」。
- 每筆行程右側「刪除」按鈕，按下後從該角色 `schedule` 移除該筆。
- 行程清單顯示遇到已被刪除的 `location_id` 時，顯示原 ID 並加註「(地點已刪除)」。
- 「新增行程」表單於每個角色編輯 expander 內運作；頁面下方既有共用「新增角色行程」表單已移除。

白名單顯示與進階設定收納：

- 表情、服裝、位置白名單收進編輯 expander 內「進階設定 (Advanced Settings)」展開區。
- `multiselect` 顯示文字為純中文 label，移除 `(id)` 後綴。
- 進階設定區頂部顯示「💡 受控素材清單」常駐說明文字。

不破壞既有功能：

- Phase 1-G-1 / 1-G-2 / 1-G-3 / 1-G-4 行為不被破壞（主角頁、地點頁、語意型欄位 `id + label`、地點清單 / 模板 / 刪除防呆 / 中文化）。
- 匯出 Setup Package 後 `characters[].schedule` canonical 仍保存英文 enum；可 parse 回 `SetupPackage`。
- exporter / parser / linter 行為與 1-G-4 一致。

Streamlit 實作限制：

- 多角色同時開啟編輯 expander 時，Streamlit 不可 raise duplicate widget key 例外。
- 進階設定區未使用 nested `st.expander`；採 `st.checkbox` / `st.toggle` 切換顯示，state key 含 `character_id` 後綴。
- 不同角色「進階設定展開狀態」彼此獨立，不互相影響。
- 新增角色 `character_id` 為空字串時被擋下並顯示 `st.error`，不寫入 `st.session_state["characters"]`。
- 新增角色 `character_id` 與既有重複時被擋下並顯示 `st.error`。
- 新增角色 `character_id` 含中文、空格或大寫等不符 canonical ID 格式時被擋下並顯示 `st.error`。
- 編輯 expander 不提供修改 `character_id` 的 widget；該欄位於進階設定區為唯讀文字。
- 刪除角色（通過引用防呆後）會清掉該角色所有 `*_<character_id>` keyed session state；後續操作或新增同名角色時不殘留舊 state。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 角色清單 label | 進入角色頁，至少先建立 1 名角色 | expander label 顯示「{name} · {性別中文} · {定位中文}」，無英文 ID | [ ] |
| 2 | 列旁刪除按鈕 | 角色清單右側 | 每列右側可見「刪除」按鈕，不需展開 expander | [ ] |
| 3 | endings 引用防呆 | 建立角色 → 建立 ending 並指向該角色 → 刪除該角色 | 阻止刪除，toast 列出 `ending_id` | [ ] |
| 4 | status_flags 引用防呆 | 建立角色 → 建立 status_flag `target` 為該角色 ID → 刪除該角色 | 阻止刪除，toast 列出 `status_id` | [ ] |
| 5 | 特殊 target 不誤判 | 建立 status_flag `target = protagonist` → 刪除任意 NPC | 不被視為引用，可正常刪除 | [ ] |
| 6 | inline 編輯基本資訊 | expander 展開 → 修改 display_name 等 → 儲存修改 | JSON 對應欄位更新 | [ ] |
| 7 | inline 編輯性格 / 秘密 | 修改性格標籤、增減秘密（最多 3）→ 儲存修改 | `personality_tags` / `secrets` 對應更新 | [ ] |
| 8 | inline 編輯白名單 | 進階設定區修改表情/服裝/位置白名單 → 儲存修改 | `allowed_emotions/costumes/positions` 對應更新，底層保留 `{id, label}` | [ ] |
| 9 | 取消編輯 | expander 修改任意欄位 → 取消 | 角色資料不變；再次展開 expander 顯示當前值 | [ ] |
| 10 | 多角色不互相污染 | 同時展開 2 名角色 → 各自修改性格 → 各自儲存 | 兩名角色性格獨立更新，未互相覆蓋 | [ ] |
| 11 | 行程 enum 中文化 | expander 內新增行程，查看 day_type / time_slot / priority 選項 | 顯示「中文(英文)」 | [ ] |
| 12 | specific_date 不顯示 | 查看行程 day_type 選項 | 不出現 `specific_date` 選項 | [ ] |
| 13 | 行程地點選單中文 | 查看行程地點 selectbox | 顯示地點 `name`（中文），底層仍是 `location_id` | [ ] |
| 14 | 行程清單顯示 | 為角色建立 1-2 筆行程 → 查看 expander 行程清單 | 顯示「平日 早上 @ XXX [一般] order=N」格式 | [ ] |
| 15 | 行程刪除 | 行程清單右側按刪除 | 該筆從角色 `schedule` 移除 | [ ] |
| 16 | 行程地點 dangling | 建立行程後刪除其地點 | 行程清單顯示原 `location_id` 並加註「(地點已刪除)」 | [ ] |
| 17 | 新增行程位置 | 比對頁面下方是否仍有共用「新增角色行程」表單 | 已移除；新增行程僅能在 expander 內進行 | [ ] |
| 18 | 白名單純中文 | 進階設定區查看表情/服裝/位置 multiselect | 顯示僅中文 label，無 `(id)` 後綴 | [ ] |
| 19 | 受控素材說明 | 進階設定區頂部 | 顯示「💡 受控素材清單」常駐說明文字 | [ ] |
| 20 | character_id 唯讀 | 編輯 expander 進階設定區 | 顯示 `character_id` 為唯讀文字 | [ ] |
| 21 | 匯出 canonical | 完成設定後匯出 Setup Package | `schedule[*]` 仍保存英文 enum；可被 parser 還原 | [ ] |
| 22 | 多角色 expander 並行 | 同時展開 3 名角色編輯 expander | 不噴 duplicate widget key 例外；各 widget 互不干擾 | [ ] |
| 23 | 進階設定切換 | 點擊「顯示進階設定」checkbox / toggle | 白名單區顯示/隱藏；不同角色切換 state 彼此獨立 | [ ] |
| 24 | 新增空 ID 被擋 | 新增角色表單 `character_id` 留空 → 提交 | `st.error` 顯示，無新角色寫入 | [ ] |
| 25 | 新增重複 ID 被擋 | 新增與既有角色相同 `character_id` | `st.error` 顯示，無新角色寫入 | [ ] |
| 26 | 新增非法 ID 被擋 | `character_id` 輸入中文、含空格或大寫 → 提交 | `st.error` 顯示，無新角色寫入 | [ ] |
| 27 | 編輯期不可改 ID | 編輯 expander 內找不到可編輯的 `character_id` widget | 進階設定區僅顯示為唯讀文字 | [ ] |
| 28 | 刪除清空 state | 為角色 A 修改性格但不儲存 → 刪除 A → 再新增同 ID 角色 | 新角色 expander 不殘留 A 的編輯草稿，舊行程刪除按鈕 state 也不殘留 | [ ] |

##### 1-G-5 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| Phase 1-G-1 到 1-G-4 回歸不破壞 | 必須 |
| 1-G-5 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#28 全過 | 必須 |
| 匯出的 Setup Package 中 `characters[].schedule` canonical enum 維持英文 | 必須 |
| `characters[].allowed_emotions/costumes/positions` 仍保存 `{id, label}` 結構 | 必須 |
| 多角色同時開啟編輯 expander 不噴 Streamlit duplicate widget key 例外 | 必須 |
| 進階設定區未使用 nested `st.expander` | 必須 |

---

#### 1-G-6 拆分旗標/結局頁與結局頁 UX 完整化

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py`（拆 `_tab_flags()` 與 `_tab_endings()`、結局 inline 編輯、`_collect_existing_ids` / `_purge_ending_state` helpers、修補角色頁 character_id 跨類型唯一，且 `_collect_existing_ids` 納入 `status_id`） | 手動驗收 / 必要時補 `tests/test_streamlit_uiw.py` |
| `src/sandbox_dating_sim/exporter/` 與 `src/sandbox_dating_sim/parser/` | 補 `tests/test_exporter.py` 對最小可用 fixture 的 roundtrip 測試 |
| `src/sandbox_dating_sim/uiw/linter.py` | 補 `tests/test_uiw_linter.py` 對最小可用 fixture 不報 error 的測試 |

1-G-6 不應修改 schema、linter 規則、exporter / parser 邏輯；僅新增測試 fixture 與測試 case。

##### 必測項目

分頁拆分：

- `PAGE_LABELS` 第 5、6 項分別為「旗標與狀態(Flags & Status)」與「結局(Endings)」，順序在「角色設定」之後、「預覽與匯出」之前。
- 「下一頁」按鈕從旗標與狀態頁進入結局頁，從結局頁進入預覽與匯出頁。
- 旗標 / 狀態頁內容與 1-G-5 之前的合併頁旗標 / 狀態區塊功能完全一致。
- 結局頁內容為 1-G-6 改造後的版本；原合併頁中的結局區塊已移出。

結局清單顯示：

- 結局清單 expander label 顯示「{title or ending_id} · {priority 中文} · {target 顯示}」。
- title 為空時 expander label fallback 顯示 `ending_id`。
- target 為 None 顯示「（無）」、`"global"` 顯示「全局結局」、character_id 顯示對應 `display_name`；歷史 / 外部匯入資料若有 dangling reference，需保留原 ID 並加註「(角色已刪除)」。
- 每列右側可直接刪除，不需展開 expander。

inline 編輯：

- 編輯既有結局：修改 title / ending_type / target_character_id / description / priority / route_tags 並按「儲存修改」後，`st.session_state["endings"][i]` 對應欄位更新。
- 編輯既有結局 required_flags / forbidden_flags：multiselect 選擇 boolean flag → 點「加入到必須條件 / 禁止條件」→ free-form text_area 對應字串被 append；儲存後 ending 對應 list 更新。
- 編輯既有結局 required_stats：直接修改 free-form text_input → 儲存後 list 更新。
- 「取消」清空該結局 keyed temp state；下次開啟 expander 重新依當前值初始化。
- 多結局同時開啟 expander 編輯，keyed temp state 不互相污染。
- `ending_id` 在編輯 expander 為唯讀。

中文化：

- priority selectbox 顯示「中文(英文)」：必定觸發(critical) / 主線(main) / 路線(route) / 一般(normal) / 背景(ambient)。
- priority selectbox 下方顯示五級語意 caption。
- target_character_id selectbox 顯示中文 / 「（無 / 不指定）」/「全局結局 (global)」。
- 匯出後 canonical 仍保存英文 priority enum 與英文 character_id / "global" / None。

F-γ multiselect 行為：

- multiselect 顯示現有旗標清單。
- 點擊「加入到必須條件」按鈕後，所選 flag 展開為 placeholder 字串 append 到 free-form text_area，multiselect 清空。
- boolean flag 展開為 `flag.<id> == true` / `flag.<id> == false`。
- integer flag 展開為 `flag.<id> == <initial_value>`（不加引號）。
- string / enum flag 展開為 `flag.<id> == "<initial_value>"`（雙引號包住字串；必要時 escape `"` 與 `\`）。
- multiselect 取消勾選不會自動移除已加入的字串。
- flags=[] 時 multiselect 空，下方顯示「尚未設定旗標」caption。

跨類型 ID 唯一即時擋（結局頁）：

- 新增 ending_id 為空字串時被擋下並顯示 `st.error`，不寫入 `endings`。
- 新增 ending_id 與既有 world_id / character_id / location_id / flag_id / status_id / ending_id 任一重複時被擋下。
- 新增 ending_id 含中文、空格或大寫等不符 canonical ID 格式時被擋下。

附帶修正（1-G-5 character_id 跨類型唯一）：

- 新增 character_id 與既有 world_id / location_id / flag_id / status_id / ending_id / character_id 任一重複時被擋下並顯示 `st.error`。
- 既有 1-G-5 character_id 空 / 重複 / 非法格式擋下行為不被破壞。

最小可用 Setup Package：

- fixture：`flags=[]` / `status_flags=[]`，其他 World / Protagonist / locations / characters / endings 全有，且該 ending `required_flags=[]` / `forbidden_flags=[]` / `required_stats=[]`。
- UIW Linter 對此 fixture 不報 error。
- `SetupPackageExporter.to_markdown()` 成功產出 MD。
- MD 透過 parser 還原為 `SetupPackage`，roundtrip 一致。

實作限制（Streamlit 層）必測：

- 多結局同時開啟編輯 expander 不可 raise duplicate widget key 例外。
- 結局編輯 expander 不使用 nested `st.expander`。
- 編輯 expander 不提供修改 `ending_id` 的 widget。
- 刪除結局後清空所有 `*_<ending_id>` keyed session state；新增同名 ending_id 不殘留舊 state。

不破壞既有功能：

- Phase 1-G-1 / 1-G-2 / 1-G-3 / 1-G-4 / 1-G-5 行為不被破壞。
- 1-G-5 角色頁 inline 編輯、刪除引用防呆、行程清單功能皆正常。
- 既有 fixture（含 flags / status_flags / endings 都有設定的 fixture）匯出 roundtrip 不變。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 分頁拆分 | 啟動 UIW 查看上方導覽 | 第 5 項「旗標與狀態」、第 6 項「結局」分開顯示 | [ ] |
| 2 | 下一頁順序 | 旗標與狀態頁按下一頁 | 進入結局頁；結局頁按下一頁進入預覽與匯出 | [ ] |
| 3 | 旗標頁內容保留 | 旗標與狀態頁建立 1 個 boolean flag、1 個 status_flag | 與 1-G-5 之前合併頁行為一致 | [ ] |
| 4 | 結局清單 label | 結局頁建立 1 個結局後查看清單 | label 顯示「{title} · {priority 中文} · {target 顯示}」，無原始 JSON dump | [ ] |
| 5 | 列旁刪除按鈕 | 結局清單右側 | 每列右側可見「刪除」按鈕，不需展開 | [ ] |
| 6 | title 為空 fallback | 建立結局時 title 留空 → 查看清單 | label 第一段 fallback 顯示 ending_id | [ ] |
| 7 | target 為 None | 結局 target_character_id 留 None | label 顯示「（無）」 | [ ] |
| 8 | target 為 global | 結局 target_character_id 設為 global | label 顯示「全局結局」 | [ ] |
| 9 | target 為角色 | 結局關聯任一角色 | label 顯示該角色 display_name（中文） | [ ] |
| 10 | target dangling 防禦顯示 | 以 fixture / session_state 注入 `target_character_id` 指向不存在角色的 ending → 查看結局清單 | label 保留原 ID 並加註「(角色已刪除)」；正常 UI 刪除被 ending 引用的角色時應先被防呆阻擋 | [ ] |
| 11 | inline 編輯基本資訊 | expander 展開 → 修改 title / description / ending_type / route_tags → 儲存 | 對應欄位更新 | [ ] |
| 12 | inline 編輯 target | expander 內切換 target_character_id 並儲存 | 對應欄位更新；canonical 仍存英文 ID / "global" / None | [ ] |
| 13 | inline 編輯 priority | 切換 priority 中文選項並儲存 | canonical 仍存英文 enum；UI 顯示中文(英文) | [ ] |
| 14 | priority caption | 查看 priority selectbox 下方 | 顯示五級語意 caption（critical 到 ambient） | [ ] |
| 15 | F-γ 加入必須 | 旗標頁建立 1 個 boolean flag → 結局 expander multiselect 選該 flag → 點「加入到必須條件」 | text_area append `flag.<id> == true`；multiselect 清空；儲存後 ending.required_flags 含該字串 | [ ] |
| 16 | F-γ 加入禁止 | 同上但點「加入到禁止條件」 | text_area append 到 forbidden 欄位；儲存後 ending.forbidden_flags 含該字串 | [ ] |
| 17 | F-γ integer | 建立 integer flag（initial_value 例 100）→ 結局 expander 加入該 flag | text_area append `flag.<id> == 100` | [ ] |
| 17a | F-γ string / enum | 建立 string 或 enum flag（initial_value 例 open）→ 結局 expander 加入該 flag | text_area append `flag.<id> == "open"` | [ ] |
| 18 | F-γ 取消勾選不移除 | 加入後 multiselect 取消勾選 | text_area 字串保留，未自動移除 | [ ] |
| 19 | F-γ 修改條件值 | text_area 直接編輯字串（true → false）→ 儲存 | ending 對應 list 反映修改 | [ ] |
| 20 | F-γ flags=[] 提示 | 不建立任何 flag 直接打開結局 expander | multiselect 空，顯示「尚未設定旗標」caption | [ ] |
| 21 | required_stats free-form | 在 required_stats text_input 打 `stat.Cash >= 100` → 儲存 | ending.required_stats 反映該字串 | [ ] |
| 22 | 取消編輯 | expander 修改任意欄位 → 取消 | ending 資料不變；再展開顯示當前值；temp_req_flags / temp_forb_flags 清空 | [ ] |
| 23 | 多結局並行編輯 | 同時展開 2 個結局 expander → 各自修改條件 → 各自儲存 | 兩結局欄位獨立更新；無 duplicate widget key 例外 | [ ] |
| 24 | ending_id 唯讀 | 編輯 expander 內 | 找不到可編輯 ending_id widget；以唯讀文字顯示 | [ ] |
| 25 | 新增空 ending_id | 新增結局 ending_id 留空 → 提交 | st.error 顯示，無新結局寫入 | [ ] |
| 26 | 新增重複 ending_id（同類） | 新增與既有結局相同 ending_id | st.error 顯示，無新結局寫入 | [ ] |
| 27 | 新增重複 ending_id（跨類） | 將 ending_id 設為某既有 character_id / status_id | st.error 顯示，無新結局寫入 | [ ] |
| 28 | 新增非法 ending_id | ending_id 輸入中文、含空格或大寫 | st.error 顯示，無新結局寫入 | [ ] |
| 29 | 結局刪除清 state | 為某結局 multiselect 加入旗標但不儲存 → 刪除該結局 → 重新建立同 ending_id | 新結局 expander 不殘留前一個結局的 temp 編輯草稿 | [ ] |
| 30 | character_id 跨類重複擋 | 角色頁新增 character_id = 某既有 location_id / flag_id / status_id / ending_id | st.error 顯示，無新角色寫入 | [ ] |
| 31 | 最小可用匯出 | 完成 World / Protagonist / 1 地點 / 1 角色 / 1 結局，跳過 flags 與 status_flags → 匯出 | UI 顯示 passed 與輸出檔案位置；該檔案存在 | [ ] |
| 32 | 最小可用 parse | 用 CLI parser 載入 #31 產生的 MD | 成功還原為 `SetupPackage`，無例外 | [ ] |

##### 1-G-6 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| Phase 1-G-1 到 1-G-5 回歸不破壞 | 必須 |
| 1-G-6 新增/調整測試通過 | 必須 |
| 手動驗收 #1-#32 全過 | 必須 |
| `PAGE_LABELS` 為 7 項，且第 5、6 項為「旗標與狀態(Flags & Status)」「結局(Endings)」 | 必須 |
| 匯出的 Setup Package 中 `endings[].priority` canonical enum 維持英文 | 必須 |
| 匯出的 Setup Package 中 `endings[].target_character_id` canonical 仍為英文 ID / "global" / None | 必須 |
| 最小可用 fixture（flags=[] / status_flags=[]）UIW Linter 通過、exporter 成功、parser roundtrip 一致 | 必須 |
| 多結局同時開啟編輯 expander 不噴 Streamlit duplicate widget key 例外 | 必須 |
| 結局編輯 expander 未使用 nested `st.expander` | 必須 |
| 角色頁 character_id 跨類型唯一即時擋已生效 | 必須 |

---

#### 1-G-7 新增流程自動產生唯一 canonical ID

##### 測試對象

| 模組 | 測試檔 |
| :--- | :--- |
| `src/sandbox_dating_sim/ui/streamlit_uiw.py`（共用 ID 產生 helper、地點 / 角色 / 旗標 / 狀態 / 結局新增流程） | `tests/test_streamlit_uiw.py` |
| `src/sandbox_dating_sim/core/ids.py` 或 UI helper（若抽出純函式） | `tests/test_ids.py` 或 `tests/test_streamlit_uiw.py` |

1-G-7 不應修改 schema、exporter、parser。UIW Linter 的 `status_id` 補洞仍排 1-G-8，1-G-7 只要求 UI 新增流程使用全域 ID 集合避免建立衝突。

##### 必測項目

純函式單測（必跑、不依賴 Streamlit AppTest）：

- `_slugify_label("咖啡廳") == "ka_fei_ting"`、`_slugify_label("Cafe Shop") == "cafe_shop"`、`_slugify_label("") == "unnamed"`。
- `_slugify_label` 處理中文標點 `、`、`，`、`。`、`：` 與空白同樣轉 `_` 並合併重複。
- `_slugify_label` 對長 description 截斷至前 8 個 token，截斷後再合併重複 `_` 並去頭尾 `_`。
- `_make_unique_id("咖啡廳", "loc_", "unnamed", set()) == "loc_ka_fei_ting"`。
- suffix 連鎖：`existing = {"loc_x"} → loc_x_2`、`{"loc_x", "loc_x_2"} → loc_x_3`、`{"loc_x", "loc_x_2", "loc_x_3"} → loc_x_4`。
- suffix 順序遞增取最小未占用：`existing = {"loc_x", "loc_x_2", "loc_x_4"} → loc_x_3`。
- 跨類前綴隔離：`_make_unique_id("林小雨", "ch_", "unnamed", {"loc_lin_xiao_yu"}) == "ch_lin_xiao_yu"`。
- 空白或全無效來源使用 `unnamed`：`_make_unique_id("", "flag_", "unnamed", set()) == "flag_unnamed"`，不得產生 `flag_flag`、`flag_unnamed_unnamed` 類疊字。
- `inspect.signature(_generate_system_id)` 與 1-G-3 完成時一致；該函式不得新增 `prefix` 等參數。

自動產生 ID（UI 行為）：

- 手動新增地點時，使用中文 `name` 可自動產生合法 `location_id`，不要求使用者填英文 ID。
- 新增 NPC 角色時，使用中文 `display_name` 可自動產生合法 `character_id`，不要求使用者填英文 ID。
- 新增旗標時，使用中文 `description` 可自動產生合法 `flag_id`，不要求使用者填英文 ID。
- 新增狀態旗標時，使用中文 `label` 可自動產生合法 `status_id`，不要求使用者填英文 ID。
- 新增結局時，使用中文 `title` 可自動產生合法 `ending_id`，不要求使用者填英文 ID。

唯一性：

- 同名來源連續新增三筆以上時，suffix 順序遞增 `_2`、`_3`、`_4`，不覆蓋既有資料、不回填空缺。
- 自動 ID 路徑下，prefix 隔離使各類型永不跨類衝突。**跨類唯一只在進階手動 ID 入口、地點模板套用、既有匯入資料三種場景觸發**；其中：
  - 進階手動 ID 跨類撞同名 → 顯示錯誤，不新增。
  - 地點模板套用撞既有 ID → **自動 suffix 避讓（不阻止建立）**。
  - 匯入既有資料 → 維持原狀，由 UIW Linter 後續處理（不在 1-G-7 範圍）。
- 自動產生 ID 必須納入 `world_id / 固定 protagonist / character_id / location_id / flag_id / status_id / ending_id` 全域集合；`_collect_existing_ids` 須補入 `protagonist`。
- 旗標 ID 來源使用 `description`，不得要求 schema 不存在的 `label` 欄位。
- 長 description 產生 slug 時最多保留前 8 個 token，避免 ID 過長。
- 中文標點如 `、`、`，`、`。`、`：` 會和空白一樣轉為 `_` 並被後續清理。
- 空白或全無效來源使用中性 fallback `unnamed`，再加 prefix，例如 `flag_unnamed`，不得產生 `flag_flag`、`loc_location` 這類重複語意 ID。
- `_make_unique_id` 只能包裝既有 `_generate_system_id`；不得修改 `_generate_system_id` 的簽章或讓 1-G-3 semantic choice 被加上類型 prefix。

UI 行為：

- 英文 ID 欄位不再是一般使用者必填主欄位。
- 自動產生的系統 ID 在 **submit 後**以 caption 顯示於新增結果旁，不在 form 內 widget callback 即時預覽。
- 若提供手動調整 ID，非法格式 / 重複 ID（含與固定 `protagonist` 衝突）會被擋下，且不清空使用者已輸入內容。
- 新增成功後才清空該表單 draft state。
- **自動 ID 路徑驗證失敗（必填欄位空、衝突無解等）也必須保留使用者輸入**，不清空 draft。

不破壞既有功能：

- 地點模板套用仍正常；模板建議 ID 衝突時**自動 suffix 避讓**（不阻止建立），允許重複套用同一模板。
- 角色、地點、旗標、狀態、結局新增後仍可進入 Review & Export。
- 匯出的 Setup Package 中 canonical ID 仍是英文，中文 name / display_name / label / title / description 不遺失。
- parser roundtrip 不因自動產生 ID 受影響。
- 1-G-3 自訂語意選項（personality / occupation / secret）行為不變，產出 ID 不帶類型 prefix。

##### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 地點自動 ID | 手動新增地點，名稱輸入「咖啡廳」 | 新增成功，系統 ID 自動為合法英文，例如 `loc_ka_fei_ting`，submit 後 caption 顯示 | [ ] |
| 2 | NPC 自動 ID | 新增角色，姓名輸入「林小雨」 | 新增成功，系統 ID 自動為合法英文，例如 `ch_lin_xiao_yu` | [ ] |
| 3 | 旗標自動 ID | 新增旗標，description 輸入「是否失業」 | 新增成功，系統 ID 自動為合法英文，例如 `flag_shi_fou_shi_ye` | [ ] |
| 4 | 狀態自動 ID | 新增狀態，label 輸入「過勞」 | 新增成功，系統 ID 自動為合法英文，例如 `status_guo_lao` | [ ] |
| 5 | 結局自動 ID | 新增結局，標題輸入「蘇菲好結局」 | 新增成功，系統 ID 自動為合法英文，例如 `end_su_fei_hao_jie_ju` | [ ] |
| 6 | 同名 suffix 連鎖 | 連續新增**四個**同名地點（皆輸入「咖啡廳」） | suffix 順序遞增為 `loc_ka_fei_ting`、`_2`、`_3`、`_4`，四筆皆保留 | [ ] |
| 7 | 跨類唯一（進階手動 ID） | 透過進階手動 ID 在 NPC 建立 `xxx`，再以進階手動 ID 在地點輸入 `xxx` | 第二次輸入被擋下，顯示錯誤訊息，原輸入保留 | [ ] |
| 8 | ID 非必填 | 查看各新增表單 | 英文 ID 不再是主流程必填欄位 | [ ] |
| 9 | 進階手動 ID 防呆 | 進階手動 ID 入口輸入中文 / 大寫 / 既有重複 ID / 字面 `protagonist` | 全部被擋下，顯示錯誤，原輸入保留 | [ ] |
| 10 | 匯出 canonical | 完成最小設定並匯出 | Setup Package 保存英文 ID，中文顯示文字不遺失，可 parse 回 `SetupPackage` | [ ] |
| 11 | 長描述截斷 | 新增旗標，description 輸入長中文句子（>8 token） | 自動 ID 只取前 8 個 token，仍合法且可讀 | [ ] |
| 12 | 空值 fallback | 來源文字空白或只含無效符號（純標點） | 自動 ID 使用中性 fallback，例如 `flag_unnamed`；連續新增則 `flag_unnamed_2`、`_3` | [ ] |
| 13 | 1-G-3 語意型回歸 | 走完 1-G-3 自訂語意選項流程（personality / occupation / secret），產出 ID 比對 | 產出 ID 與 1-G-3 完成時一致，不帶類型 prefix；`_generate_system_id` 簽章未變 | [ ] |
| 14 | 自動 ID 失敗保留 draft | 新增地點時故意留空 `name` 欄位送出，或讓自動 ID 撞無解衝突 | 提交失敗，顯示錯誤；其他已填欄位（type / description / tags）保留，不清空 | [ ] |
| 15 | 預覽 ID 時機 | 在 form 內持續修改來源欄位但**不送出** | form 內**不**即時顯示自動 ID；按 submit 後 caption 才出現最終 ID | [ ] |
| 16 | 模板衝突避讓 | 連續套用同一個地點模板（例如「家」）兩次 | 第二次自動產生 `_2` suffix（如 `loc_home_2`），不阻止建立 | [ ] |
| 17 | 實機 end-to-end | 啟動 UIW、跑完最小可用設定、export → parser roundtrip | UIW Linter 通過、export 成功、parser 載回的 `SetupPackage` 與匯出前一致 | [ ] |

##### 1-G-7 通過標準

| 條件 | 必要性 |
| :--- | :--- |
| Phase 1-A 到 1-F 回歸測試通過 | 必須 |
| Phase 1-G-1 到 1-G-6 回歸不破壞 | 必須 |
| 1-G-7 純函式單測（`_slugify_label` / `_make_unique_id`）全過 | 必須 |
| 1-G-7 UI 行為測試（Streamlit AppTest）全過 | 必須 |
| 手動驗收 #1-#17 全過 | 必須 |
| 地點 / NPC / 旗標 / 狀態 / 結局新增流程皆不要求一般使用者輸入英文 ID | 必須 |
| 自動產生 ID 符合 canonical ID 格式且全域唯一 | 必須 |
| `_generate_system_id` 簽章與行為與 1-G-3 完成時一致 | 必須 |
| 既有 ID rename / reference migration 未被混入 1-G-7 | 必須 |

---

## Phase 1 全階段回歸測試

Phase 1-F 完成後，執行：

```powershell
uv run pytest tests/ -v -m "not integration" --tb=short
```

再手動執行：

```powershell
uv run python -m sandbox_dating_sim.cli export-setup tests/fixtures/setup_minimal.yaml --out examples/
uv run streamlit run src/sandbox_dating_sim/ui/streamlit_uiw.py
```

通過標準：

| 條件 | 必要性 |
| :--- | :--- |
| 自動測試全過 | 必須 |
| CLI 可輸出 Setup Package MD | 必須 |
| Streamlit UI 可輸出 Setup Package MD | 必須 |
| 兩種產物都可 parse 回 SetupPackage | 必須 |

> Phase 1 完成後，才進入 Phase 2：Event Blueprint MVP。

---

## Phase 2 之後的測試方向

Phase 2 後續應另行補完整測試細節。先保留方向：

### Phase 2：Event Blueprint MVP

必測：

- Event Blueprint schema。
- Prompt builder 不要求完整對話。
- Blueprint parser。
- Blueprint linter。
- 新 flag 必須列入 `new_flags_proposed`。
- expected assets 不超出白名單。

### Phase 3：Map Manager / Status Manager / Route Validator

必測：

- Status Flag lifecycle。
- Schedule Conflict Resolution。
- FreeRoamState。
- container / sub_location 進入權限。
- Critical Path Mode。

### Phase 4：Scene Draft / Final Script

必測：

- Scene Draft 不可修改 Event Blueprint 邏輯。
- choice_id 一致。
- result 一致。
- time_cost / goto 不變。
- 素材白名單檢查。

---

## 故障排除

| 問題 | 常見原因 | 解法 |
| :--- | :--- | :--- |
| `ModuleNotFoundError: No module named 'sandbox_dating_sim'` | `pythonpath` 未設或不在專案根目錄執行 | 確認 `pyproject.toml` 與執行目錄 |
| `ValidationError` 發生在 linter 前 | 缺 required field，被 Pydantic schema 擋下 | 改寫 schema 測試，不要放進 linter 測試 |
| Roundtrip 後 list 順序不同 | YAML dump/load 或 model dump 未保序 | 使用 Python dict/list 原順序，測試固定順序 |
| 中文 label 變亂碼 | 編碼不是 UTF-8 | 讀寫檔案指定 UTF-8 |
| `uiw_version` 不是 `1.2` | 常數或 schema 預設值未更新 | 檢查 `core/constants.py` 與 `SetupPackage` 預設值 |
| Streamlit schedule 下拉沒有地點 | 地點尚未存入 session state 或只顯示 container | 下拉應讀取 `is_visitable=true` 的 sub_location / standalone |
| container 驗證沒報錯 | linter 未檢查子地點 | 加上 container without sub_location 測試 |
