# 已知問題2

本文件只保留目前尚未完成或尚未通過驗收的項目。`舊文件/` 仍可忽略；規格與實作以根目錄最新文件為準。

最後更新：2026-05-07

---

## 目前狀態摘要

- Phase 2 Event Blueprint MVP：已完成並驗證通過；2-E Setup Package Phase 2 Readiness 已補上。
- Phase 1-G-9 ScheduleEntry `specific_date` / `schedule_id`：已完成。
- 目前主要剩餘待辦：1-G-8 旗標/狀態 UI 的完整手動驗收覆蓋。

### Phase 2 完成紀錄

完成範圍：

- 2-A-0 Setup Prerequisites：`protagonist_home`、固定 outputs layout、同名不覆寫。
- 2-A Event Blueprint Schema：`schema/blueprint.py`、EventBlueprint / BlueprintEvent / BlueprintChoice models、Markdown shell + exactly one YAML code block。
- 2-B Blueprint Prompt Builder：`prompts/blueprint_prompt.py`、三種 scope、互動式 CLI。
- 2-C Blueprint Parser + Linter：`pipeline/blueprint_parser.py`、`validation/blueprint_linter.py`、blueprint-only / full validation、strict DSL。
- 2-D Blueprint Logic Walkthrough：`walkthrough/blueprint_walkthrough.py`、`checkpoint.py`、`route_graph.py`、互動式 CLI。
- 2-E Setup Package Phase 2 Readiness：UIW 匯出必須至少 1 個 character、至少 2 個 endings；`flags` / `status_flags` 可空；Prompt 禁止 AI 新增 canonical characters，只允許純文字背景 NPC。

2026-05-07 驗證結果：

- Phase 2-E 目標測試：`119 passed in 1.12s`（提升權限重跑；一般權限曾被 `.pytest_tmp_phase2e` 清理/掃描權限擋住）。
- 全專案非 integration 回歸：`274 passed in 2.11s`（提升權限重跑；`--basetemp .pytest_tmp_phase2e_full`）。

2026-05-06 驗證結果：

- Phase 2 指定測試：`138 passed in 1.47s`。
- 全專案非 integration 回歸：`269 passed in 2.14s`。
- 2-D 補驗四項已通過：`is_visitable`、candidate sorting、timezone-aware checkpoint、checkpoint world/blueprint filter。

注意：

- Windows/OneDrive 路徑下 pytest 暫存目錄可能出現 `PermissionError: [WinError 5]`。若測試本體已通過、只在 temp cleanup 失敗，依專案規則視為環境權限問題；用專案 venv、`--basetemp` 與提升權限重跑確認。

---

## Phase 1-G-8：旗標/狀態 UI 行為手動驗收覆蓋

狀態：功能面已處理，自動測試已補強；仍待完整手動驗收覆蓋。

驗證摘要：

- 1-G-8 目標測試曾提升權限重跑通過：`62 passed in 1.02s`。
- Phase 1 非 integration 回歸曾提升權限重跑通過：`109 passed in 1.19s`。
- 2026-05-03 針對 1-G-8 / Streamlit UI 測試重驗：`18 passed in 0.84s`。
- 新增 Status 表單 duration/value/clear_rule/permanent 行為已處理。
- 新增 Flag / Status Flag 驗證失敗保留 draft state 已處理。

目前仍建議補齊的手動驗收：

- status inline 編輯儲存 / 取消 / 刪除防呆。
- status `targets` multiselect：主角、NPC、dangling target、dedupe。
- duration.type 每個選項的 value 顯示與 canonical 寫入。
- 新增 / 編輯 permanent 缺 `permanent_reason` 不儲存。
- effect YAML UI corner cases：空字串、非 list、list 元素非 dict、dict key 非 str。
- 成功刪除 status 後清空對應 keyed session state。
- 新增 Status 表單驗證失敗後 draft state 保留。
- 新增 Status 表單的 `clear_rule` / `permanent` 行為。

影響範圍：

- `src/sandbox_dating_sim/ui/streamlit_uiw.py`
- `tests/test_uiw_1g8.py`
- `tests/test_streamlit_uiw.py` 或新增專用 UI 測試檔
